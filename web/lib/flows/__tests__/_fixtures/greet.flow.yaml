schemaVersion: 1
name: Greet (fixture)
compat:
  engine_min: 3.0.0
# M11c (AC-6): a settings-less graph flow. Absence of `settings`
# MUST NEVER trigger an enforcement refusal.
nodes:
  - id: greet
    type: cli
    action:
      command: "echo hello"
    transitions:
      success: done
