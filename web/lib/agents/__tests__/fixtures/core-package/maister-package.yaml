# MAIster core package — platform agents that ship with the control plane.
# Contract: docs/pv/package-management.md (MAIster repo). This package carries
# NO flows; it exists to deliver core platform agents (maister-agents/*.md)
# such as the Triager and Brain Improver. The version pin is the git tag
# (core/vX.Y.Z), never a field in this file.
schemaVersion: 1
name: core
metadata:
  title: "MAIster Core"
  summary: "Core platform agents shipped with MAIster: the Triager (routes simple-intent tasks) and Brain Improver (drafts Project Brain proposals)."
  links:
    - { kind: docs, title: "Triage domain", url: "https://github.com/maisterhq/maister/blob/main/docs/system-analytics/triage.md" }
# No flows: a platform-agent-only package. The agents catalog projects each
# maister-agents/<stem>.md at the package root under id `core:<stem>`.
flows: []
capabilities: []
# No MCP server templates: core agents reach MAIster through the built-in
# maister MCP facade with ephemeral agent tokens, not a package-shipped server.
mcps: []
