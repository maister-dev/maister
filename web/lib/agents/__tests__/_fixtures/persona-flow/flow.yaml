# Local TEST fixture for agent-driven-flow.integration.test.ts — NOT a shipped
# flow. A single ai_coding node, so the agent-driven-flow persona injection can
# be asserted on the prompt the runner sends to the supervisor (M39, ADR-106).
schemaVersion: 1
name: persona-flow
compat:
  engine_min: 1.3.0
nodes:
  - id: code
    type: ai_coding
    action:
      prompt: "implement the node task"
    transitions:
      success: done
