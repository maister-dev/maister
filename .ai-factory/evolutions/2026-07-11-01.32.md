# Evolution: 2026-07-11 01:32

## Intelligence Summary

- **Patches analyzed:** 114 (incremental, cursor `2026-06-16-22.45.md` → through `2026-07-07-18.55.md`;
  the mis-named `20260602-122318-m13-assignment-lifecycle-review.md` was re-scanned as predicted by the
  previous run's cursor note — its points were already covered or folded into this batch's rules).
  6 parallel readers extracted **493 prevention points**: 64 already covered by existing skill-context
  rules, 429 uncovered → consolidated into **72 improvements** (39 new rules, 33 strengthenings).
  User reviewed all 72 individually ("Let me pick", multi-select batches) and approved **72/72**.
- **Stale rules:** none. Base `skills/aif-*/SKILL.md` files unchanged since the 2026-06-17 evolve
  (`git log --since=2026-06-17 -- .claude/skills/` empty) and skill-context files unchanged since that
  same run — the previous all-Case-D verdict carries over. Step 4 skipped.
- **Tech stack:** Next.js 16 + React 19 + HeroUI v3, TypeScript strict, Drizzle/Postgres (+ SQLite
  dialect), ACP supervisor daemon (claude/codex ready; gemini/opencode/mimo readiness-gated), vitest
  unit + testcontainers integration + Playwright e2e, EN+RU i18n, `MaisterError` taxonomy.

## Recurring Patterns Identified

- Concurrency depth beyond prior rules (re-assert predicate under lock, guard-every-edge, claim counting, release tokens, converge-to-winner) — ~14 occurrences
- False-green tests (integration lane not run, mock-shaped coverage, vacuous asserts, partial matchers, actor mismatch) — ~12
- Docs drift incl. historical-vs-current classification, both-direction OpenAPI drift, status-flip stragglers — ~12
- ACP adapter specifics (undici timeouts, usage casing/turn dedup, session lookup by runId, materialization surfaces, ground-once prompting) — ~8
- MCP-facade / OpenAPI contract-mirror drift (TOOL_SPECS, dispatchTool, AGENT_TOKEN_SCOPES) — ~7
- React/CSS-grid long-content traps (minmax(0,1fr), min-w-0, refs-vs-state, mounted drawers) — ~6
- Fail-open verdict/producer routing (no-verdict invariant, unimplemented enums, silent defaults) — ~6
- `[FIX]` console-logging vs no-console/pino convention collisions — ~6
- Column-DROP/re-key sweep misses (raw SQL, e2e seeds, backfill-or-abort) — ~4
- Bounded-sweep starvation / poison items (attempt markers, rotating cursors) — ~4
- Retry caps unscoped to intent / unattended-launcher semantics — ~3
- Kill-switch & gated-resource consumer misses — ~3

## Improvements Applied (39 new rules + 33 strengthenings, all approved)

### aif-implement → skill-context (16 new + 14 strengthened)
**File:** `.ai-factory/skill-context/aif-implement/SKILL.md`
- NEW: ACP transport timeouts (undici dedicated Agent) · usage parsing + per-turn dedup · ground-once chat ·
  materialization surfaces + watchdog manifests · React state & layout · form/serialize normalization +
  tri-state controls · sweep progress + poison items · unattended launchers + scoped retry caps ·
  column-DROP whole-tree sweep · slot accounting + resume dispatch · fail-closed verdict routing + enum
  honesty · error-handling precision · runtime roots/git identity/SSE offsets/wire forms · MCP facade
  lockstep + contract test · raw-SQL correctness · mirror-pair diff-first + prompt pins
- STRENGTHENED: I2 (cross-tier acceptor same-commit), I7 (sink grammar, normalizer≠validator,
  consumer/engine limits), I9 (boundary/streaming caps, bound-the-read, paid sinks), I11 (runId-keyed
  lookup, teardown ordering, single-fire vs re-fire), I12 (exact-key redaction tests, union-exhaustive),
  I13 (phase-honest vocabulary, write-once helpers), I14 (8 new sub-points incl. re-assert-predicate,
  inert-backstop trap, insert-first, FK KEY SHARE, guard-every-edge), I15 (gates-before-side-effects,
  DB ownership, exclusive-create proof, counter bumps late), I16 (parse boundaries), I17 (symlink
  leaf/ancestor realpath, prune internal dirs, same-source ids, atomic renames), I19 (outbox in-tx,
  give-up CAS re-assert, every-terminal-route gates), I20 (pinned-source validation, durable-snapshot
  predicates, happy-path dispatcher reuse), I21 (WARN halves, enrichment try, primary-evidence
  preservation, preview provenance), I22 (Intl.NumberFormat locale, distinct copy)

### aif-plan → skill-context (4 new + 4 strengthened)
**File:** `.ai-factory/skill-context/aif-plan/SKILL.md`
- NEW: background automation design (progress/bounded retries/poison/wiring-seam/one gate) ·
  status-as-signal + per-concern predicates · migration data preservation · launch preconditions in
  create UIs + policy-axis interactions + supply-chain gates
- STRENGTHENED: P2 (in-code SSOT surfaces), P5/P6/P10 via the multi-store rule (compensation
  completeness + recovery predicates), P11 (nullable fanout, decoder enumeration, path relocations,
  scope triple), P12 (migration triple, prose forms, fold-back)

### aif-fix → skill-context (4 new + 3 strengthened)
**File:** `.ai-factory/skill-context/aif-fix/SKILL.md`
- NEW: verify-the-finding-first · [FIX]-logging yields to logger boundary · ACP/metric debugging
  heuristics · close-out gate battery
- STRENGTHENED: F1 (choke-point fixes, dispatch-branch twins, recurring sweep sets, recorded
  classifications), F2 (locked decisions, no deferring auto-merge gaps), F3 (prose forms, wrap-tolerant
  grep, both-case greps, historical-vs-current, migration triple)

### aif-review → skill-context (5 new + 6 strengthened)
**File:** `.ai-factory/skill-context/aif-review/SKILL.md`
- NEW: adversarial/invariant pass + INV checklist · #temporal ledger reads · #retry-safety flags ·
  #actor-authority gates · kill-switch/gated-resource review
- STRENGTHENED: R2 (status-not-marker, consumption ledgers, durable keys), R7 (contract-not-paraphrase,
  safer-behavior fixes, whole-operation reconcile, diagram-below-return), R8 (requireSession grep,
  changed-routes auth-first, taxonomy completeness), R10 (set-reuse semantics, silent-noop enums,
  nullable fanout, auth-surface fanout, exhaustiveness asserts), R12 (read-committed TOCTOU, pre-read
  defeats lock, guard-every-edge, named resources, starvation question, try-scope), R14 (composition
  diffs, shared field-selection helpers, parity smells, encoder homes, both-direction mirrors)

### aif-verify → skill-context (1 new + 4 strengthened)
**File:** `.ai-factory/skill-context/aif-verify/SKILL.md`
- NEW: tool authorities (pnpm build vs LSP 71007, worktree node_modules, NUL checks, characterization
  snapshots)
- STRENGTHENED: V1 (in-code SSOTs, route-tree↔OpenAPI grep, both directions/cases), V4
  (degraded/temporal matrix), V5 (gates 10–17: integration mandatory, full-suite merge sign-off, real
  acceptors, composition-root wiring, RED proofs, coverage-location honesty, RED-first contracts,
  divergent variants), V6 (migration triple, claims-are-not-evidence set)

### aif-security-checklist → skill-context (4 new + 2 strengthened)
**File:** `.ai-factory/skill-context/aif-security-checklist/SKILL.md`
- NEW: run-bound credential lifecycle · supply-chain specifier gates · LLM memory/prompt-injection ·
  request-boundary caps incl. paid-API sinks
- STRENGTHENED: S3 (symlink leaf/ancestor, provenance-transfer re-audit, repo-local manifests,
  internal-dir pruning), S4 (scope≠privilege, requireActiveSession, read-only GET authz)

### aif-docs → skill-context (NEW FILE, 5 rules)
**File:** `.ai-factory/skill-context/aif-docs/SKILL.md`
- historical-vs-current classification · sweep mechanics (directions/cases/wrap/cited homes/operationId
  edits/DDL attribution) · status-flip surface sweep · translated-docs parity · in-code SSOT surfaces

## Not Applied / Folded

- aif-qa/aif-commit/aif-improve-labeled points folded into implement/verify/fix rules (no separate
  contexts created — every such point had a co-label and a better home).
- Dropped as too situational: layout-math-over-devtools judgment note (2026-06-18-12.19).
- RESCAN file points: covered (I19/R14) or folded (reachable-enums → R10, configured≠valid → review
  kill-switch rule, launch-time role revalidation → same).

## Cursor Note

Advanced the cursor to `2026-07-07-18.55.md` (latest well-formed timestamp), NOT the lexical maximum
`20260602-122318-m13-assignment-lifecycle-review.md` — same precedent as the 2026-06-17 run: advancing
to the mis-named file would make every future `2026-*` patch sort "old" and be skipped. The file is
harmlessly re-scanned each run. Recommendation stands: rename it to `2026-06-02-12.23.md` to restore
lexical ordering.
