# Evolution: 2026-09-04 02:31

## Intelligence Summary

- **Patches analyzed:** 74 (incremental, cursor `2026-07-07-18.55.md` → through `2026-09-04-01.33.md`;
  the tail-5 overlap set lies inside that range). The mis-named `20260602-…` file flagged by the two
  previous runs has since been renamed to `2026-06-02-12.23.md`, so lexical ordering is clean and no
  re-scan quirk remains. All 74 patches were read in full by the evolving session (no reader fan-out);
  **406 prevention points** were extracted into a registry (`registry-1..3.md` in the session scratchpad):
  31 already covered by existing skill-context rules, 120 partially covered → strengthenings, 246
  uncovered → new rules (9 rows were merge-pointers to sibling rows). Every one of the 74 patches is
  cited by at least one applied rule (mechanically checked). Consolidated into **82 improvements**
  (25 new rules, 57 strengthenings). User chose "Yes, apply all improvements" → **82/82 applied**.
- **Stale rules:** none. Base `.claude/skills/aif-*/SKILL.md` files unchanged since the 2026-06-17 evolve
  (`git log --since=2026-07-11 -- .claude/skills/` empty); the only skill-context commit since the last
  run (`cd3ade544`, CCR removal) was a prettier reflow plus removal of CCR examples — no rule headings
  added or removed — so the previous all-Case-D verdict carries over. Step 4 skipped.
- **Tech stack:** Next.js 16 + React 19 + HeroUI v3, TypeScript strict, Drizzle/Postgres 16, ACP
  supervisor daemon (claude/codex ready; gemini/opencode/mimo readiness-gated) now addressed as a
  registered execution host (ADR-166), vitest unit + testcontainers integration + Playwright e2e,
  EN+RU i18n, `MaisterError` taxonomy.

## Recurring Patterns Identified

- Irreversible side effects and "cleanup that widens" (force-push rollback, cascade-before-check, fs release inside the terminal tx, publication protocol) — 11 occurrences
- Tests that lie: unrealistic seeds, deny-only tests, mock-placed values, timing assertions, wrong-layer racers, mutation-proof gaps — 14
- Contract prose over-promising (Expectations without enforcement, motivation copied into OpenAPI, inverted rationales, `.strict()` ↔ `additionalProperties:false` pairs, auth adding status codes, response-shape drift invisible to `validate:contracts`) — 12
- Lock scope ≠ invariant scope / CAS mistaken for a precondition / discarded CAS results / filesystem "CAS" — 9
- Enumeration drift in recovery arms and dead or unreachable guards (root-identity convention, `Running`-only allow-lists, dormant gates, compensating workarounds) — 9
- Process-boundary seams (single-flight deadlock, fake-vs-host parity, real ACP wire order, `getDb()` bypass, optional guard params) — 6
- Frozen/pinned data read from live sources; WARN-as-fix; validation only on preview paths — 5
- Idempotency without content guards; non-deterministic digest inputs; `sameRequest` ≠ `sameDecision` — 4
- Toolchain pins and migration legs (`node:sqlite` under pinned Vite, `Buffer`≠`Uint8Array`, `schema.ts` as the 4th migration leg, CHECK re-create at rebase, compose exit-code flattening) — 6
- Authorization by data class / response shape / per-row scope predicates / bearer binding — 6

## Improvements Applied (25 new rules + 57 strengthenings, all approved)

### aif-implement → skill-context (9 new + 21 strengthened)
**File:** `.ai-factory/skill-context/aif-implement/SKILL.md`
- NEW: filesystem ownership & publication protocol · recovery owners + normative recovery-window tables ·
  idempotency keys/digests/canonical equality · frozen-source read paths + action-path validation ·
  dormant gates/limits/coupled defaults · process-boundary contracts (single-flight, fake parity, wire
  order, fenced binding) · tests written during implementation (seams/seeds/races/wire assertions) ·
  toolchain pins/migration legs/shell hygiene · authorization at reads and writes
  ← 2026-07-11-12.25 … 2026-09-04-01.33 (all 74)
- STRENGTHENED: Concurrency control (items 18–22), No-write-after-terminal-flip, Error-handling
  precision, Bounded background sweeps, Slot-freed states, Fail-closed verdict routing/enum honesty,
  Raw-SQL correctness, Public route DTOs, Honesty+write-once (item 7), Enforcement-acts-on-what-the-run-DID,
  Validate-at-the-SINK, Centralize authored validation, Git mechanics, i18n, Every-committed-test-executes,
  DB-schema/contract edits verified by grep, MCP facade mirrors, React state & layout, Capability
  materialization, Runtime roots, Mirror-pair syncs

### aif-review → skill-context (5 new + 11 strengthened)
**File:** `.ai-factory/skill-context/aif-review/SKILL.md`
- NEW: `#irreversible-rollback` compensation review · test-quality review (`#test-seed-realism`,
  `#wrong-layer-race`, `#timing-assertion`, `#wire-not-asserted`, `#deny-only`, `#non-discriminating`,
  `#pins-weak-behavior`) · process-boundary review (`#single-flight-reentry`, `#fake-drift`, `#mock-order`,
  `#optional-param-sweep`, `#getDb-bypass`) · comments/prescriptions/prose are claims
  (`#unenforced-invariant`) · recovery-window and orphan completeness (`#uncovered-cell`,
  `#enumeration-drift`, `#half-composition`)
- STRENGTHENED: Concurrency red-flags (10 new flags), Multi-write transition (item 5), Status/enum
  fan-out, Response-leak, DRY/SSOT, Adversarial pass, Review-exit spec surfaces, Auth-first,
  Retry-safety, Kill-switches/gated resources, Temporal reads

### aif-verify → skill-context (0 new + 5 strengthened)
**File:** `.ai-factory/skill-context/aif-verify/SKILL.md`
- STRENGTHENED: "Logic exists ≠ proof" gates 18–30 (mutation-proof, seed realism, races on the
  invariant, deny-only, wire fields, discriminants, pinned weak behavior, suite-loaded/name-sets/
  baseline-prove/NO-GO, send-site + parity, mock identity, bypass shapes, test-matrix audit + DB-backed
  ownership regressions, e2e hygiene), ADR/migration integrity (`schema.ts` 4th leg, CHECK re-create,
  guard-the-guards, status labels), Contract-surface list (auth status codes, `.strict()` pairs, engine
  bumps, read surfaces, reproducible-from-tag), Regression-class matrix, Tool authorities (5–9)

### aif-fix → skill-context (1 new + 6 strengthened)
**File:** `.ai-factory/skill-context/aif-fix/SKILL.md`
- NEW: fixing past the point of no return (widened cleanup is a regression)
- STRENGTHENED: Sibling sweep (paste the grep; grep the predicate; required param over test; forms
  covered; call-site-aware scans; group by control flow), All findings (dead gate = two tasks;
  compensating workarounds; neighbouring helper; frozen-spec banner), Renumber (status labels/comments/
  Consequences; CHECK re-create; `schema.ts`; cross-cutting numbers; dup-line grep), Verify the finding
  (items 6–9: prescription is a hypothesis; inference/premise/consequence; reject/reverse/refute;
  provenance triage; deterministic vs race; reproducible ≠ product bug), Debugging heuristics (4–9),
  Close-out battery (`validate:docs:all`, mcp build, blast script, name sets, baseline-prove, env gaps,
  explicit `cd`, adversarial re-review of every fix cycle)

### aif-plan → skill-context (4 new + 8 strengthened)
**File:** `.ai-factory/skill-context/aif-plan/SKILL.md`
- NEW: lock scope = invariant scope + racer from the invariant · frozen-source read paths + action-path
  gate · idempotency/provenance design · authorization by data class/response shape + parity/positive
  grant tests/per-cycle adversarial review
- STRENGTHENED: Multi-store atomicity/crash windows, Fan-out, ADR/migration numbers (item 6), Background
  automation (items 6–10), Statuses as signals, Migrations preserve data, Launch preconditions, Phase 0
  analytics

### aif-security-checklist → skill-context (3 new + 2 strengthened)
**File:** `.ai-factory/skill-context/aif-security-checklist/SKILL.md`
- NEW: git transport URLs/remotes as command-injection sinks + bypass-shape tests · idempotency/replay as
  authorization decisions · redaction by kind + inherited scope sets + lineage guards at every mutation path
- STRENGTHENED: Fail-open/authz scope (read gate by data class, RSC read gating, two-domain reads, opt-in
  aggregations, per-row scope predicates, principal-bound bearer tokens, positive grant tests, DB-backed
  ownership regressions), LLM memory (self-influence framing, ADR residual, one normalization)

### aif-docs → skill-context (3 new + 4 strengthened)
**File:** `.ai-factory/skill-context/aif-docs/SKILL.md`
- NEW: Expectations name their enforcement / motivation ≠ contract prose / rationales match mechanisms ·
  read-surface sweep on stored-form changes + JSON-Schema/MCP description precision · operational docs
  (verify-before-destroy, component responsibilities, one authoritative roadmap section)
- STRENGTHENED: Historical vs current (supersession banners), Sweep mechanics (table-drop contract-wide,
  `$ref` resolution, diagram order, grep the old trigger phrase), Status flips (phased re-reconcile,
  forms covered, status labels after renumber, as-built analytics), In-code SSOTs (engine-bump grammar,
  agent-facing topology truth, retention windows vs config constants)

## Not Applied / Folded

- aif-qa / aif-commit / aif-improve-labeled points (release tag+commit evidence, killed-agent audit,
  parallel-agent disjoint ownership, reproducible-from-tag release evidence) were folded into
  implement/verify/fix rules — no separate context files created (same precedent as the two previous runs).
- Nothing dropped as too situational. A few reference facts (package `schemas/<name>.json` layout and
  field types, `DEFAULT_GC_AGE_DAYS = 14`, the `pending→permission→execution` ACP event order) were kept
  deliberately because wrong fixtures had been built from the wrong assumption.

## Cursor Note

Advanced the cursor to `2026-09-04-01.33.md` (the newest new patch; also the lexical maximum — the two
non-standard names `2026-07-14-human-ask-review-remediation.md` and
`2026-07-14T19-18-32-plan-review-handoff-recovery.md` sort inside the processed range, so nothing is
skipped). Application was mechanical: a script parsed the per-skill drafts, verified every strengthened
heading exists exactly once in its target (dry run, zero problems), appended new rules below the
auto-generated marker, inserted strengthenings at the end of their rule (the verify gate list 18–30 was
inserted before its closing "The verify report MUST state" paragraph), and updated each header's
`Last updated` / `Based on` lines. Originals were backed up in the session scratchpad.
