# /aif-fix self-improvement note

## Trigger

Review findings for the M24 scheduler service and M25 authored capability catalog
groundwork exposed implementation/docs drift after the feature implementation
pass.

## What changed

- Treat review findings against "implemented config knobs" as code bugs, not doc
  bugs. Verify every documented environment variable has a concrete read path
  and regression coverage.
- For scheduler/lease work, add adversarial integration coverage for claim
  overlap, catch-up without backfill, lease reaping, stale handler completion,
  default job bootstrap, and per-kind budget exhaustion.
- For projection tables that share identity keys with config-owned rows, add
  tests for SET/CLEAR symmetry and overwrite prevention before accepting the
  contract as implemented.
- After running auto-fixing linters, immediately inspect `git status --short`
  and undo formatter-only churn outside the requested scope.

## Evidence from this pass

- `pnpm --filter maister-web typecheck` passed.
- Focused unit slice passed: scheduler job helpers, command handler hardening,
  and authored-service pure tests.
- `pnpm validate:docs:all` passed.
- `git --no-pager diff --check` passed.
- Focused Testcontainers integration slice remained blocked by local container
  runtime availability before executing assertions.
