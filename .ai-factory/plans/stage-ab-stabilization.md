# Implementation Plan: Stage A/B stabilization

Branch at planning: `feature/stage-b-durable-execution-host-data-plane`.
Created: 2026-09-05. Status: **Implementation in progress; S0–S2 qualified; S3.1–S3.5 and S5.1 complete (C07 closed); S5.2 core qualified; S3.6–S3.8, S4 and the remaining S5 matrices pending**.
Implementation base: the audited Stage B tip, followed by revalidation against the then-current local `main`.

## Settings

- Testing: **yes**. SDD followed by RED → GREEN → REFACTOR, real Postgres and real supervisor processes where the invariant crosses those boundaries.
- Current user instruction (2026-09-09, latest override): qualify and amend the S3.3 draft in place, close C07, fast-forward local `master` to the amended commit (no push), then continue the plan in order with one green conventional commit per task and the checkpoint integration rules (C08 after S3.8, C09 after S4.4, C10 after S4.8, C11 after S5.4). Host SQLite 12→13 and PostgreSQL 0160 are committed history; every migration remains forward-only and any new number is asked for first. Linux isolation is not validated by the local slice; ask for the smallest missing host prerequisite when S5.3 reaches it.
- Logging: **verbose**, structured and bounded. No raw paths, tokens, environment values, full prompts, object bodies, or arbitrary unsanitized exception bodies at any level.
- Documentation: **yes**, mandatory within every increment. Normative contracts precede production changes; status changes to Implemented require executed scenario evidence.
- User-requested final documentation audit (2026-09-06): S5.4 must verify architecture prose and diagrams, database/API/event schemas, component responsibilities and connectivity, actual configuration readers against `.env` and repository environment templates (`.env.example`/`.env.sample` where present), deployment wiring, and all inter-component data paths (HTTP/SSE, commands/receipts, canonical events, Postgres, host-private files and remaining shared repository/worktree access). Correct drift from the final implementation; retain explicit Stage C/D boundaries. Compare local environment key names without copying private values into documentation or logs.
- Scope: correctness and completion of accepted A/B. Retain ExecutionHosts, the Stage A command/receipt ledger, Postgres canonical events, host-private SQLite/files, opaque identities, and `node_attempts` as the sole Flow execution ledger.
- Original planning-turn boundary (implementation is now authorized): planning wrote only this file. Preserve the existing A/B plans and the user's untracked review directory. No production edits, test implementation, migrations, branch switching, pull, commit, or execution of the implementation during planning. Pass this explicit plan path to implementation; the existing branch-derived Stage B plan is historical input, not the task list for this work.

## Execution progress

2026-09-09 — S3.5 complete (crash-gap recovery). RED `s35-host-red` (18/21) on the host: a receipt write lost after the seal left the upload receipt `accepted` across a restart, a crash after the tombstone commit left the file on disk, and a retried upload tripped `EEXIST` on its stale `.partial` spool (500). RED `s35-manager-red` (20/21) on the manager: a delete the host had accepted but never tombstoned folded as `turn_lost` and stranded the catalogue's `deleting` intent. GREEN: host startup repair (`runtime-object-recovery.ts`, `RuntimeObjectRegistry.recoverAfterRestart`) finishes `deleting` rows, discards interrupted spools of pending non-producer objects and completes accepted upload/delete receipts from the sealed/deleted row with the matching canonical event before any route exists; `runtime_object.reserve`/`delete` receipts are restartable over the durable row like uploads; manager recovery redelivers a `delivering` delete whose receipt is accepted without an in-flight turn. Reserve receipts carry no object target, so an interrupted reserve heals through the caller's idempotent re-reserve, documented. Two restart-durability guards (upload and delete acknowledgements lost across a real supervisor restart, late availability after deletion) passed immediately; a focused mutation removing receipt-fold object application kills both plus the S3.1 fold case (`s35-manager-mutation`, 3 failed / 18 passed), so the guards are live. The host fixture's `stop()` hung about half the runs after a streamed response — the keep-alive socket was not yet idle at Fastify's close sweep and the client honours the 72 s hint — and now mirrors `main.ts` with a forced close after 1 s; four consecutive complete-file runs pass 21/21. Gates on the final source: host owning file 21/21, lifecycle owning file 21/21, supervisor unit 361/361, supervisor integration 209/209, supervisor A/B 106/106, web unit 7509/7509, web A/B 396/396 (1287.0 s, no sleep record), web integration 3875/3875 across all 453 files (4238.84 s serially, no Sleep/Wake record in the window; `s35-web-integration`); both typechecks, both lints (17 pre-existing web warnings), docs (15/15 mermaid), contracts and `db:generate` no-diff pass. Runtime-object, supervisor and execution-host docs and the test-lane row are updated in the increment. S3.6 is next.

2026-09-09 — S3.3 qualified (AT-15) and C07 closed on the amended draft source; local `master` is fast-forwarded to that commit. The rebased branch sits on `master` `a6fa6ba6` with S3.1 `2a98cc7d` and S3.2 `08bb6aad`; all ten S3.2 files match their qualified hashes (`s32-rebase-source-proof.json`). Qualification evidence is under `s33q-*` with the final source manifest `s33q-source-manifest-final.json`.

RED `s33-host-red` (17/18), `s33-wire-red` (7/11) and `s33-catalog-red` (13/16) expose whole-object digest on 206 and missing actual-body/catalogue verification. `s33-stream-error-red` (19/20) exposes cancellation replacing a typed truncated-network failure. The draft shares strict digest/range/ETag parsing, sends actual response Content-Digest and full Repr-Digest with generation/hash ETag, and verifies manager bytes into an anonymous 0600 descriptor before success (8 MiB per response, two active responses, EOF/cancel/abort cleanup). Both browser routes preserve S3.4 authorization and safe-download headers. Complete component files pass 38/38 (real HTTP plus Postgres/supervisor) and host objects pass 18/18. Coverage migration is recorded in `s33-coverage-map.json`: no deleted/skipped/quarantined cases or weakened assertions.

After rebase, D10 inventory detected the new master directory scan and exported `uniqueSlugForName` wrapper. The full nine-case HEAD inventory reproduced five failures; the draft explicitly classifies both existing repository-worktree operations, plus the new manager-owned response spool, without weakening the guard. On that source, full web unit passed 7509/7509 (24.36 s), supervisor unit 361/361 (14.00 s), supervisor integration 206/206 (14.00 s), supervisor A/B 103/103 (17.197 s), serial isolation 4/4 (74.051 s), and browser A/B 5/5 (23.762 s, every retry 0). Types, lint, docs, contracts and no-diff `db:generate` passed. These earlier results do not qualify later consumer changes.

The first full web A/B was interrupted by host sleep (384/392); power transitions are captured separately. Complete HEAD recovery files passed 108/108 serially (2145.955 s). A fresh awake full A/B on rebased draft source still failed 2/392 in 1284.320 s: Agent `terminate_restorable/live` timed out in afterEach graceful child shutdown (10 s), followed by `after_response` initial NeedsInputIdle wait (15 s). No Sleep/Wake transition occurred. A comparable clean HEAD full A/B passed 374/374 in 1284.481 s (`s33-head-ab-web-parallel`), so these two intermittent failures are not labelled pre-existing or fully attributed.

Investigation found a separate reproducible cancellation gap: `consumeRuntimeEventStreamOnce` awaited its PG claim before registering the abort listener. The real row-lock/barrier case fails identically on draft and HEAD (2/3; `s33-consumer-cancel-red-2` and `s33-consumer-cancel-head-red`): one stream opens after abort, the call fulfills and a successor cannot claim. The first test fixture attempt violated the single active local-host constraint and is not contract RED evidence; the corrected fixture reuses the existing host and removes its isolated stream. The minimal typed aborted check uses the existing exact-owner failure recorder to expire the claim. Complete claim/ingest files then pass 21/21 (`s33-consumer-cancel-green`). Its effect on the two intermittent A/B failures still needs a full final run; do not claim that causal link from the narrower test alone.

Final gates on the qualified source (Node 24.15.0, all reports outside the worktree, zero failed/skipped/todo cases unless stated): web unit 7509/7509 (27.56 s; repeated on the final source), supervisor unit 361/361 (13.78 s), supervisor integration 206/206 (13.00 s), supervisor A/B 103/103, web A/B 393/393 across 31 owning files (1283.6 s, concurrency 4, no Sleep/Wake record in the window), isolation 4/4 (73.7 s) and Chromium execution A/B 5/5 (18.1 s, every result on retry 0). Both strict typechecks pass; web lint has 0 errors and the 17 pre-existing warnings; docs/link/ADR/index/ERD and contract/adapter-mirror validation pass; `db:generate` reports no schema change. The two previously intermittent agent `terminate_restorable` cases (`live`, `after_response`) passed in the full lane after the consumer cancellation fix, which matches the observed mechanism: the driver fixture's shutdown awaits `stopRuntimeEventConsumers`, and an abort lost during the PostgreSQL claim left an un-aborted SSE loop against the hang fixture, so the child never exited, afterEach SIGKILLed it at 10 s and the orphaned 30 s lease starved the next scenario's NeedsInputIdle wait. The three restricted-environment unit failures (askpass, pinned dispatcher, Docker-probe child) are environment: their complete owning files reproduce the same three failures on the draft and on clean HEAD `08bb6aad` under a `sandbox-exec` network-deny profile (`s33q-unit-sandbox-{draft,head}-*`), and pass with sockets and spawn allowed; the draft touches none of those modules. The first serial full web integration was invalidated by clamshell sleep on battery (`s33q-web-integration-interrupted.json`, power log beside it); the awake serial rerun `s33q-web-integration-2` passed 3857/3872 across all 453 files in 4907.19 s with 15 non-passes, every one attributed (`s33q-web-integration-2-attribution.json`): the Docker-probe timeout and two 180 s hook timeouts in `suites`, `agent-driven-flow` and `authored-bridge` landed at 13:40–13:45Z inside a second clamshell-sleep window and the three complete files pass awake (14/14 with the fourth file); the one genuine failure, `e2e-loop`, was S3.3-exposed: the e2e seed fake supervisor served object content with only `Content-Digest`, which the AB-15 transport refuses (prepare-phase `ACP_PROTOCOL` five times, poisoned, run left `Running`). The fake now emits the selected-byte Content-Digest, full Repr-Digest and generation/hash ETag like the central fake, and the complete file passes (2.1 s); the symlinked control checkouts fail that file at a later facade-token assertion for control-environment reasons unrelated to this change. Runtime-object/API docs now label AB-15 Implemented; crash-gap recovery, fair retention, historical import, Linux isolation and the remaining S5 matrices stay open.

2026-09-09 — S3.2 qualified (AT-13). RED `s32-red` and HEAD control `s32-head-red-3` reject the prior integrity claim: same-size tamper, truncate, symlink and same-byte inode replacement returned 200; unlink returned an untyped 500. The five original transport cases remained green. The first HEAD control exposed an unclosed unexpected-success response in the new RED fixture; explicit connection close and cancellation removed its hook timeout without changing assertions. Intermediate fault-injection setup refused native fs `spyOn`; wrappers preserve the real fs implementation and intercept only the tested barrier. `s32-reopen-red` additionally proved that a distinct inode at the same producer filename is insufficient: reopening that name corrupted the seal (17/18). The final path split protects both retained descriptors and filename reuse.

User-approved host SQLite 12→13 adds private `producer_path`, `sealed_device` and `sealed_inode`; committed historical SQL remains untouched. Uploads fsync their exclusive temporary descriptor before rename; producer output copies to a distinct sealed path while its producer path remains separately charged. Captured ACP segments record their descriptor identity. A legacy seal acquires identity only through a size/SHA-256-verified copy with reserved temporary capacity. Reads use no-follow/nonblocking regular-file descriptors, full 64 KiB-buffer hashing and initial/final stat checks, copying only the selected ≤8 MiB into the exact unlinked 0600 response descriptor. Two active responses cap retained response bytes at 16 MiB; cancellation/close releases descriptors and temporary accounting rows. Missing/corrupt state and canonical outbox evidence commit together before HTTP refusal; a failed event write rolls back and latches typed storage degradation. Known corrupt symlinks survive restart without being followed or losing their physical charge.

The full integration RED `s32-integration-1` identified four assertion/fixture migrations; all four unchanged HEAD owning files pass 54/54 (`s32-head-gate-attribution`). Coverage map: v6/v7/v9 migration fixtures now remove the v13-only columns when constructing their old schemas, retaining every original receipt/event/ACK assertion. The maximum-output case still verifies two real producers and all three 25 MiB outputs each; its 50 MiB reservation is asserted before seal, and after seal it asserts the two retained physical copies (50 MiB written) and `sealed: true`. No cases were deleted, weakened, filtered, quarantined or skipped.

Final plain gates on the same source: supervisor full unit 361/361 in 13.26 s (`s32-unit-2`), full integration 206/206 across 24 files in 12.68 s (`s32-integration-3`), A/B 103/103 across ten complete owning files in 16.65 s (`s32-ab-supervisor-2`); serial isolation 4/4 in 68.89 s and Chromium execution A/B 5/5 in 17.4541 s, every browser result on retry 0. All green suites have zero failed/skipped/todo cases. Strict supervisor types and full lint pass. Documentation/link/diagram/index/ERD checks and wire-contract/adapter-mirror validation pass. Offline `db:generate` produces no diff; its initial missing-DB_URL refusal reproduced on HEAD, then a non-secret local configuration URL satisfied the generator without a database connection. Runtime-object, supervisor HTTP, host-private schema, artifact and test-infrastructure docs are updated in the increment. Stable reports and the final ten-file source manifest are under `/private/tmp/maister-ab-implementation/s32-*`. S3.3 is next; whole-digest-on-206 metadata and manager verification remain its paired host/web change. C07 remains after S3.3.


2026-09-09 — S3.1 qualified (AT-09) on local `master` base `6bb4076b`, after rebasing the in-progress increment and preserving its named WIP stash. RED `s31-red.{out,json}` is 1 passed / 5 failed; `s31-reconciliation-red.{out,json}` is 10 passed / 5 failed; `s31-migration-proof-all-red.{out,json}` fails one migration case at two strict comparisons. The resulting real Postgres/supervisor lifecycle suite covers 13 cases, retention covers two, and one migration case checks six independent backfill boundaries. Immutable declared size/hash are separate from the established seal. One locked, exact-binding reducer handles client ACKs, receipt recovery, canonical projection and deletion: an ACK can establish a seal while leaving pending; canonical availability promotes it; exact historical evidence stays with its old assignment/generation and never mutates a successor or resurrects terminal bytes. Ordinary unsolicited objects are refused; native output proves its original command/session. Recovery preserves the original delete generation, and terminal delete refusal retains deleting. Migration `0160_runtime_object_declarations` was explicitly authorized: only the earliest unambiguous exact reserve request, including expiry, can backfill declarations; a missing/compacted or tied first request and producer-only allocation stay unknown. Neither a later request nor the seal invents a declaration. This is a new SQL/journal/snapshot triple; committed 0131–0136 are untouched and `db:generate` produces no diff.

The full gate exposed obsolete fixtures and assertions, with attribution retained in `s31-failure-attribution.json`: at the historical `8fb7115b` base, 48 of the 56 failing cases were reproduced in complete HEAD owning-file runs; the remaining eight crossed the intentional object-intent seam or reached cleanup that the old fixture failure had hidden. The explicit coverage map is: ext/message's Running-after-completion assumption moves to a held in-flight prompt with active binding, while a completed park asserts the exact four-field NeedsInputIdle response; missing-handle/no-live rejection assumptions now assert durable queued input, current status and no premature host delivery, with the complete real Agent owner queue/restart matrix retaining recovery coverage under the S2.8 Agent messages contract. Launch-paths P4's fabricated exit/stack-local checkpoint assumption becomes a real keepalive sweep with exact checkpoint target, preserving NeedsInputIdle and the paused, nonterminal node. Event-claim-lock's unsolicited diagnostic object becomes a native stdout-overflow object with exact create-command provenance; the same blocked two-writer INSERT race retains both fulfilled results, one row, source event, generation and conflicting-hash poison checks. Scratch/HITL/browser fixtures now carry actual creator IDs, projected incarnation admission and canonical v2 terminal/receipt/object evidence; their original status, permission and webhook assertions remain. Other repairs preserve assertions: child turn/command evidence is removed before disposable run rows without weakening 0158, scheduler fixtures carry released assignments, seeded prompt attempts remain unique, two accidental directory segments are removed, and error assertions use typed code/reason. The production incarnation wait covers the successive stream-claim and projection leases and propagates the existing abort signal; the earlier INFO-level HEAD diagnostic, rather than a fast-path passing run, proves the lease boundary.

After rebase, the first full web integration gate was 3849 passed / 1 failed out of 3850 in 3833.16 s (`s31-rebased-web-integration`). The sole C4 shared-worktree failure also reproduced on unchanged `6bb4076b` production: four plain complete HEAD-file runs passed 11/11, then a complete run with post-result code-only diagnostics failed 10/11 (`s31-head-c4-diagnostic-3`). Competing allocators overwrite provenance before the first owner is committed; mismatch compensation then removes the other installation's tree. The existing OS-backed materialization mutex now covers shared allocation, provenance, row commit and compensation in `MAISTER_WORKTREES_ROOT/<project>/agents`; Git remains outside the Postgres transaction, preflight precedes locking and scheduling follows unlock. A new refusal control caught the first mutex placement dirtying the parent checkout: complete HEAD file 12/12 versus 11/12 with that placement (`s31-c4-checkout-{head,red}-2`). The final managed-directory placement passes the original C4 assertions plus provenance/row owner equality and checkout cleanliness. Eleven complete related files pass 59/59 in 11.91 s, followed by four complete owning-file runs at 12/12 in 4.32/4.35/4.39/4.28 s. Temporary diagnostics were removed and the HEAD attribution checkout restored.

One final-source full project attempt then completed 3838 passing assertions but left 13 cases unexecuted when runner-graph setup failed (`s31-final-web-integration`, 3810.81 s). The typed Docker-boundary error occurs at container-start after 3688 ms: testcontainers cannot resolve a matching host port during container inspection. Both the owning test and pg-container helper are byte-identical to `6bb4076b`; the failure precedes database connection, migration and application assertions (`s31-docker-attribution.json`). Complete owning-file controls subsequently pass 13/13 on clean HEAD in 5.59 s and 13/13 on the current source in 5.19 s. This classifies the observed failure as container infrastructure; it does not claim the transient was reproduced by those passing controls. No test/source change, per-case test retry or combined partial-run pass was used. All 43 source checksums and HEAD remained unchanged for the complete project rerun.

Final plain gates on unchanged source: web full unit 7509/7509 in 23.07 s; web full integration 3851/3851 across 453 files in 3809.54 s (`s31-final-web-integration-2`); web A/B 374/374 across 31 complete owning files in 1273.90 s. Supervisor source is unchanged after rebase: full unit 361/361 in 13.84 s, full integration 193/193 in 13.05 s, A/B 90/90 across ten complete owning files in 17.00 s. Standalone serial isolation passes 4/4 in 68.53 s and the real Chromium execution A/B lane passes 5/5 (auth setup plus four content/access cases) in 16.8345 s. Every green test gate has zero failed, skipped or todo cases. The first isolation invocation failed before any case because its explicitly supplied external evidence directory did not exist; a complete HEAD-file run reproduced ENOENT. Precreating that directory fixed the invocation without source changes. Both strict typechecks and full lint pass; web lint retains 17 existing warnings and zero errors. Contract validation, six A/B runner unit cases, documentation/link/diagram/index checks and generated ERD validation pass. Evidence remains outside the worktree under `/private/tmp/maister-ab-implementation/s31-*`; final source identity is recorded in `s31-final-source-manifest.json`. The object, prompt, execution-host and database contracts, ERD, test infrastructure and ADR-102 are updated in the same increment. S3.2 is next; read integrity, crash-gap recovery and fair retention retain their later task boundaries. The next merge checkpoint remains C07 after S3.3.

2026-09-08 — Adapter-compatibility Closure 1 qualified on Node 24.15.0. The historical RED capture (`badframe.log`, outside the worktree) contains 283 bytes: two valid newline-terminated JSON-RPC frames coalesced into one byte-mode iterator value, with the second frame starting at byte 223. The current clean base `f26686fb` already routes the compatibility fixture through the production framer and preserves child stdout across exit. This increment closes the remaining seam: `boundedAcpStream` refuses non-object-mode sources at construction with the existing `ACP_PROTOCOL` / `required_output_incomplete` / `producer_frame_invalid` taxonomy; `context-mounts` uses `captureAcpFrames` and awaits its drain before deleting scratch capture files. New unit RED fails because construction did not throw (`closure1-framed-source-red.{out,json}`); its second case feeds a notification and result in ONE byte-source write through the production framer and checks both messages in order, EOF and spool cleanup.

The old full-unit spawn failure did not recur on this base: three unmodified-HEAD full runs pass 355/355 in 13.33/13.05/13.08 s (`closure1-head-unit-{1,2,3}-host.{out,json}`). An initial sandbox run failed six loopback cases with `listen EPERM`; those are environment failures, not product regressions. A controlled real-child diagnostic held the log write past exit + the old 25 ms grace: zero events, no occupied decoder and no queued decoder waiter; after releasing the write and awaiting `outputDrained`, IDs are exactly 1/2/3 and all buffer reservations return to zero (`closure1-spawn-barrier.out`). This proves the timer is not a capture-completion barrier; it does not establish a decoder leak as the historical cause. Both spawn cases now await the existing production drain promise and assert no output failure before assertions or cleanup. No decoder-budget or production teardown contract changed.

Final plain gates, with no instrumentation in the tree: adapter-compatibility 11/11 on each of five runs (the current suite includes all Gemini/MiMo and shared-arbitration cases; 1.13 s each); supervisor full unit 357/357 on three runs (13.53/13.14/13.06 s); full integration 192/192 (12.61 s); supervisor A/B lane 90/90 across all ten owning suites (17.38 s); all with zero failed/skipped cases. Strict supervisor types, full lint and docs/ERD validation pass; discovery includes both new unit cases. Implementation commit: `23ca8854`. Evidence is `/private/tmp/maister-ab-implementation/closure1-*.{out,json}`. The framed-source contract is documented in `execution-event-plane.md` and the ACP source argument. No web/runtime files or migration history changed. S3.1 is next; subsequent integration checkpoints remain C07 after S3.3, C08 after S3.8, C09 after S4.4, C10 after S4.8 and C11 after S5.4.

S3.4 complete (AT-12), taken out of order with S5.1 at the user's request for launch readiness. RED first showed the defect it was aimed at — the manager's runtime-object content route and the artifact payload route served an uploaded document with its supplied `text/html` type, and in the new browser lane a real Chromium EXECUTED the upload inside the MAIster origin: the script renamed the run through an authenticated PATCH, requested a marker URL and rewrote the title, and no download event ever fired (`s34-browser-red7.log`). GREEN is one shared policy module, `runtime/safe-download.ts`, applied by the host content route, the manager proxy and the payload route (opaque bytes as `application/octet-stream`; server-derived text/JSON keep their passive type) with `Content-Disposition: attachment` (sanitized ASCII `filename` + RFC 8187 `filename*` from the logical name), `nosniff`, `Content-Security-Policy: sandbox; default-src 'none'` and `Cache-Control: private, no-store` on 200 and 206; the catalogued MIME is metadata only. Evidence: route unit tests 35/35 (including a non-ASCII/header-unsafe logical name and the migrated inline-`text/plain` assumption), host integration 5/5, the browser lane `pnpm --filter maister-web test:e2e:execution-ab` 5/5 against a REAL supervisor (exact bytes and headers, download-not-execution for HTML and SVG, the payload route over the same object, anonymous 401 / non-member 403 / foreign-run 404), strict types and lint. Two defects were found on the way and fixed, neither by weakening a test: the host's prompt path resolved uploaded attachments without `expectedKind`, so since the Stage B lifecycle commit every scratch prompt carrying an upload had been refused (`runtime object kind does not match the requested session input`) — the supervisor unit test mocked the resolver and never crossed that seam; and the browser lane's `next dev` boot livelocks when the host is busy (my own concurrent writes and scans at first), so the lane is documented as idle-host only. Existing authorization was kept as-is: the payload route still serves execution-object bytes under `readBoard` while the direct content route requires `readRepoFiles` — an inconsistency to decide, not something S3.4 changed.

S5.1 complete. The old file-level guard was shown blind before it was replaced: brought current with the tree (it was already red at HEAD because S2 added two fs-using modules and one entry went stale — a failure nobody had run), it passed 4/4 and STILL passed 4/4 with a host-runtime read injected into an already allow-listed file (`lib/runs/hook-trip.ts`). The replacement is an operation-scoped scanner over the TypeScript AST (`web/test-support/filesystem-ownership.ts`): every production source in every root D10 names, including `.tsx`, scripts, entrypoints and the shared `runtime/` helpers; test sources role-tagged rather than dropped; bindings resolved through aliases, namespace/default imports, `require`/`createRequire`/dynamic import, destructuring and `promisify`; each callsite keyed by source + enclosing function + callee + operation (+ literal spawn command); value uses it cannot resolve reported as `unresolved`; exported wrappers found by closing the effect over each module's local calls. The inventory classifies 537 callsites in 110 modules and 261 wrappers in 80 modules; wrappers flagged path-generic have every caller enumerated (a bounded depth-1 rule, chosen because enumerating all 638 wrapper callers would bury the signal); `supervisor-runtime` and `watch` are forbidden classes/operations; migration-only and operator-import entries carry an explicit authority and lifetime. The guard passes 9/9 including three mutation cases (host read injected into a classified mixed file, a new call of a path-generic wrapper, an fs-using `.tsx` page and script), each printing the exact tuple a maintainer must add. A per-(size, mtime) parse cache keeps mutation scans under a second.

S5.2 core (AT-16) complete on this host; the process-death/partition matrix and the Linux driver remain. `web/test-support/process-isolation.ts` offers only a driver the host can enforce (macOS `sandbox-exec`, EPERM; an unsupported host fails loudly — no fake driver), `web/test-support/real-web.ts` starts the PRODUCTION web (`next build` of the tree under test, `server.ts`, production instrumentation boot, own process group, credentials sign-in over Auth.js) under that driver, and `execution-ab-isolation.integration.test.ts` passes 4/4: the web identity is denied the supervisor's sentinel and its live `state.sqlite` while the harness and host keep them and the deny is scoped; a signed-in scratch launch with an upload completes through HTTP/Postgres with the real host executing the prompt, the object reads back under AB-12 with the transcript; a SIGKILLed web restarts through production initialization under the same isolation, serves the same history and bytes, and completes a further turn on the still-live host session; the supervisor is untouched. Runs alone as the serial `isolation` slice of `scripts/run-stage-ab-tests.mjs`. The documented deployment runs both units as the SAME `maister` user on one `MAISTER_RUNTIME_ROOT`, so the boundary this proves is not what `deploy/` installs today; the stale "roots MUST be equal or the event stream breaks" claim was corrected and the separate-user Linux layout is explicitly unqualified until S5.3.

Failure sets at this checkpoint were attributed against a HEAD worktree rather than assumed, then acted on. The full web unit project had 58 failures in nine files at HEAD — none run by any S2 gate — and every one was a test-side artefact of S2: six were my own S2.11 table-reset regex, which had also inserted `"execution_commands"` into the path-join arrays of the HITL service and respond-route tests; fourteen scratch cases died on the S2.9 prompt-admission wait (the legacy scratch adapter and the fakes now carry the host/assignment identity and answer the incarnation query; the launch fake gained the session-binding lock chain, `returning()` and the binding seam stub the recover test already used); the launch/messages/recover fakes now model where S2.9 moved the `WaitingForUser` flip (inside the owned prompt's application); the two flow-consumer wiring suites (34 cases) drove `runAgentStep` over a fake db, a path S2 made Postgres-only by design, and were removed with an explicit coverage map (hook halts → `hook-trip-escalate`/`agent-hook-trip`; create-body fields → `commands`; checkpoint/fenced/exited/permission-row reuse → `permission-resume`/`gate-permission-resume`/`prompt-owners`; persona composition → `agent-driven-flow` + the platform-agents-binding e2e) and the one homeless pure behaviour, the halt-rule mapper, ported to `lib/runs/__tests__/hook-trip-rule.test.ts`; the respond-route case expecting a synchronous `startAgentSession` on an idle agent is retired for the same S2.8 reason. Result: 7505/7505, 0 failed. Also decided: the artifact payload route now requires `readRepoFiles` for `execution-object` locators like the direct content route (agent output can quote any file the agent read); other artifact kinds stay `readBoard`.

The web lane's rotating failure (`owner-agent-budget … before_terminal`) is not host load. Its log shows the continuation worker re-observing the host session every ~6 s (`agent session stream ended without a terminal event`, 15 times in 95 s) while the durable terminal is applied only once the dead launcher's stream claim expires — one to three `RUNTIME_EVENT_CLAIM_LEASE_MS` cycles for the same window (19 s / 47 s / 122 s); the eight budget variants pass 8/8 in a short sequence (19–78 s). Modelling the boot-time `recoverExecutionCommands({graceMs: 0})` in the kill windows did NOT remove the variance (the prompt is still in flight when recovery runs) and was reverted. The poll bound is now derived from the observed ceiling (60 s + 3 leases) with no assertion changed; the pacing itself — a worker that re-observes instead of consulting durable evidence, and a recovery that waits on lease phase — is an open A/B item (AB-07 pacing) for S5.2/S5.4. Evidence at this checkpoint, each lane alone (the host carried another session's load, average 27 at the start): agent-owner suite 50/50 in isolation (827 s; `before_terminal` 64 s and 19 s), web A/B lane 359/359 across 29 suites (exit 0, `s5-web-lane-final.log`), supervisor A/B lane 89/89, browser lane 5/5 with C4 covering the payload-route grant, isolation suite 4/4, full web unit project 7505/7505, both strict typechecks, docs/contract/ERD gates. Still open and unchanged: `adapter-compatibility` fails the OpenCode-like case deterministically at HEAD (`producer_frame_invalid`, second case rotating), and `spawn.test.ts` `emits bounded session-line events` fails only under the full unit run (3/3 alone).


S2.13 documentation truth pass complete. Verified against the implementation, not against the S2 commit messages. Corrected: `execution-prompt-lifecycle.md` carried fifteen `Designed` labels for work now shipped — prompt-owner windows, the domain owner adapters, exhaustive owner eligibility, command retirement, and the PRM-02/04/06/07/08/09/11/12 corrections — and one specific false claim that the local-package assistant turn's ownership was unqualified "because its suite mocks the supervisor transport wholesale", which S2.9 fixed by making that suite publish the canonical `session.created` event an owned prompt admits against. `execution-hosts.md` still described the whole AB-01–17 set as Designed stabilization and told the reader that AT identifiers were "required future scenarios, not claims that a runner has collected or passed them" — AT-05–08 and AT-10 are collected and pass at the gate. `architecture.md`'s `command-receipts` row described only `GET /commands/:id`. `adr-166.md` stated as current behaviour that "Receipts prune at boot and hourly (7-day TTL)", which S2.11 removed outright; an ADR records a decision, so that line now carries an explicit supersession note rather than being rewritten. `CLAUDE.md`'s execution-host addressing block gained the two S2 invariants an agent needs before touching this code. Two labels were deliberately NOT flipped wholesale: the remote-effect/database failure tables and the identifier classification contract both cover runtime-object and import rows that belong to S3/S4, so each is now scoped ("Implemented for … ; … lands with S3") rather than marked done — a blanket flip would have been as wrong as leaving them Designed. The published site under `site-docs/` was checked on the same surface at the user's request: its execution-hosts, delivery-spine and deployment pages describe the single-host boundary, epochs, fencing, command ledger and idempotent receipts accurately, no page carried a stale receipt-TTL or retention claim, EN/RU parity is complete at 38/38 content pages, and `mint validate` passes. GREEN: all four repo doc gates at `--all` scope (431/431 mermaid blocks, 831 ADR anchors, 3166 relative links across 341 files, 125 indexed files), the generated ERD current, and the docs-site build validation clean.

S2.12 gate outcome. The first full lane after activation reported 53 failed / 306 passed across 15 suites, in three causes, and the largest was a regression the gate caught that no suite I had touched would have: the old launcher assigned `agentTurn` only when `ownedVariant` was non-null, and mine assigned unconditionally, so an agent resume that had ALREADY resolved a claimed turn minted a second one — stranding the original `queued` and consuming another pool slot ("agent pool is full", "expected 'queued' to be 'claimed'", 23 cases). The admit is now guarded on `!agentTurn`, which restores the original control flow exactly and adds an owner only to the branch that previously had none. The second cause was the S2.11 protected-evidence trigger meeting test cleanup: 25 suites delete `runs` before discharging command evidence, and each now deletes `execution_commands` first — what the protocol requires of any deleter, not a concession. The third was three raw unowned prompt seeds in the event suites, now owned; two further failures in those files that looked unrelated were cascades from the same constraint violation corrupting earlier state, and passed once it was fixed. One fixture defect surfaced last: `seedNodePromptOwner` created its node attempt at "now", and reconcile uses `latestAttemptStartedAt` as its grace anchor, so every run using the fixture sat inside the grace and stayed `Running` — silently changing what the V3 SIGKILL recovery test measures. The fixture backdates the attempt instead, because a real attempt starts BEFORE the prompt it dispatches; relaxing the assertion would have left the test green and measuring nothing. This is the third fixture this session that read as correct while being wrong about a property nothing had previously depended on. GREEN at the release revision: supervisor 88/88 and web 359/359, both exit 0 and both re-measured against the final tree rather than carried forward; 5/5 lane-runner unit cases; strict typechecks in both packages; scoped lint; the ERD and doc-link gates current.

S2.12 complete; the S2 gate is green (supervisor 88/88, web 359/359, both exit 0). The prompt owner is now mandatory at BOTH boundaries. At the client, `BoundClient.prompt` requires `admitOwner`: the unowned branch is deleted, so the compiler — not a convention — enumerates every prompt origin. That enumeration found two production paths S2.6/S2.8 had left unowned. The agent path fell through to a plain prompt whenever no claimed turn existed, which is exactly the resume/rework re-entry; a resume mints its assignment BEFORE the prompt, so the placement reason it minted for names the generation, and `resumeVariantFor` maps it to the `resume`/`rework` turn `dispatchStoredAgentTurn` already owns. An unmappable reason (a `legacy_backfill` placement reaching a resume) now refuses `unowned_agent_generation` instead of starting a turn nothing could finish — a deliberate behaviour change for pre-ADR-166 agent runs. The legacy M8 idle-resume continuation in `resume-driver.ts` was the other: it is an ACTION prompt on the node attempt it wakes, so it carries the same `node` owner the graph dispatch uses, and without an open attempt it refuses rather than prompting. That wiring was decided empirically rather than by inference — `admitNodePrompt`'s admission was read as possibly refusing the legacy generation, so it was wired and measured (7/7 resume-driver, 4/4 resume-recovery) instead of deleting a working recovery path on a guess. `promptOwner` on the flow node dispatch and `owner` on the scratch turn were already passed by every production caller and are now required, closing their conditional branches. At the ledger, migration `0159` adds `execution_commands_prompt_owner_required` as NOT VALID: it binds every new row AND every update — the old-writer rejection — while leaving pre-v2 history exactly as written, which is what "preserve, never reconstruct" requires. The same migration REFUSES the upgrade when any pre-v2 prompt command is still open, because an open prompt with no owner can be neither proven nor reconstructed, and it logs the count of terminal pre-v2 rows it preserved. Those rows now classify as `pre_activation_unowned` in the retirement scan rather than `owner_unapplied`: no owner ever existed, so there is no obligation to discharge, and the label makes the bounded historical set nameable instead of hiding inside an unrelated reason. Eighteen transport-level test callsites went through a shared `seedNodePromptOwner` fixture that seeds the real ledger row and binds the live incarnation — the same thing a production dispatch does — rather than a test-only bypass. The gate lane now also owns the S2.9/S2.10 suites (scratch, local-package assistant, gate chat, sync resolver/recovery, resume recovery) plus the two new S2.11/S2.12 suites, which S2.9 and S2.10 had not added. Contracts and analytics updated: the supervisor OpenAPI documents `POST /commands/{commandId}/retirement` with its four refusal reasons, `database-schema.md` records both new constraints and the tombstone column, `execution-prompt-lifecycle.md` flips retirement and adapter coverage from Designed to Implemented, and the generated ERD is regenerated.

S2.11 complete. Both sides reclaimed command evidence by age alone: the manager deleted any terminal row whose `completed_at` passed seven days with no look at owner application, terminal-event acknowledgement or run state, and the host deleted legacy receipts on a timer that ran at boot BEFORE the `turn_lost` repair. Reclamation is now a two-sided handshake. The manager derives eligibility under one bounded keyset page joined to the run, the terminal event and its stream's proven ACK frontier, and names the outstanding obligation on every refusal (`owner_unapplied`, `terminal_evidence_missing`, `terminal_event_unacked`, `run_retained`, `replay_grace`, `host_evidence_missing`); the cursor advances past protected rows so a permanently protected command cannot stall the rows behind it. Only then does it call the new idempotent `POST /commands/{id}/retirement`, where the host re-derives the same decision from its OWN receipt — terminal phase, epoch, digest, no open producer wallet, and for v2 its terminal sequence at or below the acknowledged frontier — and compacts rather than deletes. What survives on both sides is a tombstone: identity, digests and outcome disposition, never a replayable request. Age is not an input anywhere in that path; it appears only as the replay grace, which is the same seven-day window the ledger already promised for a legal retry, so the idempotency claim was not silently widened. Retirement never removes a key, so retention is bounded by refusing new admissions under pressure rather than forgetting a key a retry may still use. Deleting a run or an assignment around the protocol is refused by a database trigger (`execution_commands_protected_evidence`) rather than by a caller convention, because `execution_commands.run_id` cascades and nothing in the codebase would have stopped a future caller. Obligations are scoped to what each kind actually carries: only prompts have owners and canonical terminal events, and an UNOWNED prompt is a pre-v2 row whose classification belongs to S2.12, never to age. GREEN: 10/10 AT-10 cases against a real supervisor (`command-retirement.integration.test.ts`), 24/24 migrated supervisor receipt/outbox/storage cases, 42/42 in the two suites whose cascade expectations moved, 130 execution-host web cases with the same three pre-existing failures as the clean tree (adoption K5/K7, launch-paths P4 — verified identical by stashed baseline), and 539–541/545 supervisor cases whose failing SET is unstable run to run inside `adapter-compatibility` and green 11/11 in isolation. Four assertion families were migrated rather than deleted, each because it encoded the defect: `V6: prune deletes terminal rows older than 7 days only` now proves age retires nothing; `R6: receipts older than the TTL are pruned at open` now proves a restart reclaims nothing and only the handshake compacts; the producer-wallet reopen test now proves the retained tombstone makes a re-issue a recognised replay so no second execution is admitted at all; and the agent-turn cascade test now proves the run delete is refused while its command is protected and resumes once the command is a tombstone. Three v6/v7/v9 downgrade fixtures were corrected to drop the new column so they reconstruct genuine historical schemas. One design rule is recorded in code: a null `expectedRequestSha256` means "not asserted", not "matches anything" — the manager records a request digest only for owned prompts, and command id, epoch and phase are compared either way.

S5.4 documentation audit started (evidence-gathering plus two verified corrections; the full audit still runs at its gate). Read-only revalidation of the earlier preparation is appended to `final-doc-audit-prep.md`: the supervisor HTTP surface is 21 routes as built; the literal `MAISTER_*` reader scan now finds 74 distinct keys against 63 assigned in `.env.example`, with the 35-key difference captured as key names only in `s5-env-readers.txt`/`s5-env-template.txt` and no values read or retained. Two claims that are wrong today were corrected now rather than deferred: `AGENTS.md` described `compose.yml` as "app + supervisor + postgres" when the file is Postgres-only and says so in its own header, and both `native-hook-materializer.ts` and `guardrail-hooks.md` claimed the guard-script path override supports split-host topologies, which ADR-023 does not permit before Stage C. The remaining audit surface — architecture prose and diagrams, database/API/event schemas, component responsibilities and connectivity, deployment wiring, and the classification of the 35 unassigned environment keys — stays open for S5.4.

S2.10 complete, both halves. The chat reply previously existed only on the stack that issued the prompt — it accumulated from a live session stream — so a death after the host finished the turn lost it and reconcile later aborted the turn LEASE_EXPIRED. The turn now carries a `gate_chat` owner that decodes the reply from its own command's verified output, performs the L3 sense-and-restore in `prepare` so the workspace is durably neutral BEFORE `apply` releases the pending-turn fence, and commits reply and completion with the application marker; recovery reconciles that command before it may treat an expired lease as a failure. Both gate-chat suites pass 29/29. Four semantics were corrected while wiring it, each found by an existing assertion: a fenced prompt writes nothing and surfaces as `assignment_fenced` rather than letting the owner machinery's refusal to apply against a released assignment masquerade as the turn's failure; admission takes the HITL lock first, matching the response claim, because taking the assignment lock first deadlocked concurrent turns; the admitted lease is provenance and NOT an admission gate, since recovery legitimately extends a pending turn's lease and gating on equality refused the very turn recovery was keeping alive; and why a turn was cancelled is durable state rather than stack knowledge, so a turn whose own lease has run out reads LEASE_EXPIRED in any process. `sendGateChatTurn` also no longer flattens typed refusals into ACP_PROTOCOL. The sync resolver turn now advances its attempt `agent_running -> verifying` under its own sync claim, and recovery's orphaned-live-session window reconciles that command before discarding anything, so a resolver that finished before the crash continues into the existing idempotent re-verify instead of returning the run to Review. The owner's scope is deliberately only the ACP boundary: a crashed, refused or fenced turn stays with the existing sync state machine, which owns its restore and error fields. All three sync suites pass 68/68 with 6 `applied` and 1 `superseded` resolver dispositions observed, and the four fake-host owner suites pass 101/101 with no regression. Two disposition rules were established and are recorded in the code: the framework reads `superseded` as "this result is inapplicable" and a live waiter refuses it with CONFLICT, so an owner that deliberately writes nothing must still return `applied`; and the resolver's session is created through the ordinary path rather than owned-create, so `run_sessions` carries no assignment binding and the incarnation itself is the fence. The blocking defect was neither: `fakeExecutionHosts` built its accepted-prompt map inside the helper's closure, making it per REGISTRATION rather than per host, so a suite that re-registers the same fake while a prompt is in flight — as the HITL round-trip does to obtain a bound client for the permission response — silently dropped that prompt's accepted position, its terminal event could never be published, and the owner's wait blocked forever. It is now keyed by the fake host. Nothing had depended on that terminal event before, which is why the gap was invisible; this is the third defect of that family found in this session.

S2.9 completed, and the harness blocker recorded against it was diagnosed wrongly the first time. That suite is NOT missing an event plane: it wires one through `fakeExecutionHosts`. Two separate defects kept owned prompts from ever being admitted there. First, its `createSession` override replaces the fake's own transport method and never published the `session.created` that method publishes, so no incarnation was projected; `publishCanonical` is now exposed and the override publishes it, because a suite that replaces a transport method still owes the event plane that method's lifecycle events. Second — the one that mattered — the canonical sink copied the wire event's `phase` into the command payload, but `parseCommandEventPayloadV2` validates that field as the RECEIPT phase, so every failed turn produced an envelope the schema refused (`terminal_success`), and each refusal broke stream contiguity, leaving the lifecycle projector permanently short of the event it needed. The receipt built beside it already derived the phase correctly; both now share one derived value, and the fake's event shape is unchanged and still mirrors the supervisor, which reports a failed turn as a `completed` command carrying a non-succeeded status. A first attempt that changed the EVENT phase to `rejected` was wrong — it made the fake unfaithful to the real host — and was reverted. The publish chain also no longer poisons itself, where one rejected link rejected every later publish. Both assistant callsites now carry real owners threading the server-minted postprocess action id and the package lock generation: 26/26 in that suite with 13 `package_initial` and 3 `package_message` applications observed (`lp-final.log`), and no regression in the three owner suites that share the fake (5/5, 52/52, 3/3). One product correction fell out: a settled owner makes a domain transition durable but does not make a failed turn succeed, so all three owner waits now rethrow a definitive `failed`/`fenced` command and swallow only a succeeded one, restoring the rejection the assistant teardown path depends on.

A/B lane pacing and the runtime-event consumer, from re-diagnosing the one lane failure carried over from the previous session. The saturation explanation recorded there is WRONG and is withdrawn: at concurrency 4 (verified — peak 4 lane containers, 4 vitest workers) the same suite still failed, a lane-like reproduction with three concurrent owner suites passed 8/8, the cases pass in isolation, and in the lane the failing cases finish FASTER than the passing isolated ones (49s/66s against 75-78s) — they time out rather than run slow. The full 51-case suite alone then passed but with no headroom at all: 62s against a 60s poll, 49s against a 45s poll, on an otherwise idle host. The headroom is consumed by a real defect: a consumer that cannot claim the runtime-event stream re-requests it from its first event, ingests one duplicate and breaks having made no progress, and its retry interval came from a ternary whose arms were identical, so it spun at `RECONNECT_MIN_MS` for the life of the other claim (113 duplicates against 79 accepted, every duplicate at sequence 0, ~260ms apart; 284:71 and 532:172 in other runs). A contested claim resolves at a known bound — the holder's lease — so it now backs off to a separate smaller cap that stops the spin without delaying takeover past that lease; a first attempt using the 15s transport cap regressed two cases by delaying takeover and was corrected. Windows that deliberately SIGKILL a claim holder now derive their waits from `CLAIM_LEASE_MS` rather than literals, and the lane runner bounds its own suite concurrency and records it in the evidence JSON. No assertion was weakened. One agent-suite run reporting 44/50 is discarded as environmentally invalid, not interpreted: it reported single cases at 3929s and 3144s against their own 180s timeouts, with the host carrying an interactive desktop workload at load average 15.8 on 16 cores. Host load is recorded with every run from here.

S2.9 scratch recovery arm qualified; the assistant turn arm is refused with a stated reason. A recovery prompt now owns its own `scratch_recover` generation at ordinal zero and admits only under that placement reason, so it can never adopt the launch turn's identity; the existing placement suite's recover case asserts the owned command, its `recovery` reference and its applied state, and passes 5/5 (`s2-9-recovery.json`). All 44 scratch integration cases pass without skips (`s2-9-final.json`). Wiring the two local-package assistant turns to owners was implemented and then REVERTED: that suite mocks `@/lib/supervisor-client` wholesale, so no canonical lifecycle event ever projects the session incarnation an owned prompt must admit against, and all 13 of its turn-driving cases failed `prompt_incarnation_pending`. Qualifying those two turns needs a real event plane in that suite — a harness increment, not a wiring change — so they keep the pre-owner stack completion and S2.9 stays unchecked with exactly that gap named. Their postprocess action is already durable through the `0157` journal.

Final A/B lane after every increment in this session: 265/266 across 13 owning suites (`ab-lane-final.log`). The one failure is again the `owner-agent-budget` 60-second poll, and the failing variant rotated across the three runs (`escalate`, `terminate_restorable`, `escalate`) while the suite passes 50/50 in isolation — host saturation, recorded and NOT quarantined. Strict web types, scoped lint over every changed file and the docs gates (119-table DBML) pass.

A/B lane status after S2.7 and the S2.9 arms: `node scripts/run-stage-ab-tests.mjs web` runs 266 cases across 13 owning suites. The first full run failed two agent-owner cases; the second failed one. `owner-agent-idle-message` was coupled to residue: it pinned `MAISTER_MAX_CONCURRENT_AGENTS` to an absolute `1` while earlier cases in the same shared database still held slots whose settlement waits on 30-second lease expiry, so under load `promoteNextPending` correctly refused. Pinning the cap to `countLiveRuns(db, "agent") + 1` keeps the assertion exactly as strong (full pool retains input; freeing a slot promotes) and removes the coupling — that case now passes in the lane (265/266). The single remaining failure is a 60-second poll timeout in `owner-agent-budget`, whose failing variant changed between runs (`escalate`, then `terminate_restorable`) while the same suite passes 50/50 in isolation (`s27c-agent-owners-isolated.json`); that signature is host saturation from thirteen concurrent Postgres containers, supervisors and adapter processes, not a product defect. It is NOT quarantined or weakened: the lane gate is red until either the lane is given a serialized or resource-bounded execution mode or that case's wait is shown to be genuinely insufficient. Evidence: `ab-lane-web.log` (264/266) and `ab-lane-web2.log` (265/266).

S2.9 package-assistant action journal qualified: extracting a local-package assistant action SANITIZES its assistant message, so a process that died before the package apply lost the action entirely — the message no longer carried it. Additive generated migration `0157_flow_assistant_actions` retains the parsed action as durable intent in that same transaction, keyed uniquely by the message and carrying the edit-lock generation that authorized it; applying settles it forward under a CAS out of `pending`, and only the settling caller writes the result message. Both new controls pass with the existing 24 cases (`s2-9-package-journal.json`, 26/26, no skips): a retained pending intent applies exactly once, leaves the working-dir change and one `flow_action_result` message, and a second recovery pass is a no-op; a pending intent whose lock generation was taken over settles `skipped` with the working dir untouched. The first run failed on an invalid fixture operation kind (`write_file` instead of `upsert_file`), which the real protocol schema rejected. Strict web types, scoped lint and docs (2 Mermaid blocks, 19 ADR anchors, 91 relative links, 125 indexed files, 119-table DBML) pass. The two local-package assistant turns still complete their dialog transition on the caller's stack, and scratch recovery has no owner yet, so S2.9 stays unchecked.

S2.9 project-scratch continuation qualified (package arm still open): a scratch dialog turn now owns its own `WaitingForUser` transition through the durable identity that already exists before dispatch — the launch assignment at ordinal zero for the initial prompt, the accepted `run_messages` row and its sequence for a user message. The shared transition moved into a DB-only `applyScratchPromptCompletion`, and owned prompts wait for the incarnation the create ACK projects asynchronously (the first live control failed `scratch_admission_incarnation` without that wait). All three production-service cases pass without skips (`s2-9-scratch-owner.json`): the launch turn's applied command and `WaitingForUser` status, two distinct message turns with two distinct commands and no shared logical key, and an actual SIGKILL of the driver inside the `scratch_runs` dialog UPDATE which leaves the host outcome succeeded, the application unapplied and the dialog still `Running`, after which the ordinary prompt-owner worker applies the original turn once. 66 scratch unit cases and the four-file scratch integration regression (39/39, including placement, run-status emission, retryable marking and the local-package assistant) pass without skips (`s2-9-scratch-regression.json`). Strict web types, scoped lint and docs (8 Mermaid blocks, 32 relative links, 125 indexed files, 118-table DBML) pass. Scratch recovery, the two local-package assistant arms and the `postProcessFlowAssistantTurn` action journal remain S2.9 work, so S2.9 stays unchecked.

S2.7 consensus verifier/synthesis continuations qualified: a verification turn now owns its exact matrix cell (`flow_node_attempt:consensus_verifier:<verdict row id>`) and a synthesis owns its attempt/round/source generation, both admitted under the current assignment, active incarnation and the Running consensus attempt, and both applied DB-only with the command marker. The RED control exposed two real defects rather than a missing owner: the verifier and synthesizer sent `stepId` values containing `:`, which the supervisor's `SAFE_PATH_SEGMENT` session schema rejects, so **every** consensus verification had been failing at session create and silently degrading to a fail-closed `disagree` (`s2-7-verify-red.json` recorded `parseStatus: invalid_json` with the create refusal preserved in the raw-output artifact); and the first owner wiring converted "application still pending" into that same fail-closed verdict. Both are fixed: the step ids use `-`, and an unapplied generation raises the typed `consensus_generation_pending` refusal. All five owner cases pass without skips (`s2-7-consensus-qualified.json`): live draft publication, SIGKILL-before-application recovery, per-cell command ownership with two distinct cells, synthesis published from its own applied generation, and a recorded cell that can be neither re-pointed (Postgres refuses the admitted owner reference as immutable) nor re-applied (the recorded-generation guard supersedes the replay and preserves both rows). The 34 consensus unit cases pass after their fixtures were moved from scripted stdout to the applied-row seams (`loadConsensusVerdictCell`, `loadConsensusSynthesis`) and `vi.clearAllMocks` was replaced with `resetAllMocks`, which removed cross-test leakage two cases had depended on. The three-file Flow owner/driver/consensus-output regression passes 64/64 without skips (`s2-7-flow-regression.json`). Strict web types, scoped lint, contracts and docs (6 Mermaid blocks, 35 relative links, 125 indexed files, 118-table DBML) pass. Consensus permission ownership and owned create for consensus turns stay out of this increment; S2.9–S2.12 and full S2 acceptance remain open.

S2.7 consensus draft continuation qualified: the draft child's accepted input, owned create, prompt and completion are now one durable `consensus_draft` agent turn (additive generated migration `0156_consensus_draft_turn`), supplied to the S2.8 seam by `flows/graph/consensus/draft-prompt-owner.ts`. Before this increment a runner-kind draft could never settle at all — its session set no `reapOnEndTurn` and its artifact came from a stack-local accumulator in `consumeAgentSession`. The first live control exposed a missing turn/command binding (drafts superseded with the turns left `claimed`); reusing the extracted `bindAgentTurnCommand` fixed that exact case. Both production-launcher cases pass (`s2-7-draft-owner-green.json`, 2/2, no skips): the live fan-out publishes each participant's artifact from verified command output and finalizes its child `Done`, and an actual SIGKILL of the driver inside the draft artifact INSERT leaves the host outcome succeeded with no artifact and no completion, after which the ordinary prompt-owner worker applies the original draft once per participant. Strict web types and scoped lint pass. The four owning suites pass 84/86 in one parallel run; the two failures are the known load-sensitive agent-owner cases (`owner-agent-budget escalate before_terminal`, `owner-agent-idle-message` capacity promotion) and the same suite passes 50/50 in isolation (`s27c-agent-owners-isolated.json`). Docs pass 7 Mermaid blocks, 21 ADR anchors, 124 relative links, 125 indexed files and the 118-table DBML check. Consensus verifier/synthesis continuations and full S2 acceptance remain open.

S2.8 hook/budget handoffs qualified: exact source binding, checkpoint/halt ordering, durable resume/raise decisions, held/idle capacity claims and superseded permission rejection pass all 13 live/SIGKILL pause cases (`s2-8-agent-pause-qualified1.json`, 36 unrelated cases filtered). A fresh permission on the resumed turn remains unanswered until its own response (`s2-8-fresh-permission.json`, 1/1 selected, 49 filtered). The nine-file existing regression passes 148/150; the two failures expose a DB-less assignment in the old hook consumer fixture. Persisting that fixture's actual host/assignment restores all three hook cases. The typed consensus draft adapter seam and those hook cases pass together (`s2-8-draft-seam-hook.json`, 4/4 selected, 24 filtered); the seam verifies full multibyte output and once-only application but does not claim S2.7 consensus recovery. The default registry still refuses drafts. Strict web types and scoped lint pass; affected docs pass 149 relative links, 19 Mermaid blocks, 24 ADR links, 125 indexed files and the 118-table DBML check. Final S2.8 qualification passes all 99 tests in five owning suites without skips (`s2-8-final-owner-family.json`): 50 production agent restart/source/cap scenarios, seven turn admission cases, four atomic finalizer cases, 13 public-result cases and 25 verified-output/adapter/reducer cases. S2.8 is complete at the requested stop boundary. The continuation worker remains globally dormant until S2.12; consensus integration and full S2 acceptance remain pending.

S2.8 autonomous pre-turn admission qualified: an accepted Running launch with no retained turn remains stranded in the RED control (`s2-8-agent-worker-before-turn-red.json`, 0/1 selected). The existing worker now discovers that exact active launch assignment and enters normal input/create admission. Two concurrent workers produce one turn, one create, one prompt and the original result. This control, checkpoint re-entry, three idle-message windows and full-pool refusal pass together (`s2-8-agent-worker-before-turn-green.json`, 6/6 selected, 30 unrelated cases filtered). The worker remains globally dormant until S2.12.

S2.8 checkpoint permission handoff qualified: unanswered checkpoint/resume, historical completed result, actual SIGKILL before historical application, definitive input rejection without a new slot, six altered-proof controls and the existing live/replay/resume cases pass together (`s2-8-agent-handoff-qualified2.json`, 10/10 selected, 25 unrelated cases filtered). The first checkpoint controls expose missing delayed-evidence re-entry and a 30-second event lease left by SIGKILL; a bounded agent continuation worker re-enters the normal cap claim. The intermediate combined run passes 8/9 and exposes an incorrect error-field assertion in the new control; the final run checks the actual typed cause and restores altered fixture evidence in `finally`. Data-only source checks now guard create admission/ACK, prompt admission and original result application. This additive worker remains globally dormant until S2.12. The full five-file regression initially passes 51/52 (`s2-8-agent-checkpoint-regression.json`); its last failure exposes fixture processes and DB pools retained until file teardown. Per-scenario graceful shutdown then passes all 35 agent owner cases without skips (`s2-8-agent-checkpoint-isolated.json`); the other 17 storage/park/finalizer cases already pass. All 11 shared Flow create controls pass (`s2-8-agent-checkpoint-flow-create.json`, 41 unrelated cases filtered). The delayed-evidence API response retains the actual NeedsInputIdle status and then resumes (`s2-8-agent-checkpoint-response.json`, 1/1 selected). Strict types and scoped lint pass; 60 relative links and 16 Mermaid blocks in the two owning documents pass. Hook/budget pause handoffs, autonomous pre-turn admission, draft integration and full family activation still keep S2.8 unchecked.

S2.8 live permission source qualified: a real writable-worktree ACP request first lacks the original turn binding (`s2-8-agent-permission-worktree-red.json`, 0/1 selected). Earlier none-workspace probes exercised automatic headless arbitration and are not HITL evidence. Live and launcher-restart cases pass after retaining the source and delivery (`s2-8-agent-permission-source-green2.json`, 2/2 selected). Responder SIGKILL before ACK then exposes a duplicate input from legacy event replay (`s2-8-agent-permission-source-qualified.json`, 7/8 selected). Source-bound replay fixes this exact control and autonomously completes the original ACK without user retry (`s2-8-agent-permission-replay-green.json`, 8/8 selected including five existing service/ledger cases). Strict types and scoped lint pass. Checkpoint handoffs and full-family continuation remain pending.

S2.8 completed-turn resume qualified: the missing-turn control fails first (`s2-8-agent-resume-red.json`, 0/1 selected). Live completion, both actual SIGKILL windows and four existing cap controls pass after atomic admission (`s2-8-agent-resume-green.json`, 7/7 selected). FIFO/live-message and full-pool promotion regressions also pass (`s2-8-agent-resume-queue-regression.json`, 2/2 selected). Strict web types and scoped lint pass. The next increment is the source-bound checkpoint permission handoff specified in the owning lifecycle document; S2.8 remains in progress.

S2.8 completed-turn resume contract (Designed): a capacity-approved idle wake without queued input retains the last applied turn's original prompt as a new `resume` turn in the same transaction as its assignment claim. It does not rebuild accepted input from the current catalog. Queued messages retain priority and their own identity. Creation, terminal evidence, park and restart then use the common owned-turn path. Qualify live execution and actual launcher death before terminal evidence and before application. Historical interrupted/checkpointed turns require a separate source-proof handoff before they can authorize continuation; this increment does not grant that authority.

S2.8 rework increment qualified: its source/result/capacity controls fail first (`s2-8-agent-rework-red.json`, 0/2 selected), then all four live/SIGKILL/capacity cases pass (`s2-8-agent-rework-green.json`, 4/4 selected). The actual API-service response reports the completed status (`s2-8-agent-rework-response.json`, 1/1 selected). Supporting API guards pass 11/11; their shared-tree companion exposes missing canonical teardown in the test host (`s2-8-agent-rework-guards.json`, 12/13). Publishing checkpoint/delete lifecycle events and asserting the applied shared rework restores all four promotion/real-host lifecycle controls (`s2-8-agent-rework-compatibility.json`, 4/4). MCP tool contracts pass 56/56; strict types, scoped lint and owning docs checks pass. Resume, historical handoffs, autonomous agent continuation and draft integration remain pending; S2.8 stays unchecked.

S2.8 rework contract (Designed): preserve the existing Review/promotion fence and acquire the scheduler cap lock before the workspace/run claim. A full agent pool refuses rework before changing Review or accepting input. A successful claim atomically mints `rework_return`, stales the prior public result and stores the original rework prompt as one generation-owned turn. Re-entry selects that turn/create/command before rebuilding current catalog input. The common agent owner publishes exactly one new result revision from the original complete output; old generations cannot publish into it. Qualify live execution, SIGKILL before terminal evidence and before application, plus capacity refusal and existing workspace promotion guards.

S2.8 owned-create increment qualified: generated migration `0154_agent_owned_create` extends the private create-intent authority to agent turns. Initial input now persists before creation, and restart reuses its saved create/prompt before current catalog or credentials. Both actual SIGKILL boundaries first fail (`s2-8-agent-create-red.json`, 0/2 selected), then pass with queue regression (`s2-8-agent-create-green1.json`, 4/4 selected), even after changing the agent definition. A real unavailable ACP resume first leaves its accepted input unfinished (`s2-8-agent-create-refusal-red.json`, 0/1 selected); source-bound failure settlement then passes live and before-commit death (`s2-8-agent-create-refusal-green.json`, 2/2 selected), with no fresh-session fallback. Full agent/admission/schema regression passes 50/50 without skips (`s2-8-agent-create-qualified.json`); all 11 Flow create refusal/restart/stale/unknown controls pass (`s2-8-flow-create-regression.json`, 41 unrelated cases filtered). Strict web types, scoped lint and owning docs checks pass. Resume/rework, historical permission/wait handoffs and family activation remain pending; S2.8 is not complete.

S2.8 message increment qualified: `sendAgentMessage` persists immutable input, claims under scheduler/run/turn locks, binds one original owned command and acknowledges it atomically with park/finalization. Optional API/MCP request keys deduplicate only identical same-run input. The real full-pool control first fails by starting above cap (`s2-8-message-cap-red.json`, 0/1 selected). Message SIGKILL recovery passes three cases, while the expanded dequeue case exposes C3's missing assignment mint (`s2-8-message-reentry-first.json`, 3/4 selected). Reusing the resume claim fixes that exact scenario (`s2-8-message-promotion-qualified.json`, 1/1 selected). Two busy-session messages plus a duplicate request survive launcher death and drain once in order (`s2-8-message-fifo.json`, 1/1 selected); park restores the oldest remaining queued wake. The full six-file agent/message/park/scheduler regression passes 43/43 without skips (`s2-8-message-regression.json`), MCP contracts pass 231/231, strict web types and scoped lint pass. Documentation links/ADRs/Mermaid/indexes and the 118-table DBML check pass. Pre-create, resume/rework and historical permission/wait handoffs remain S2.8 work; global activation is S2.12.

S2.8 pre-create contract (Designed): initial agent input is retained in `agent_turns` before session creation, using the existing launch assignment ID and ordinal zero. Message inputs retain their accepted IDs and ordinals. Extend the private owned-create intent with an agent-turn authority, preserve its exact create bytes, and reattach the saved turn/create before catalog, token, credential or prompt preparation on restart. Agent creation must not inherit the Flow-specific fresh-session fallback on CHECKPOINT refusal. The create ACK and prompt binding recheck the same run, assignment and turn; delayed historical ACKs cannot bind a successor. Qualify real process death after host creation but before manager ACK and after ACK but before prompt admission, checking one create/one prompt and unchanged accepted input. Global family activation remains S2.12.

A definitive owned-create refusal settles the same current claimed turn as superseded (no prompt was dispatched) and the run as Failed in one transaction. Re-entry repeats that exact settlement after process death before its commit. Require the original failed create, agent turn and current assignment under the run lock; unknown admission stays pending. An unavailable retained ACP handle must not produce a new empty session or a replacement prompt.

S2.8 turn-storage preparation qualified: additive generated migration `0153_agent_turn_admission` and `acceptAgentMessage` persist queued input under the run lock. The absent-storage control fails first (`s2-8-turn-storage-red.json`, 0/1); after creating the table, the source-rewrite control still fails while three admission cases pass (`s2-8-turn-immutability-red.json`, 3/4). Postgres source/binding/transition guards then pass all five admission tests, including exact immutable command binding, active-turn uniqueness and run cascade (`s2-8-turn-binding-qualified.json`, 5/5). The preceding storage/schema regression passes 26/26 (four admission plus 22 existing schema cases). Strict types and scoped lint pass. Three Mermaid blocks, 19 ADR anchors, 99 relative links and 125 indexed files pass; generated DBML is current at 118 tables. The pnpm ERD CLI hit sandbox IPC-listen EPERM; executing the same generator with Node's tsx import and `--check` passes. The required A/B runner includes this suite. Caller/cap/recovery wiring remains pending; this is preparation, not completed S2.8.

S2.8 durable turn admission contract (Designed): use `agent_turns` for accepted agent invocations whose lifecycle begins before host dispatch or a capacity claim. Existing `run_messages` is a canonical transcript projection with its own sequence allocator, so it cannot also own queued input, acknowledgment and restart claims; `run_sessions` owns bindings rather than individual turns. This is an agent-only input entity, not a second Flow ledger. Persist server-generated turn ID, run-local ordinal, variant and immutable original prompt before dispatch; message callers may supply an optional request key (never a row ID), whose same-run reuse returns the same turn only for identical input. Distinct accepted messages remain distinct. Serialize admission under the run lock, enforce immutable source fields in Postgres and allow at most one bound turn per run. Bind only under the existing scheduler-cap/run/assignment claim, retain the exact session/incarnation and command identity, and acknowledge with the existing domain transition and command marker. Terminal/cancelled/superseded authority cannot bind queued input. A busy session must not receive overlapping request-v2 prompts; accepted input waits durably for its prior owned turn and normal capacity-safe re-entry. Initial/rework/resume inputs reuse their existing launch/claim identities, and create recovery replays the existing private create intent rather than rebuilding credentials. The family worker and global activation remain pending until caller/restart qualification. Apply an additive generated migration; rollback disables new admission first and preserves accepted pending input for forward recovery.

S2.8 persistent-first increment qualified: the live control first fails because a successful persistent turn remains Running (`s2-8-persistent-first-red.json`, 0/1 selected). The owned park transition then passes all ten initial/persistent launcher cases and five existing park regressions (`s2-8-persistent-first-green.json`, 15/15, no skips), including SIGKILL before terminal evidence and before the park transaction. Two additional persistent non-end-turn/adapter-error cases pass and confirm ordinary Failed settlement, no terminal public result, exact process cleanup and capacity release (`s2-8-persistent-first-failures.json`, 2/2 selected, 10 unrelated cases filtered). Strict types, scoped lint and owning documentation checks pass. Initial nonpersistent and persistent turns now share the owned result boundary; the remaining resume/rework/message/pre-create arms keep S2.8 unchecked.

S2.8 persistent-first contract before implementation: retain the same persisted initial-turn identity and exact session/assignment checks. A successful `end_turn` closes its process before atomically parking the persistent run in `NeedsInputIdle`, preserving its ACP resume handle and releasing its assignment with the command application marker. Do not publish a terminal public result or terminal event for a parked turn. Failed ACP completion still uses ordinary failed finalization. Parked status is durable; post-commit pool promotion is an advisory hint backed by existing scheduler recovery. Qualify the live completion and actual launcher death before completion and before the park transaction commits, with one original prompt and no live process left behind.

S2.8 initial-turn increment qualified: all seven production-launcher cases pass without skips (`s2-8-initial-qualified.json`), including full original output, SIGKILL before completion/application, autonomous recovery, superseded generation and failed/non-end-turn teardown. The qualified failure control first leaves both runs Running (`s2-8-initial-failure-red-qualified.json`, 0/2 selected); exact-session teardown makes them settle Failed and release capacity. Removing the application-generation guard makes the stale-source control finalize its successor (`s2-8-initial-generation-mutation.json`, 0/1 selected); the guard is restored. Domain deferral survives six pending attempts without poison and runs cleanup only after the committed marker (1/1 selected control). All 63 bounded-output/finalizer regressions pass without skips; three selected Flow decoder/incarnation regressions pass (49 unrelated cases filtered). Strict web types, scoped lint, nine documentation links and two Mermaid blocks pass. The two new agent suites are required by the A/B runner. S2.8 remains unchecked: persistent, resume, rework, messages, pre-create recovery and full-family qualification are still pending.

S2.8 initial teardown contract: after verified original prompt completion, close only its exact current session through the existing fenced command ledger before releasing the assignment or cleaning its workspace. A non-`end_turn` response or adapter error remains a failed result even if partial text contains a valid public-result block. Recheck current launch/turn/session authority before issuing teardown and again in the terminal application transaction; a checkpointed or superseded incarnation cannot finalize the run. Natural reaping may already have completed; pending lifecycle projection is a retryable domain condition with no poison-attempt increment.

S2.8 initial-turn contract before caller activation: the first nonpersistent agent prompt belongs to its persisted launch assignment, logical run session and concrete incarnation, with `turnId` equal to that launch assignment ID and prompt ordinal zero. Admit one immutable owned command under that authority; restart must reattach its existing command before rebuilding any prompt or creating another session. Apply fully verified original output against the launch-time public-result contract and exact current binding through the prepared finalizer, with the command application marker in the same transaction. A raw process exit must not race this owned result with stack-local text. Missing authority, superseded assignment/session and checkpointed status cannot finalize a successor. Post-commit cleanup remains generation-bound with existing durable GC backstops. Qualify a result after a multi-megabyte prefix and actual web-process death before terminal evidence/application. Persistent, resume, rework, message and pre-prompt create recovery remain separate required S2.8 arms before the family is complete.

S2.8 finalization preparation increment: the existing launch finalizer delegates to a typed prepared terminal operation; path helpers retain their existing public exports. Preparation performs external read-only inspection on a pooled DB connection; the DB-only operation rechecks provenance under the run lock and keeps status/public-result/events atomic, while cleanup verifies the committed generation. Existing finalizer behavior passes 35/35 before extraction. Four new real-Postgres/filesystem controls cover pooled-connection refusal, outer rollback, changed provenance and competing finalizers. The pooled-connection guard first fails its selected control (`s2-8-finalization-pooled-red.json`, 0/1 selected), then all four pass. After wiring the original launch entrypoint to the new module, all 39 cases pass without skips (`s2-8-finalizer-wired.json`); strict web types, scoped lint, nine documentation links and two Mermaid blocks pass. This prepares the required agent owner application boundary; turn/message admission, capacity-safe deferred messages, restart recovery and S2.8 completion remain pending.

S2.8 finalization preparation contract (Designed): split the existing agent terminal choke point into preparation, a DB-only application, and post-commit cleanup. Preparation reads the launch-time result contract and immutable workspace provenance, decodes verified command output, and performs the existing read-only workspace inspection before the application transaction. Application locks the run, rechecks that provenance and the exact turn/assignment/session authority, and commits the ordinary status, public result, token revocation, assignment release and domain/webhook events with the command application marker. A pending human-ask activation must remain pending, and a changed generation cannot finalize a successor. Cleanup may run only after the outer transaction commits; retain existing materialization/directory/context-mount GC backstops. This is preparation for the agent owner adapter, not its activation or an additional workflow ledger. Qualify rollback and competing finalizers as well as the existing result, shared-tree and persistent-park suites. S2.8 precedes S2.7 because consensus drafts depend on this adapter interface.

S2.6 final qualification (Implemented): the selected original prompt may finish successfully or with an agreed adapter-unavailable error during checkpoint teardown; both retain the same-ordinal result handoff. The six new real ACP overlap cases first fail at the old pending disposition (`s2-6-checkpoint-completion-red.json`, 0/3 selected; `s2-6-checkpoint-adapter-error-red.json`, 0/3 selected), then pass. The first mandatory web gate passes 192/198 and exposes the fixture reading its completion text after ordinary permission journal cleanup; limiting the late read to the held original prompt fixes that fixture regression. The affected permission suites pass 29/29 (`s2-6-checkpoint-fixture-regression.json`). The final mandatory web gate passes 198/198 across 17 files without skips (`s2-6-final-ab-web-qualified.json`); domain regression passes 33/33, existing pure resume/gate/journal suites 70/70, and the real supervisor checkpoint/resume spike 1/1. Strict web types, scoped lint, nine documentation links and two Mermaid blocks pass. Evidence is under `/private/tmp/maister-ab-implementation`. S2.6 is complete; global owner and graph recovery worker activation remains S2.12.

S2.6 completed-source/checkpoint overlap contract: an agreed successful `session.prompt` response remains completed command evidence even when its terminal event follows checkpoint acceptance in the same canonical host stream. A confirmed original input plus the exact acknowledged checkpoint may transfer the fully verified output at the same ordinal using `permission_result`; do not wait forever or create a continuation turn. Likewise retain an agreed adapter-unavailable failure as a failed result; only a post-checkpoint `ACP_PROTOCOL` interruption authorizes `permission_continue`. Both event identities and their shared host ordering must still be proven. Qualify the real selected prompt finishing during checkpoint teardown for node and both gate kinds, with no new prompt, parent replay or duplicated successor.

S2.6 rejected-input settlement contract: a request-bound original `session.input` receipt with rejected phase, HTTP 410 and `HITL_TIMEOUT` proves that this delivery did not resolve the deferred. Once its exact source prompt has agreed terminal evidence and its checkpoint is acknowledged, an explicit current-domain transaction may settle a still-`NeedsInputIdle` Flow as Failed with `HITL_TIMEOUT`, matching the live response failure policy. Lock the current run, original HITL/input/prompt/assignment/incarnation and exact latest attempt/evaluation; recheck the response, revision, request/terminal digests and checkpoint scope. Refuse a successor claim, changed owner/choice or conflicting receipt. Retain the original action/verdict evidence, record the rejected delivery in HITL audit, close the pending human assignment and emit the existing terminal events once. This is a terminal management decision: do not allocate capacity, mint another execution assignment, fabricate a prompt result, redeliver the choice or start another prompt. Missing/accepted/other rejected receipts remain retryable until classified. Qualify node and both gates through real deferred cancellation, rejected original input and responder SIGKILL before the web transaction commits; include rollback and changed-source controls. No new execution ledger is introduced.

S2.6 rejected-input increment: the preflight classifies only an original HTTP 410 `HITL_TIMEOUT` receipt as the known rejected-delivery arm. A shared locked evidence reader serves positive delivery and rejection with separate receipt predicates. The new current-domain settlement checks the released source and latest owner under the run lock, keeps its original action/verdict, atomically records rejection/HITL closure and ordinary terminal assignment cleanup, and emits `run.failed` once without claiming capacity or another prompt. Three real cancellation/responder-SIGKILL cases first return retryable pending (`s2-6-rejected-input-red.json`, 0/3 selected, fourteen filtered), then pass. A rollback after the complete settlement leaves the run/HITL/event ledger unchanged; a different input reference or changed gate parent action refuses the transition (`s2-6-rejected-input-controls.json`, 3/3 selected). Removing only the parent digest guard makes the changed-parent control incorrectly succeed (`s2-6-rejected-input-mutation.json`, 0/1 selected, sixteen filtered); source is restored before ordinary verification. All three rejected-input cases also verify no remaining active assignments. Final combined regression passes 35/35 without skips (`s2-6-rejected-input-regression.json`), and existing resume/journal suites pass 31/31. Strict web types, scoped lint, nine documentation links and two Mermaid blocks pass. No schema or host production change is required. Final S2.6 qualification and the successful-source/checkpoint overlap disposition remain to verify.

S2.6 adapter-unavailable result contract: an original prompt's request-bound, receipt/event-agreed `EXECUTOR_UNAVAILABLE` rejection is a completed failed action, including completion during checkpoint teardown. A confirmed original permission input may transfer that exact failure through the existing result handoff, preserving the node error code and the ordinary failed gate verdict with no new turn. This code does not authorize the `permission_continue` path; that interruption authorization requires post-checkpoint `ACP_PROTOCOL`. Keep missing/accepted input receipts, conflicting evidence and fenced sources non-applying. Qualify an actual ACP request error after confirmed input for node and both gate kinds, with responder SIGKILL before manager ACK. No schema change is required.

S2.6 adapter-unavailable increment: the shared agreed-terminal predicate admits `EXECUTOR_UNAVAILABLE`, while a separate interruption predicate keeps preflight, claim and historical continuation checks restricted to `ACP_PROTOCOL`. The resumable adapter fixture throws a real ACP request error after the selected permission; all three new owner cases first remain pending (`s2-6-unavailable-red.json`, 0/3 selected, eleven filtered). After implementation the expanded suite passes 14/14, and the final combined four-suite regression passes 32/32 without skips (`s2-6-unavailable-regression.json`). The supervisor checkpoint/resume spike passes 1/1; strict web types, scoped lint, nine documentation links and two Mermaid blocks pass. The existing same-ordinal result authorization preserves the failure and prevents an extra prompt, successor or parent action. No migration is required. Authoritative no-effect input rejection and final S2.6 qualification remain open.

S2.6 confirmed-input interruption continuation contract: when the original input receipt proves delivery, the source prompt has an agreed non-quarantined `ACP_PROTOCOL` failure after checkpoint acceptance in the same host stream, and that exact checkpoint has succeeded, the ordinary capacity claim may continue the retained ACP session. Record a distinct `permission_continue` authorization with original input/checkpoint/source incarnation lineage. Advance the same node attempt or gate evaluation by one prompt ordinal; preserve the gate's parent action digest and result. Atomically settle the original input/HITL and move to Running. Create, prompt admission and graph reentry recheck the persisted handoff evidence; no original permission choice is redelivered. Any later permission is a new HITL requiring its own decision. Restart after claim reuses the authorized ordinal and create intent. A refused retained handle fails without an empty-session replacement. The original accepted prompt remains historical and fenced. Add a forward schema migration after 0151, qualify node and both gate kinds with actual responder SIGKILL, and retain explicit pending dispositions for missing receipts, unproven ordering and unsupported failures.

S2.6 confirmed-input interruption increment: forward migration `0152_flow_permission_continuations` admits the distinct authorization on the existing attempt/evaluation. Node and gate claims advance only the authorized ordinal; create, admission, application and graph reentry validate the retained input/checkpoint/incarnation lineage. Shared source schemas and evidence readers now live below Flow to avoid a create/graph runtime import cycle. The initial three real responder-SIGKILL cases fail before implementation (`s2-6-permission-continue-red.json`, 0/3 selected), then pass for node and both gate kinds. The expanded matrix first passes 10/11: a refused node resume handle entered the old empty-session replacement branch. Extending that branch's refusal to the new authorization fixes the actual defect; the focused refusal passes 1/1 and the full new matrix passes 11/11 (`s2-6-permission-continue-shared.json`). It includes real process death after a gate claim, independent new permissions, and node/gate create refusal for a schema-valid but incorrect input command reference. The first mutation inadvertently broke initial creation and is not guard evidence; the corrected mutation bypasses only the continuation check and fails because the tampered create is wrongly accepted (`s2-6-permission-continue-mutation-qualified.json`, 0/1 selected, ten filtered). Source was restored byte-for-byte, SHA-256 `0d3a4ca656952d754581576950c1141f77cb57546607edaf6c8122a7cd023230`. Two combined runs each pass 27/29 and expose existing tests starting a claim/mutation before the checkpoint event reaches Postgres. Those tests now await the read-only completed-evidence preflight; their capacity-race and source-change assertions remain. Final combined regression passes 29/29 with no skips (`s2-6-permission-continue-regression-qualified.json`); existing resume/journal suites pass 31/31 and the supervisor checkpoint/resume spike passes 1/1. Strict types, scoped lint, 88 documentation links, two Mermaid blocks and the 117-table ERD pass; Drizzle finds no additional changes. Evidence is under `/private/tmp/maister-ab-implementation`. Other failed-envelope and authoritative no-effect input dispositions plus final S2.6 qualification remain open; no S2.6 or phase completion is claimed.

S2.6 checkpoint ordering contract: compare the source prompt's agreed terminal event with the exact checkpoint command's accepted event in the same host stream. Both events must retain their canonical assignment/session/command identity. Recheck this evidence during capacity claim and historical lineage validation. A post-checkpoint `ACP_PROTOCOL` failure is interruption evidence and uses the explicit continuation contract above; an earlier agreed failure, successful response or explicit adapter-unavailable error uses result handoff. Missing ordering evidence stays pending. Qualify actual input delivery followed by checkpoint while the adapter is still running, distinguishing interruption from completed output. Wall-clock timestamps do not establish this ordering.

S2.6 checkpoint ordering increment: the shared handoff proof now compares canonical host event positions in preflight, claim and historical lineage reads. A real adapter retains its selected prompt until checkpoint teardown; node, AI gate and skill gate previously claimed the resulting `ACP_PROTOCOL` failure as ordinary output (`s2-6-checkpoint-order-red.json`, 0/3 selected, four filtered). The first guarded run exposed a test expecting an exception instead of the public retryable `ok: false` result. After correcting that expectation, all three interruption controls pass; the next seven-case run exposed a test capacity barrier starting before the checkpoint event reached Postgres (6/7). The test now waits for the read-only preflight before testing the claim race. Temporarily bypassing only the new ordering predicate makes the corrected node control fail because it incorrectly claims Running (`s2-6-checkpoint-order-mutation.json`, 0/1 selected, six filtered). The predicate was restored before the final four-suite regression, which passes 25/25 with no skips (`s2-6-checkpoint-order-regression.json`); the existing real supervisor checkpoint/resume spike passes 1/1 (`s2-6-checkpoint-order-host.json`). No schema or wire change is required. The interrupted confirmed-input continuation, remaining input/error dispositions and final S2.6 qualification remain open; this refusal is not reported as their completion.

S2.6 completed-command failure handoff contract: after the original input receipt confirms delivery and checkpoint teardown is acknowledged, transfer the ordinary decoded terminal result under the existing locked `permission_result` claim at the same ordinal. Accept a succeeded command's full verified output, including non-`end_turn` stop reasons, or a failed command carrying the original request-bound, receipt/event-agreed `ACP_PROTOCOL` error. Recheck its immutable request/terminal digests and terminal state inside the claim and historical lineage reads. Node results retain `ok: false` and `ACP_PROTOCOL`; gate results retain the ordinary failed verdict even if partial text contains a pass object. Continue through the existing graph failure path with no fresh prompt, no successor effect and no parent action replay. Missing/accepted input receipts, unverified/quarantined output, fenced commands and other failed-envelope codes remain pending. This increment does not classify authoritative no-effect input rejection. Qualify real responder death before manager ACK for node `max_tokens`, plus node and both gate kinds with unexpected `cancelled` (the supervisor maps it to `ACP_PROTOCOL`). No schema or wire change is required.

S2.6 completed-command failure handoff increment: the shared preflight, claim and historical lineage predicate now accept verified completed output with a failed action, or a receipt/event-agreed `ACP_PROTOCOL` command failure. The action cleanup proof accepts this same recorded failure without addressing the released source. Source state/digests are rechecked under capacity; terminal-conflict quarantine after preflight refuses the claim. Four real responder-SIGKILL scenarios first fail at the previous pending disposition (`s2-6-permission-failure-terminal-red.json`, 0/4); earlier new-fixture driver/argument/wire mismatches are harness failures, not behavioral RED. The first implementation run passes 2/4 and reveals a real event/resume lock-upgrade deadlock. A two-transaction AFTER-FK barrier reproduces it (`s2-6-permission-event-lock-terminal-red.json`, 0/1 selected, 18 filtered); ingestion now takes the allocator's run lock before its FK insert. The initial stale-event control did not allocate a sequence and therefore did not reproduce this deadlock. The new test was moved to its own database after its stream setup polluted the shared ingestion fixture; the subsequent combined event/handoff run passes 23/23. Additional driver diagnostics then identify an initial-HITL timeout caused by concurrent runtime-object catalogue insertion (`s2-6-permission-failure-controls.json`, 3/4). A BEFORE-INSERT two-writer barrier reproduces the primary-key conflict (`s2-6-permission-object-race-qualified-red.json`, 0/1 selected, one filtered). Object projection now inserts atomically and locks/validates the winning row on conflict; changed immutable metadata still fails. The six-case handoff/concurrency suite passes (`s2-6-permission-failure-concurrency.json`, 6/6). The earlier startup observations without child diagnostics are not retrospectively claimed to have this proven cause. Final affected regression passes 96/96 without skips across ten suites (node/gate permissions, gate execution/calibration, lifecycle, full prompt output, ingestion and both concurrency controls; `s2-6-permission-failure-regression.json`). Existing resume/gate pure suites pass 51/51 (`s2-6-permission-failure-unit.json`). Removing only the terminal-conflict guard makes the capacity-race control fail because a quarantined source incorrectly claims Running (`s2-6-permission-failure-quarantine-mutation.json`, 0/1 selected, three filtered); the source was restored before the 96-case regression, SHA-256 `0bbff8a41cd4eb3c289b19e8869fca559b3154390f4ac92995dbafa78394e285`. Both new suites are discovered and included in the mandatory A/B runner. Strict types, scoped lint, nine documentation links and two Mermaid blocks pass. No schema/wire change, full A/B qualification or S2.6 completion is claimed. Other failed-command dispositions, authoritative no-effect input rejection and final qualification remain.

S2.6 completed-source gate handoff contract: an already admitted input remains pending until its original receipt confirms delivery and the request-bound source prompt has a complete, verified result. Decode that result with the same manifest-bound gate parser/calibration used by ordinary owner application. The normal capacity claim must recheck the exact source/request/choice, immutable request and terminal digests, flow revision, checkpoint acknowledgement, latest evaluation and parent action snapshot. Transfer the verdict into that same evaluation at the same gate ordinal under an explicit `permission_result` authorization; retain source input/checkpoint/incarnation lineage and current receiving assignment. Rebind only the parent assignment, preserving its action ordinal/result. Settle the original HITL and input receipt atomically with the claim. Graph recovery consumes this authorized verdict without reopening the released source session, another gate prompt or another paid parent action; the old owner adapter remains fenced. Use a forward migration after 0150 for the additional authorization variant. Qualify AI and skill gates through actual responder SIGKILL before manager ACK, process death after the receiving claim, stale preflight evidence refusal and duplicate continuation. Missing/accepted receipts remain retryable without a claim; other admitted-input classifications remain required before S2.6 completion.

S2.6 completed-source gate handoff increment: migration `0151_flow_gate_permission_results` adds the `permission_result` authorization to the existing evaluation. Normal owner application and handoff share the manifest-bound gate decoder; node/gate handoff claims share the locked original input/checkpoint proof and atomic HITL settlement. Graph reentry verifies the parent and verdict digests plus historical lineage, preserves the gate ordinal and avoids addressing the released source. The continuation worker now discovers completed gate handoffs even after a local parent with no current command. The two real responder-SIGKILL cases first failed at the node-only source preflight (`s2-6-gate-result-red.json`, 0/2), then exposed undefined optional verdict fields changing across JSONB serialization (`s2-6-gate-result-first.json`, 0/2). The decoder now omits absent fields; both cases pass (`s2-6-gate-result-second.json`, 2/2). A third real SIGKILL after claim exposed missing autonomous discovery for a completed gate/local parent (2/3); the final control matrix passes 3/3, including missing receipt, parent change while awaiting capacity and verdict change before graph reentry (`s2-6-gate-result-controls-final.json`). The final combined affected regression passes 49/49 without skips across node/gate permissions, gates, calibration and E1/E2 (`s2-6-gate-result-regression-final.json`). Existing gate owner/crash and orchestrator wake cases pass 10/10 selected, with 42 unrelated cases filtered (`s2-6-gate-result-owner-regression.json`). Before that final pass, the repeated-node checkpoint case timed out before its first HITL in two runs; the subsequent diagnostic nine-case suite and combined run pass, but no cause was established. Child driver diagnostics are now retained; this startup observation remains to recheck during S2.6/S2.12 qualification rather than being called an accepted baseline. A focused pure RED also demonstrates that numeric overflow (`1e999`) cannot round-trip through JSONB; such verdicts are refused instead of hashing non-finite values. The final pure regression passes 70/70 (`s2-6-gate-result-unit-qualified.json`), and all nine gate-result/calibration integration cases pass after that guard (`s2-6-gate-result-json-regression.json`). Strict types, scoped lint, 116 documentation links, three Mermaid blocks and the 117-table ERD pass; Drizzle reports no additional schema changes. No full A/B gate or S2.6 completion is claimed. Remaining admitted-input dispositions are next.

S2.6 checkpointed gate-permission contract: persist a nonnegative `gate_results.prompt_ordinal` (initially 0) and nullable version-1 `permission_resume` authorization on the existing evaluation. The authorization binds the source command/assignment/incarnation/request/HITL/choice, current assignment, new ordinal and retained ACP handle; it also freezes the parent action snapshot identity so graph recovery cannot replay that action. The ordinary NeedsInputIdle capacity claim locks the exact current attempt and latest evaluation, verifies the checkpointed source and absence of an unclassified input, rebinds the parent attempt, and advances only this gate's ordinal. Keep the node action ordinal/result unchanged. New gate commands use that persisted ordinal; create-intent identity remains evaluation plus assignment because every permission claim mints an assignment. Live reissue and input ACK reuse the original HITL only after the existing exact tool/options checks. Pre-prompt discovery, leased graph reentry, create/admission fencing and unavailable-handle refusal must all recognize this authorization. Ordinary restart never advances it. Qualify both AI and skill gates, local and paid parent actions, and process death after claim; no paid parent rerun, new evaluation or skipped verdict/cursor is permitted. Completed-source gate handoff and other admitted-input dispositions remain separate required arms before S2.6 completion.

S2.6 checkpointed gate-permission increment: migration `0150_flow_gate_permission_resumes` records the current ordinal and source/current authorization on the existing evaluation. Capacity claim, leased graph discovery, session creation, prompt admission and permission reissue preserve the parent action snapshot and gate identity. Both AI and skill gates resume through the ordinary lifecycle; a refused ACP handle fails without a fresh-session fallback. The initial real Postgres/supervisor matrix failed 0/5 (`s2-6-gate-permission-red.json`) and then passed 5/5. Expanded controls pass 6/6, including the actual response service, a paid parent, SIGKILL after claim and refusal of a changed parent snapshot before any new command. Temporarily removing only the parent digest check makes that negative control fail waiting for the required driver rejection (`s2-6-gate-permission-parent-mutation.json`, 0/1 selected, five filtered); the guard was restored before regression. Final affected integration regression passes **92/92**, with no skips: 52 prompt-owner, nine node-permission, six gate-permission, 23 gate execution and two lifecycle cases (`s2-6-gate-permission-regression.json`). The existing supervisor ACP resume spike passes 1/1; gate/resume-driver/recovery/migration-journal units pass 39/39. Strict types, scoped lint, 116 documentation links, three Mermaid blocks and the 117-table ERD pass; Drizzle reports no additional schema changes. No full A/B qualification or S2.6 completion is claimed. Completed-source gate handoff and other admitted-input classifications remain.

S2.6 repeated node-permission contract: a fresh permission emitted by a `permission_resume` command belongs to that command and its exact prior HITL owner, while the new request has its own HITL row and choice. A second checkpoint may resume that current request through the ordinary capacity claim, advancing the same attempt once; an original input receipt plus a completed verified source instead permits the existing result handoff at the same ordinal. Share the closed node/resumed-node source schema with live permission delivery, and require exact variant/HITL identity when matching historical source evidence. Never accept a gate source through the node path. Qualification uses a real adapter that replays the first permission and then emits a distinct second tool request; cover both second checkpoint/new turn and lost-input-ACK/completed-source handoff, with one gate/cursor continuation and no reuse of the first permission's choice for the new request.

S2.6 repeated node-permission increment: live delivery and idle resume now use the same closed node/resumed-node source schema. Both the claim and historical result cleanup require the exact prior HITL owner on a `permission_resume` source; gate sources remain excluded from the node path. Two real chained-permission cases first fail at the old node-only schema (`s2-6-repeated-permission-red.json`, 0/2 selected), then pass (`s2-6-repeated-permission-first.json`, 2/2 selected). The second request remains unanswered until its own choice is recorded. A second checkpoint produces ordinal 2 and three prompts; a SIGKILL during the second input ACK instead hands off the completed ordinal-1 result with two prompts. Both preserve one attempt, gate and successor effect, and reject a changed prior HITL identity without claiming an assignment. Final scoped regression passes 27/27 selected cases across the nine checkpoint/resume/handoff cases, 16 live node/gate permission windows and E1/E2 (`s2-6-repeated-permission-regression.json`; 36 unrelated cases filtered). The existing real supervisor resume spike passes 1/1; legacy resume-driver/recovery unit suites pass 12/12 (`s2-6-repeated-permission-legacy-unit.json`). Strict web types, scoped lint, nine documentation links and two Mermaid blocks pass. No schema/protocol migration or full A/B rerun is claimed. Gate checkpoints and remaining admitted-input classifications keep S2.6 open.

S2.6 orchestrator permission-source contract: a child wake may continue a completed `node` or `permission_resume` prompt only through the exact parked attempt and its released `waiting_on_children` assignment. A `permission_result` snapshot instead requires its explicit, verified checkpoint/input/HITL handoff to that parked assignment; the historical source assignment remains released as `checkpointed` and gains no application authority. Preserve this handoff lineage in the next orchestrator authorization so a pre-prompt rollback/reclaim reuses the same ordinal and source. Detect an already admitted current prompt using both node-owner variants. Qualify real Postgres/supervisor permission continuation and completed-source handoff through child wake, rollback/reclaim, duplicate child delivery and a fresh permission request on the next turn. Extend the existing JSON constraint only by forward migration if additional persisted lineage is needed. No global activation or Stage C placement change.

S2.6 orchestrator permission-source increment: migration `0149_flow_orchestrator_handoff_sources` retains the full verified `permissionResult` lineage in the next orchestrator authorization. Cleanup and child wake share the source-evidence validator; source and receiving assignments retain their separate checkpoint/wait authority. Both real Postgres/supervisor paths first failed at the child-wake authorization (`s2-6-permission-orchestrator-red.json`, 0/2 selected), then passed (`s2-6-permission-orchestrator-first.json`, 2/2 selected). Final scoped regression passes all seven permission-resume/handoff scenarios and three existing orchestrator wake windows, including SIGKILL and capacity deferral (`s2-6-permission-orchestrator-regression.json`, 10/10 selected; 49 unrelated cases filtered). The new paths include pre-prompt rollback/reclaim, fresh permission delivery, duplicate child events, and refusal of changed handoff provenance both before claim and after rollback. Strict web types, scoped lint, links/Mermaid and the generated 117-table ERD pass. Existing resume-driver and migration-journal unit suites pass 26/26 (`s2-6-permission-orchestrator-unit.json`); Drizzle reports no remaining schema drift. This is an increment, not S2.6 completion or a full A/B qualification. Repeated/gate checkpoints and the remaining admitted-input classifications are next.

Current task: **S3.6 — next** (S3.1–S3.5 qualified, C07 closed). Completed: **31/45**, with **S5.2 core qualified** and its remaining matrix still pending.

S2.6 checkpointed node permission increment: migration `0147_flow_permission_resumes` extends the existing action authorization with the exact HITL/source request/choice. The capacity claim rebinds the same attempt and persists its new ordinal before the leased graph driver creates a session or prompt. The original HITL survives reissue; full action output continues through the normal gate/cursor reducer. RED reproduced the missing attempt binding/ordinal (`s2-6-permission-resume-red.json`, 0/1). Direct capacity resume and actual SIGKILL after claim pass (`s2-6-permission-resume-matrix-second.json`, 2/3; the third fixture initially emitted generic ACP_PROTOCOL instead of CHECKPOINT). Explicit SDK RequestError now qualifies unavailable-handle refusal without empty-session replacement (`s2-6-permission-resume-refusal-final.json`, 1/1, two filtered). The refusal also exposed a terminal CAS that excluded NeedsInput; the exact leased permission generation can now close Failed/Crashed. Legacy unit seams pass 20/20 after keeping their pre-owner fixtures outside the new durable predicate. Existing Postgres state transitions and startup recovery pass 49/49 (`s2-6-permission-resume-legacy-pg.json`); strict types, scoped lint, 88 links and two Mermaid blocks pass. Full A/B run passed 157/158 across 13 suites with no skips (`s2-6-permission-resume-ab-web.json`), including all 52 existing Flow owner cases and all three new resume cases. The lone E1 failure assumed session creation was synchronous with HTTP 202. Updating that wait exposed missing documented resume audit fields; the input ACK now retains original/reissued request IDs and deliveredViaResume. The final focused run passes all five new and existing real-supervisor lifecycle cases (`s2-6-permission-resume-final.json`, 5/5, no skips), including the actual respondToHitl entrypoint. The full lane was not repeated after this audit-only production correction; it must not be reported as a combined 158-case passing run. The completed-source handoff is covered by the following increment; other admitted-input classifications, repeated/gate checkpoints and final activation remain. S2.6 is unchecked.

S2.6 completed-source handoff increment: forward migration `0148_flow_permission_result_handoffs` adds `action_resume.kind = permission_result` to the existing attempt authorization. It retains ordinal and original verified output, records original input/checkpoint/incarnation IDs and binds the snapshot command with a CHECK. The original input receipt and full request-bound output are read before the capacity claim; the claim locks/rechecks the source, choice, revision, attempt, assignments and acknowledged checkpoint. It atomically settles the input, responds to the HITL/human assignment and moves the run to Running. Historical owner application remains fenced; cleanup validates the exact checkpoint command rather than expecting a stale-epoch lifecycle event to update the live incarnation projection. RED is a real SIGKILL while the input ACK transaction is blocked (`s2-6-source-handoff-red.json`, 0/1). The same source then completes through its gate/cursor with one prompt and ordinal 0. Four checkpoint/resume cases now pass, including missing/accepted original receipt refusal and a real scheduler-lock race that changes the stored choice between preflight and claim (`s2-6-source-handoff-controls.json`, 4/4, no skips). Existing legacy unit cases pass 20/20 after isolating their pre-owner assignment mock; strict types pass. Scoped real-process regression passes 24/24 (30 unrelated cases filtered) across existing node output, node/gate permissions and E1/E2 (`s2-6-source-handoff-regression.json`). The actual respondToHitl retry continues to Review autonomously; its RED exposed a stale NeedsInput HTTP response (`s2-6-source-handoff-route-red-second.json`, 0/1). The response now reports the claimed Running status for handoff, with the OpenAPI updated first. Final verification after separating new-turn authorization from result handoff passes 7/7 real-process lifecycle cases (`s2-6-source-handoff-final.json`, no skips), 20/20 legacy resume unit cases and 98/98 existing response/service unit cases. The new host-journal fault fixture uses a bounded SQLite busy timeout for concurrent real host writes. Strict web types, scoped lint, all eight API/event contract validators, documentation links/Mermaid and the generated 117-table ERD pass. No full A/B rerun or new mutation proof is claimed for this increment. Repeated/gate checkpoints, confirmed-input cases without a successful completed source, orchestrator wake after permission continuation/handoff and S2.12 activation remain; S2.6 stays unchecked.

S2.6 node/cursor work in progress: forward migrations `0142_flow_driver_claims` and `0143_flow_action_completions` add paired run token/lease fields and versioned attempt action snapshots. Live owned-prompt graphs now use the same 30-second claim and 10-second renewal as the bounded continuation worker. A scoped Drizzle handle fences each graph transaction, including its final database-clock lease check; host consumers/application and other runs retain the root handle. Prompt admission checks the claim again after its immutable request insert. Closing the ordinary successful node and selecting its next cursor are atomic. Complete verified node output is decoded independently of the stdout preview. Local actions preceding gates persist their action/file result before gate dispatch. Three real-driver SIGKILL windows (before terminal, before application and after application before node close) now recover the original command through its gate and successor exactly once (`s2-6-graph-recovery-second.json`, 3/3). The first run exposed missing source-session cleanup on recovery (1/3); exact applied-incarnation cleanup fixed the failure. Nine existing graph integration suites pass 83/83 (`s2-6-graph-regression-first.json`). The pure output matrix passes 65/65 and the isolated driver/projection boundary passes 18/18. Strict types pass; scoped lint is clean. Both gate parent recoveries and both N+1 refusal cases pass (`s2-6-gate-parent-first.json`, 4/4); a real crash after node failure preserves the failed attempt without another prompt (`s2-6-failed-node-first.json`, 1/1). Migration `0144_flow_finish_continuations` now records the selected edge, comments, session policy and retry marker with the run cursor. A real SIGKILL reproduced the lost rework target (`s2-6-rework-red.json`, 0/1); recovery now preserves rework and the bounded retry (`s2-6-retry-rework-first.json`, 2/2). Existing retry/mismatch/routing integrations pass 28/28 (`s2-6-finish-regression.json`). The real orchestrator child-wake test reproduced reuse of the old action after assignment replacement (`s2-6-orchestrator-red.json`, 0/1). Migration `0145_flow_action_resumes` now persists its authorized new ordinal and source identity in the capacity-checked domain claim. Park releases the source assignment atomically. Live wake, actual SIGKILL after claim before first prompt, and cap-deferred autonomous wake pass (`s2-6-orchestrator-matrix-first.json`, 3/3). Permission-resume, released-source result handoff, pre-prompt create recovery and final activation still remain; Checkpoint validation: mandatory A/B web passes 119/119 in 12 suites, without skips (`s2-6-node-orchestrator-ab-web-final.json`). Its first run passed 118/119 and exposed an old-schema fixture using current run/assignment writers; the pre-0140 fixture now inserts its historical columns explicitly. The park regression initially caught default artifacts being written before a fenced checkpoint. Checkpoint now precedes artifacts and the atomic park; the fenced driver leaves status unchanged. All four existing orchestrator node/park cases pass (`s2-6-orchestrator-park-final.json`). Strict web types, scoped lint, 88 documentation links and both Mermaid diagrams pass; the generated ERD covers all four forward migrations (117 tables). S2.6 is not complete.
S0.1: baseline refs rechecked; all audit findings retained, canonical requirements/enforcement/acceptance mapping added, false whole-surface implementation and test-proof claims corrected. No behavioral acceptance has run.
S0.2: Designed contracts now live in the owning analytics, API/event schemas, error taxonomy and database field/constraint reference; exhaustive run-status and owner-mode windows are explicit. ADR-167 authority is retained; no new decision record is needed.
S0.3: canonical resource defaults/lease arithmetic and deployment wiring frozen in configuration; isolation, deterministic fault barriers and runner lanes specified in test infrastructure. Real physical-capacity qualification remains an S1 gate.
S0.4: OpenAPI/AsyncAPI parsers, adapter mirrors, 23 Mermaid blocks, 122 ADR anchors, relative links, canonical indexes and 115-table ERD pass. Existing Vitest discovery is nonempty: command-recovery 9 cases (including V7b), ingest 12, retention 1, historical import 1, host outbox 3, host objects 4. Discovery used installed Vitest directly (`node node_modules/vitest/vitest.mjs list <files> --project=integration` from each package); pnpm 11 attempted dependency replacement before execution and was not allowed to do so. Discovery used Node 26.3.0 and is not Node 24 behavioral qualification. No acceptance scenario has run yet.
S1.1: RED reproduced V7b `UND_ERR_INVALID_ARG: invalid content-length header` on Node 24.15.0; three local preflight cases also failed before the change. GREEN: 7 selected AT-17/V7b cases pass on both Node 24.15.0 and 24.19.0, including real manager full/range reads, chunked HTTP, no-dispatch refusals, body timeout and cancellation. Eight unrelated V cases were filtered, not qualified. Existing wire unit suite: 30 pass; web typecheck and changed-file ESLint pass. Reports are `/private/tmp/maister-ab-implementation/s1-1-{red,final}-node24.15.json`, `s1-1-final-node24.19.json`, and `s1-1-wire-unit.json`. Image qualification belongs to S1.6 and the S1 release gate remains open.
S1.2: RED reproduced the 65,537-byte supervisor crash, terminal-before-drain ordering, historical-reference loss after assignment release, and 31-MiB retained output under 20 producers. GREEN: 34 real-PG manager cases, 44 host lifecycle/HITL/guardrail cases, shared JSON/wire and 10 content-authorization/wire cases pass on Node 24.15. All six frame/tool/text/historical-reference cases also pass on Node 24.19. The isolated 20-producer heap-profile gate passes two maximum-frame waves plus bounded gate-chat success/refusal, 21st-producer refusal and sibling progress; it verifies every producer/decoder reservation is released. The standalone maximum-frame profile measured 4,457,616 retained output bytes after fixing generator-held frames, within the 10-MiB pool. References use the closed v2 payload version, preserve exact immutable UTF-8 JSON via bounded encoding, and hydrate outside domain transactions under source-command/fence checks. Evidence: `/private/tmp/maister-ab-implementation/s1-2-{manager-final,guards-barrier,memory-gate,node24.19,auth-wire-final}.json`, `s1-2-memory.{json,heapprofile}`, `s1-2-memory-overflow.heapsnapshot`, `s1-2-barrier-red.json`, `s1-2-historical-red.json`. The S1 release gate remains open: saturation/terminal wallets are S1.3, compatibility/deployment are S1.6, and durable command-wide owner output is S2.
S1.3: RED reproduced retained-only admission, missing producer/upload file promises, absent boot inventory, pinned-reader ACK WAL growth (8,050,512 bytes), and missing physical admission pressure. GREEN: SQLite v7 transactional regular/control/emergency counters and terminal wallets, v8 compact ACK ranges and bounded pruning, and v9 file/wallet/frame reservations now preserve capacity across restart. File writes reserve before capture; uploads serialize writers; confirmed unlink and known final sizes release capacity. A bounded boot inventory preserves and charges orphan object copies and released-workspace logs. Real storage failures latch unavailable, stop producers and preserve evidence without fabricated terminal success. The 66-case integrated host run (`/private/tmp/maister-ab-implementation/s1-3-files-verified.json`) covers outbox, physical storage, receipts, lifecycle, six 25-MiB outputs from two producers, two pressured producers with verified preserved segments, and the 20-producer heap gate. Final native EACCES/overrun/admission cases pass 23 on Node 24.15 (`s1-3-files-fault-final.json`); file/pipe cases pass 11 on Node 24.19 (`s1-3-files-node24.19.json`). V6/V7 upgrade fixtures preserve committed migration definitions. Earlier discriminator evidence remains `s1-3-{verified,ack-ranges,ack-upgrade,physical-red,storage-verified,storage-fault-final,storage-node24.19,files-red,files-inventory-red}.json`. Shared wire and real-PG reference consumers passed at the wallet checkpoint. Host/web typechecks, contract parsers and adapter mirrors pass. Agent output copy capacity remains conservatively charged until S3 immutable sealing; full release-scale physical/deployment qualification remains S1.8. S1.3 is complete; the S1 release gate remains open.
S1.4: RED: both absent-cursor failures were masked by missing-consumer errors; delayed recorder poisoned a successor. GREEN: all 15 ingest integration cases pass on real PostgreSQL/Node 24.15, including the three AT-04 cases. Claim initialization commits before apply, each invocation mints a distinct token, failure recording compares token + starting cursor + failing sequence and preserves bounded diagnostics/event identity. Existing schema suffices; no migration. Web typecheck and changed-file lint pass. Reports: `/private/tmp/maister-ab-implementation/s1-4-{red,green}.json`. Autonomous scheduling and full transaction deadlines remain S1.5.
S1.5: 50/50 integration cases pass on Node 24.15/real PostgreSQL with no skipped cases (`/private/tmp/maister-ab-implementation/s1-5-final-node24.15.json`). The production six-consumer registry recovers after SIGKILL and actual 30-second lease expiry, drains terminal evidence after sequence 200, preserves transcript/cost and competing-worker idempotency. Tests cover cumulative and blocked-query deadlines, lost connections, stale first-failure writers, cross-run gap seeding, 100-run persistent backfill, 1-MiB quantum and generation/cursor-bound rearm. Claim/apply/failure transactions use the same bounded connection path and DB clock. Transcript and cost now apply incremental DB-only changes; explicit runner reattribution remains supported. Pre-cutover importer seeding also passes on the real 0133 lineage (`s1-5-import-seeding.json`). Additive migrations 0137–0139 and generated 117-table ERD describe the projection fields. Production deployment shutdown wiring and full image/runtime qualification remain S1.6/S1.8; scratch owner continuation remains S2.
S1.6: qualified runtime and deployment wiring complete. Root/web/supervisor enforce Node >=24.15.0 <25; strict host/worker config and SET/remove/SET tests pass. Supervisor A/B lane passes 79 cases on each patch; web passes 57 on 24.15 and 58 on 24.19 (the additional shutdown/DB-loss case also passes separately on 24.15). Mandatory CI rejects missing/empty/skipped suites and covers both runtimes plus the production image. Evidence: `/private/tmp/maister-ab-implementation/s1-6-ab-{supervisor-final,supervisor-node24.19,web-final,web-node24.19}.json`, `s1-6-{host-config,projection-config,shutdown-final-node24.15}.json`. Node 24.15 bundled Undici is 7.24.4; Node 24.19 bundled Undici is 7.29.0; binary requests use pinned package Undici 8.4.1. Docker base is Node 24.19.0-bookworm-slim index `sha256:a9f5f7c91a432850b2a8a7797adf5eadb6c733ceed61167806cee7ea7fbc29df`; the built/tested linux/arm64 image is `sha256:090588346ded791dd54b5fbbfb35e8eb2bae69b8dc8d2b620631748c5e77e3df`. Its seven real-supervisor binary cases and default ENTRYPOINT/CMD production HTTP + SIGTERM + zero remaining PostgreSQL sessions pass (`s1-6-image-{direct-binary.json,direct-smoke.log,inspect.json,runtime.json,direct-build.log}`). RED caught asynchronous Next instrumentation readiness and pnpm exiting 143 before forwarding SIGTERM: image/systemd now run Node directly and await the shared activation promise. Shutdown cancels prepare/stream/retry waits, drains timers/HTTP/DB, preserves claims on unconfirmed DB cleanup and waits for supervisor output/terminal barriers. Compose renders only postgres; compose.override is confirmed absent. Types, scoped lint, three runtime contracts, eight API/event parsers and adapter mirrors pass. S1 release gate remains open.
S1.7: affected assertions and regression coverage complete. W4 now uses the production six-consumer worker and proves recovery without a process-local SSE subscriber; the log-redaction fixture no longer races an unrelated adapter exit timer; cancel fixtures reserve the producer wallet; instrumentation fixtures expose and drain lifecycle callbacks. No skipped case in the mandatory A/B runner. Existing unit suites pass 355 supervisor and 7,499 web cases (`s1-7-{supervisor,web}-unit-final.json`); web was repeated with four workers after a Docker-probe timeout under excessive local concurrency, and that probe also passed separately. The real pressure/restart, output-reference, pinned-reader and output-memory scenarios remain in the mandatory S1 runner. Full phase/one-host launch/HITL gates remain S1.8.

S1.8 complete: full supervisor integration passes 182/182 and full web integration passes 3,608/3,608 across all 435 files, without skips (`s1-8-{supervisor,web}-final.json`). Initial W7 log-name drift and four old harnesses without the production projection worker were corrected; the two Docker host-port setup failures passed on rerun and in the full final run. The default-capacity test fills the outbox beyond 400 MiB, retains pressure/history through ACK+restart, refuses fresh producers and records a reserved terminal below the 2-GiB physical cap. Preparation additionally checks hydrated byte usage against the quantum, while valid content references remain bounded at SQL selection. A real-PG RED proved worker shutdown falsely succeeded after unconfirmed cleanup (`s1-8-shutdown-worker-red.json`); stop now propagates that failure and preserves the claim, while intentional preparation cancellation releases only its exact claim. The final shutdown correction landed after the full web run started; the final mandatory A/B runner then passed all 65 web cases on both Node 24.15 and 24.19 (`s1-8-ab-web-node24.{15,19}.json`), including all nine owning suites, the new failure window and real HITL/checkpoint/resume. Supervisor's current-patch A/B lane passes 80 cases (`s1-8-ab-supervisor-node24.19.json`); the minimum's full 182-case run includes those same suites. S1.7's full unit results remain 355 supervisor and 7,499 web. Typechecks, full lint (zero errors, 18 pre-existing web warnings), final changed-file lint, 8 contract parsers/3 parser tests, 5 adapter mirrors, 117-table ERD, 85 changed-doc links, 12 Mermaid blocks, 12 ADR links/166 hub entries and 125 indexed docs pass. Both systemd units use direct Node and `KillMode=mixed`. The final linux/arm64 image `maister-ab:s1-8-verified` is `sha256:a33da764e309adc944149d02ce12cf44f1b2467bd3dda8166bee1f2c39d475e1`; its real-supervisor binary suite passes 7/7 and default ENTRYPOINT/CMD HTTP/SIGTERM smoke passes with zero remaining PostgreSQL sessions (74.65 ms shutdown). Evidence: `s1-8-image-verified-{build,smoke}.log` and `s1-8-image-verified-binary.json`. These are local equivalents of mandatory CI lanes; no remote CI, deployment or later owner/import/browser qualification is claimed.

S2.1 complete: forward migration 0140 adds exact private request snapshots, closed typed owner references, transport/application constraints and immutable admission guards. The dormant `issueOwnedPrompt` entrypoint locks owner/routing identity, reuses the stored operation ID/time and rejects a changed request before any effect. Real-PG RED reproduced missing-body admission; GREEN covers concurrent same-key admission, private snapshot round-trip, owner-write rollback, immutable fields and populated 0139 upgrade without fabricated requests. The mandatory A/B web lane passes 70/70 across ten files (`s2-1-ab-web.json`); the focused storage/upgrade lane passes 12/12, pure canonical-data and journal checks 21/21, typecheck, scoped lint, 88 links and two Mermaid blocks pass. Production prompt owners still use the pre-v2 path until S2.5–S2.12.

S2.2 verified partial checkpoint: the common DB-only terminal reducer now owns canonical prompt outcome transitions from projection, admission reconciliation, recovery and wait. Migration 0141 retains the exact receipt/event identity, protects verified evidence/result writes, and restricts referenced-event deletion; matching receipt alone stays pending, nested errors remain intact, and disagreement quarantines application without replacing valid outcomes. Verified results remain readable with the host unavailable. Host replay rejects a different URL-selected target; legacy target-less receipts remain held rather than attaching to a new session. SQLite v10 stores exact JCS request-v2 digests including the URL target, original host key, accepted sequence and terminal stream/sequence; populated legacy upgrades preserve original digests and null missing bindings. Public receipt v2, full command-output manifests and remaining v2 validation are still in progress, so S2.2 remains unchecked. REDs: `s2-2-host-target-red.json`, `s2-2-receipt-first-red.json`, `s2-2-v2-receipt-red.json`. GREEN: 72/72 mandatory web and 83/83 mandatory supervisor cases on Node 24.15 (`s2-2-ab-{web,supervisor}.json`), plus the separate 27-case host identity/receipt and 12-case worker runs. The old V3 receipt-only synthetic rewrite was replaced by actual restart/evidence assertions. Three event-only fixtures now include independently deposited receipts; v6/v7 fixtures reconstruct their actual old schema before migration. One full-run Docker host-port setup failure was followed by the passing worker and full web runs. Typechecks, scoped lint, eight API/event parsers, 112 changed-doc links, two Mermaid blocks and the regenerated 117-table ERD pass.

S2.2 public-receipt/output checkpoint: explicit requestVersion 2 now binds the URL target and cannot change protocol version on retry. SQLite v11 preserves legacy wire version 1 separately from the v2 digest algorithm. Strict portable receipt/manifest codecs, immutable original ACP response capture before asynchronous sealing, command-attributed event range manifests and the internal verified output reader are implemented. The common reducer validates original request/host/assignment/session/stream identity, including refusing legacy receipts for v2 commands. Historical reads page bounded original events and content objects without applying current-domain associations; an ingested frontier cannot hide a missing retained event. V2 pending spans survive host ACK pruning until terminal ACK; v2 receipts await explicit S2.11 retirement. A concurrent new turn cannot overwrite a v2 source command. S2.2 remains unchecked: the designed canonical command payload v2 is the next increment, followed by complete receipt/event agreement qualification. Production owner activation remains pending S2.12. GREEN: 78/78 web and 87/87 supervisor mandatory A/B cases on Node 24.15 (`s2-2-public-v2-ab-{web,supervisor}.json`), 7 portable/event codec cases (`s2-2-v2-codecs-final.json`), strict typechecks, scoped lint, eight API/event contracts, five adapter mirrors, nine doc links and two Mermaid blocks. The public-output RED is `s2-2-public-v2-red.json`; the successful end-to-end original-output/gap-refusal suite is `s2-2-full-output.json`. Validation exposed and fixed v2 admission masking legacy backpressure, a test using the wrong ACK API, and a success fixture whose independent 10 ms exit timer raced response capture; the success fixture now remains alive until teardown.

S2.2 complete: canonical command payload v2 now shares strict request/terminal validation with receipt v2, including original event ID, host stream UUID and sequence. Live publication and native restart recovery preserve identical nested outcomes; private terminal content uses verified immutable references without flattening the v2 value. Historical admission requires the original request digest and target session and settles only its bound command. RED reproduced the legacy canonical payload (`s2-2-command-event-v2-red.json`). GREEN: 80/80 web and 88/88 supervisor mandatory A/B cases on Node 24.15 (`s2-2-command-event-ab-{web,supervisor}.json`), including late receipt/event ordering, wrong digest/session rejection, private nested errors, original output reads and native restart. The focused lanes passed 22 host receipt cases, 12 output/codec cases and 11 immutable-command cases (`s2-2-command-event-host-v2.json`, `s2-2-command-event-content-web-v2.json`, `s2-2-v2-historical.json`). Strict typechecks, scoped lint, API/event contracts and documentation checks pass. The private-error fixture now uses the SDK's explicit RequestError wire contract; ordinary Error is converted to generic Internal error by the SDK. Production owner activation, recoverable unknown transport, binding fences and retirement remain subsequent S2 tasks.

S2.3 complete: lost ACK or unavailable receipt reads preserve an open command and its original handle, with separate unknown/reconciliation-required transport state. V2 dispatch rereads the frozen request; query/wait/startup share bounded receipt-read claims and the existing terminal reducer. Startup can replay a missing unsent request within budget under its original current assignment, while a crash after the final dispatch claim remains rearmable. The internal repair CAS compares ID/digest/attempts/budget and never resets history. HTTP request deadlines now cover partial success bodies. No new schema or public repair route is introduced. RED: real accepted prompt plus reset ACK/blocked reads previously threw admission failure (`s2-3-unknown-red.json`). GREEN: 86/86 mandatory web and 88/88 supervisor A/B cases on Node 24.15 (`s2-3-ab-web-final.json`, `s2-3-ab-supervisor-verified.json`), plus all 13 output/recovery cases on Node 24.19 (`s2-3-output-node24.19.json`). Both reset and stalled-body ACK cases kill a real recovery process with SIGKILL and start another process, retaining one accepted/terminal host event pair and pending owner application. Added unsent-claim, last-attempt/rearm, changed-caller-payload and concurrent repair cases pass. A neighboring receipt-first regression was corrected by retaining the accepted transition without terminal authority. Mandatory native runs exposed two pre-existing 10 ms fixture-exit races: lifecycle tests now explicitly release a SIGUSR2 exit barrier after prompt completion; object-sealing tests keep the fixture alive through receipt observation. Failed reports are preserved as `s2-3-ab-supervisor{,-final}.json`. Strict typechecks, scoped lint, eight API/event parsers, five adapter mirrors, 123 doc links and two Mermaid blocks pass. Production owner activation and create/ACP binding fences remain later S2 tasks.




S0 local commit: `61aec5b1`. Verified S1.1/S1.4/S1.5 checkpoint: `b29943a7`, committed at the user's explicit request during S1.2. S1.2 commit: `d349789d`. Verified partial S1.3 wallet/pipe checkpoint: `e600c195`; bounded ACK/pruning checkpoint: `580ce290`; SQLite physical guard/readiness checkpoint: `1c686f1f`; completed S1.3 file-capacity checkpoint: `52151646`; S1.6/S1.7 runtime/shutdown checkpoint: `f00416a2`; completed S1 gate: `2dcc33fc`. Subsequent local commits by phase remain authorized; no further checkpoint question is needed.
The implementation request authorizes this task list explicitly; the branch-named Stage B plan remains historical input. The audit directory is preserved.

S2.4 complete: live create ACK, recovered receipt and canonical lifecycle projection share one run/assignment/session/incarnation binding helper. Historical successful commands remain settled, while stale live callers receive assignment_fenced; independent ACP setters are removed and post-create scratch writes assert the persisted binding under the same transaction. Superseded incarnations retain their identities/evidence while releasing the active projection slot. RED reproduced the late-ACK overwrite and the old active-incarnation unique-index blockage (`s2-4-create-ack-red.json`, `s2-4-incarnation-red.json`). GREEN: 87/87 mandatory A/B web cases and 51/51 caller/parity cases on Node 24.15 (`s2-4-ab-web-final.json`, `s2-4-callers-final.json`), followed by all four deliverer cases with the final node-attempt/status and barrier-cleanup assertions (`s2-4-at08-final.json`). AT-08 reconciles the old receipt before releasing its live ACK after N+1 and checks both historical evidence and the untouched successor. Strict web types, scoped lint, nine changed-doc links and two Mermaid blocks pass. Qualification corrected fake-host receipt publication order and manual fixtures missing the authoritative run assignment pointer; the initial failed reports remain retained. No schema or host production change is introduced. The S2 release gate and owner activation remain open.

S2.5 complete: typed application-layer registries now supply owner adapters to query/wait and the durable owner worker. The existing command row owns the claim, 30-second renewable lease, retry/poison state and atomic application marker; full request-bound output verification precedes the bounded DB-only callback, whose final token/digest/lease CAS rolls back stale domain writes. Shutdown drains renewal and reports unconfirmed claim release as failure. Application failure retries are independent of canonical terminal evidence; host/DB unavailability consumes no application failure attempt. The default production registry remains inactive until domain migration and S2.12 activation. RED reproduced a terminal command remaining unapplied (`s2-5-owner-apply-red.json`); removing the final CAS was killed by the stale-commit test and the source was restored (`s2-5-owner-cas-mutation-red.json`). GREEN: 95/95 mandatory A/B web cases on Node 24.15 (`s2-5-ab-web-final.json`), including eight engine scenarios for exact output/marker application, rollback with competing workers, stale commit/failure, partial-output poison, actual lease renewal, unconfirmed shutdown cleanup and unavailable output storage. The fixtures test the engine transaction seam; production owner-entrypoint acceptance remains S2.6–S2.10. Initial poison classification exposed the base MaisterError constructor resetting subclass prototypes; both new domain error types now preserve their identity. Strict types, scoped lint and documentation validation pass. No new schema, remote protocol or workflow ledger is introduced.

## Roadmap Linkage

Milestone: **none**. No named A/B stabilization milestone was found in the current roadmap. This is a prerequisite to Stage C, linked to ADR-023/166/167 and the two existing A/B plans; it does not change the roadmap.

Research context: `.ai-factory/RESEARCH.md` is absent. Current code, accepted contracts, and the supplied audit are the evidence sources. Earlier memory was used only to locate the A/B boundary; current repository evidence governs this plan.

## Baseline and evidence rules

| Item | Verified planning state | Consequence |
| --- | --- | --- |
| HEAD | `33f9d34f4393f037c8ace935f3722ad449bac2f7` | Exactly the audit tip; no later checkout fixes invalidate its findings. |
| Local main | `5b458457a94b689713491d158e751553d676be6a` | Stage A lineage; inspected as well as HEAD. No network refresh or assumption about remote main. |
| Worktree changes before planning | Untracked `.ai-factory/reviews/` only | Retain these user inputs. |
| Latest ADR | HEAD: 167; main: 166 | Add an ADR only if a decision changes; allocate the next available number at implementation time after inspecting both histories. Amend implementation detail/status without rewriting immutable decisions. |
| Latest migration | HEAD: `0136_shiny_the_executioner`; main: `0130_execution_hosts` | Forward-only corrections after the combined lineage. Do not amend committed SQL. |
| Journal/snapshots | Entries 0130–0136 have matching snapshots | Implementation rechecks IDs, `prevId`, monotonic `when`, SQL hashes, schema and generated ERD. Existence alone is not semantic validation. |
| Deployment | `compose.yml` and production overlay contain **Postgres only**; no `compose.override.yml` exists | Web and supervisor stay host processes. Do not add application services merely to satisfy a stale wiring checklist. |
| Runtime | Planning shell is Node 26.3.0; installed Undici is 8.4.1; its engine floor is 22.19.0 | This shell cannot discharge Node 24 compatibility. R17's audit runs are historical evidence, not new results from this plan. |

Inputs read: root/web/docs instructions, applicable rules and architecture, the Stage A and B plans, ADR-023/166/167, execution-* analytics, affected source/tests, API/event specs and migration/deployment surfaces. Source line anchors below refer to the verified HEAD unless explicitly prefixed `main:`. The audit is `.ai-factory/reviews/stage-ab-execution-host-audit-2026-09-05.md`.

**Evidence terminology:** `source-confirmed` means the reachable control flow was checked during planning. `audit reproduction` means the supplied audit reports a run; it was not rerun here. Every acceptance test named below is **planned**, unless explicitly called an existing test. No checked box, file existence, aggregate pass count, or successful spec parser proves acceptance. No R01–R17 item is removed: all remain applicable; qualifications below narrow the mechanism, not the accepted requirement.

### As-built migration mapping

| Committed migration | Actual responsibility |
| --- | --- |
| `0130_execution_hosts` | Stage A host/assignment/command substrate. |
| `0131_foamy_venom` | Event streams/events/consumers/import lanes/incarnations; nullable prompt owner/request/application fields; compatibility mode. |
| `0132_soft_loa` | Command owner-shape and event source/protocol-bound CHECK constraints. |
| `0133_rich_blob` | Runtime-object catalog plus runtime-object command kinds/constraints. |
| `0134_lovely_tarot` | Scratch association preservation and destructive scratch mirror removal, with active/ambiguous association guards. |
| `0135_lush_jetstream` | Five-lane preservation preflight, canonical mode flip, removal of artifact projection cursors. |
| `0136_shiny_the_executioner` | Canonical-only mode default/constraint. |

`web/lib/db/migrate.ts` currently stages before **0134**, then attempts the remaining chain. The old Stage B plan's provisional object/cleanup numbers must not become operator instructions. S4 supplies explicit bounded stops before 0134 and 0135. New migrations after 0136 cannot supply prerequisites needed before 0134.

## Accepted boundary and delivery decisions

Keep the supported single host with two Node processes and Postgres. Repository/worktree/Git/check/promotion ownership stays where it is. Do not add host placement, multiple local hosts, host enrollment, public authentication, a relay, object storage, cross-host ACP resume, a second workflow engine, filesystem event authority, a scratch session mirror, or a permanent compatibility fallback.

The work is delivered as S0–S5. S1 and S2 repair the execution foundation; S3 repairs object lifecycle, security, integrity and retention; S4 makes the historical upgrade executable; S5 qualifies actual isolation/restart behavior. Bounded parallel edits are permitted only for disjoint files after contracts are frozen. One integrator owns shared schema, transport, host HTTP, deployment and documentation merges. Each increment must leave one-host operation working.

Existing useful mechanisms are retained: monotonic durable host streams, idempotent Postgres ingest, independent consumer cursors, assignment epochs, receipts, opaque object/workspace handles, manager authorization, and fail-loud cutover guards. Repairs must use these mechanisms rather than adding an alternate authority.

## Finding disposition and normative requirements

Owners: **E** = event/outbox and projection maintainer; **C** = command/reconciliation maintainer; **O** = object lifecycle maintainer; **M** = migration maintainer; **Q** = qualification/transport maintainer; **D** = specification/integration owner. These are responsibility assignments, not new runtime services. Each row is a MUST requirement, traced to an enforcement point and one primary discriminating test. Task dependencies are defined later.

| Finding / requirement | Current evidence and root cause | Minimal correction / enforcing mechanism | Owner; dependency | Primary test and acceptance |
| --- | --- | --- | --- | --- |
| **R01 / AB-01** Safe bounded ACP output (EVT-02/08/09) | `spawn.ts:296–320` → `registry.ts:78` → `runtime-event-publisher.ts:130` → `runtime-events.ts:163` synchronously throws. Spawn's nominal 1 MiB check is JS string length, while payload strings use UTF-8 bytes. Audit reproduced uncaught exception at 65,537 bytes. | Byte framing and bounded publisher boundary; preserve semantic/raw bytes in immutable command-bound objects; publish bounded summaries/references; explicit producer-local terminal error for nonrepresentable output. No catch-and-ignore. | E; S0 | **AT-01**, real child emits 65,536/65,537 and 1 MiB boundaries including multibyte text/tool results while a sibling works. Process survives; no sequence gap; full required bytes or typed incomplete-output failure. |
| **R02 / AB-02** Consistent outbox pressure (EVT-09/12) | `host-state.ts:1077,1288,1597`: admission counts unACKed, append counts retained. Audit: 948 ACKed 60-KiB events fill regular storage while admission stays open. No active producer pause. | One budget/accounting function and durable control credits; retained and unACKed row/byte limits; bounded pipe backpressure, hysteresis and terminal/control reserve. | E; AB-01 | **AT-02**, fill retained and unACKed partitions separately; refuse new effects before overflow, pause/resume existing producers, allow credited teardown, preserve every committed event. |
| **R03 / AB-03** Autonomous bounded projection (EVT-07/10, PRM-11) | `instrumentation.ts:95`, projector catch-up wrappers and `consumer.ts:333` run one batch or wake from new ingest; no due-retry worker. Cross-run gap promotion can miss a run's wake. | Durable worker over registered consumer/run pairs; boot backfill, fair claims, due timers and commit-driven hints; ACK independent of projection. | E; S0, AB-04 | **AT-03**, >250 events, terminal after event 200, process restart and no new ingest; transient last-event retry and two-run gap promotion all finish without stream completion. |
| **R04 / AB-04** Durable first-batch failure (EVT-10) | `events/projector.ts:84,222,265`: cursor creation rolls back with failed first apply; recorder requires absent row. Audit real-PG probe loses original error. | Commit cursor/claim before apply; savepoint failure recording or exact-claim/cursor CAS in separate failure transaction; no stale failure overwrite. | E; S0 | **AT-04**, absent cursor + deterministic/transient failure preserves original sanitized error, event ID, attempts, deadline/poison; a delayed recorder cannot poison an advanced cursor. |
| **R05 / AB-05** Durable prompt owners/request/output (PRM-02/04/11) | `schema.ts:2227`, `client.ts:461`, `ledger.ts:120`, `commands.ts:49`: declared owner/request/application fields unused. Scratch, gate chat, Flow, agent, sync and nested consensus/gate continuations depend on stack-local output/handles. | Typed durable owner subvariants; immutable private request; command-scoped output reference and terminal application under the owning row/fence. Reuse existing domain ledgers and driver entrypoints. | C; S1 | **AT-05**, parameterized owner matrix below: restart before terminal and after terminal before apply; exactly one domain application, no new prompt, original complete output. Every origin runs as its own case. |
| **R06 / AB-06** Single terminal reconciliation (PRM-03/06) | `recovery.ts:130,208,386` folds receipts directly and flattens nested errors; `prompt-projector.ts:219`/`deliverer.ts:794` then conflict. Existing V3 expects receipt-only failure. | One reducer used by live delivery, event projection, query/wait and recovery; receipt-first terminal is evidence-awaiting-canonical; exact versioned outcome identity. | C; AB-05 | **AT-06**, completed and nested-error receipt first; no terminal owner action until exact canonical agreement; disagreement poisons only the operation. |
| **R07 / AB-07** Unknown admission stays recoverable (PRM-02/03) | `deliverer.ts:464–477` writes failed after receipt lookup exhaustion; host may be executing. | Explicit transport unknown state separate from command evidence and application; retry same frozen request/ID; bounded attempts then durable rearmable reconciliation, never fabricated domain failure. | C; AB-06 | **AT-07**, host accepts, ACK/receipts/SSE partitioned beyond retries, ACP completes, reconnect: one ACP effect and correct owner application. |
| **R08 / AB-08** Fence every create-derived write (Stage A E-EH-11, EVT-05) | `deliverer.ts:353`, `create-ack.ts:24`, `active-run-session.ts:197` upsert without current assignment CAS. Independent ACP-handle writers at `active-run-session.ts:150–164`, agent/scratch/sync callers also require guarding. | Atomic historical evidence settlement separate from current binding application; authoritative run/assignment/logical-session lock and applied/stale result required by all post-create callers. | C; AB-06 | **AT-08**, delayed epoch N ACK and restart receipt fold after N+1 create: old evidence may settle, N+1 host/ACP session IDs and status remain unchanged. |
| **R09 / AB-09** Object intent/event/ACK convergence (OBJ-04/05) | `client.ts:694`; `runtime-object-projector.ts:168,232`: equality demands sealed size/hash while pending demands null. Audit event-before-ACK poisons. Released-assignment filtering also needs intent-aware historical treatment. | Separate immutable identity/declarations from sealed metadata; one monotonic object reducer for HTTP evidence, canonical event and reconciliation; no current-owner mutation from stale evidence. | O; S1, S2 reducer contracts | **AT-09**, all ACK/event orders, lost ACK, replay, release and supersession; valid intent becomes available once, conflicting metadata remains quarantined. |
| **R10 / AB-10** State-aware command/receipt retention (PRM-12) | `host-state.ts:765,1145` prunes accepted receipts before accepted-turn startup repair; `commands.ts:344` age-prunes terminal commands without application/ACK checks. | Durable eligibility exchange, exact terminal evidence, owner disposition, run/replay constraints and compact tombstones. Accepted/unknown/unapplied operations never age out. | C; AB-05/06/07/08 | **AT-10**, controlled time beyond seven days for accepted, unknown, unapplied and eligible operations; recovery/idempotency survive host+web restart. |
| **R11 / AB-11** Full resumable historical preservation (CUT-03/04/05/07/09) | Importer `:303,340,603,758` refuses ordinary logs and file locators, records zero imported objects. `main:supervisor/src/spawn.ts:166–178,333–336` creates normal logs; main default artifacts point to them. Existing import test removes `plan.log` before success. | Deterministic inventory/manifest and authorized host import, large-file chunks, real readback, locator/association CAS, five genuine lanes, guarded 0134–0136 cutover. | M; S3 | **AT-11**, full Stage A fixture, logs >25 MiB, uploads/evidence/file locators, multiple incarnations; interrupt/resume, run real migration and read history with web denied source access. |
| **R12 / AB-12** Safe untrusted downloads (OBJ-02/07) | Scratch accepts supplied MIME; web object content route `:93` delivers it inline without attachment/nosniff/sandbox. Source-confirmed; no exploit executed. | Central safe attachment header policy on direct/proxy content paths, safe filename encoding and existing response-data authorization. No active MIME inline. | O; S0 (can start independently) | **AT-12**, real authorized browser opens HTML/SVG artifacts; no script or authenticated side effect; positive authorized download and foreign/unauthorized denial. |
| **R13 / AB-13** Post-seal integrity and failure evidence (OBJ-04/07/12) | `supervisor/src/runtime-objects.ts:469`, `http-api.ts:1392–1427`, web resolver `:301` trust stored hash/header without validating current bytes/file identity. | Immutable seal; no-follow descriptor validation; full hash into bounded response spool before response; missing/corrupt host state + canonical event; exact descriptor served. | O; AB-09 | **AT-13**, same-size tamper, truncate, unlink, symlink/path swap and mutation during read; no successful obsolete-digest body; correct durable missing/corrupt transition. |
| **R14 / AB-14** Fair retention scans (OBJ-12) | `runtime-object-retention.ts:142–154` LIMIT 100 precedes reference/live-session eligibility, no progress cursor. | Indexed keyset/last-examined progress recorded for every examined row, claim/fence deletion and protected-reference predicate. | O; AB-09/10 | **AT-14**, >1 page protected at front, eligible later and transient deletion failures; bounded sweeps reach all eligible rows and preserve protected bytes. |
| **R15 / AB-15** Digest semantics match bytes (OBJ-06) | Host response `http-api.ts:1419` and integration test `:489` put whole-object SHA in Content-Digest for 206. | Content-Digest hashes actual body; Repr-Digest/ETag identify full verified object. Proxy validates each independently and actual body, with no transformation. | O; AB-13 | **AT-15**, independently hash actual 200/206 bytes, including nonzero range; slice digest differs from whole digest where appropriate. |
| **R16 / AB-16** Operation-scoped boundary and real lifecycle proof (CUT-09/11/12) | Inventory scans only app/lib `.ts` and authorizes entire filenames (`runtime-data-boundary-inventory.test.ts:12,70`); current E2E uses test supervisor and does not deny filesystem access. | AST/callsite inventory plus isolated real-process qualification; static guard is supplementary. | Q; S0 topology, S1–S4 final behavior | **AT-16**, inject prohibited operation into already allowed mixed file to fail guard; production web cannot read host root, yet lifecycle/history/object scenarios succeed through real HTTP/SSE. |
| **R17 / AB-17** Binary HTTP runtime compatibility (OBJ-05/06) | `supervisor-client.ts:13,1407,1458` mixes native fetch and Undici 8.4.1. Installed Undici initializes global dispatcher v2 and v1 wrapper. Audit V7b fails on Node 24.15 with `UND_ERR_INVALID_ARG: invalid content-length header`, passes unchanged on 24.19. | Use explicit pinned Undici stack/dispatcher for binary transfer with its matching types; validate local request before dispatch and classify local errors separately. Explicit supported Node floor and CI/image parity. | Q; S0; deliver early S1 | **AT-17**, existing V7b and streamed/chunked reserve/upload/read on Node 24.15, 24.19 and selected default image; minimum runtime and real host must pass. Node 24.19 alone is insufficient. |

## Normative contracts and state machines

### D1. Immutable requests, outcomes and command authority

Keep `execution_commands.payload` as the existing allowlisted diagnostic projection. Add a **server-private** immutable `request_canonical_json` TEXT on the same ledger, with version and SHA-256. Store the exact JCS UTF-8 string for `{requestVersion, command:{id,kind,issuedAt}, fence, target:{hostSessionId}, payload}`; include every effect-affecting field, array order and frozen content/object references. Normalize optional fields once before storage. `issuedAt` and all generated IDs are created once and reused; no clock/random input during retry. Parsed request → strict schema → digest comparison precedes every dispatch. The host uses the same digest schema, including the URL target (not only envelope payload), before replaying a receipt.

Prompt text in this private request is protected user data, never a generic command DTO, browser event, log or metric. Persist credential references only; never resolved API keys/env secrets. Do not replay from redacted `payload`, a hash alone, a mutable scratch message, a refreshed capability profile, or current configuration. Existing credential injection remains at its established trusted boundary. An immutable request containing an unsupported secret-bearing transport field must be refused before admission, not silently redacted into a different replay request.

For new prompts, owner reference, logical operation key, request schema/digest/body, target incarnation and accepted generation are mandatory together. Retain the existing unique `(run_id, logical_operation_key)` with the owner family/subvariant encoded in the namespaced key and request-content comparison before replay; the key includes durable owner generation/turn identity, not a freshly generated retry UUID. Lock the authoritative owner generation and look up its logical key **before** allocating command UUID/issuedAt. If present, reuse those stored fields and compare the normalized caller-controlled semantics against the immutable request; a concurrent unique-key winner follows this same lookup/comparison path. Only a genuinely new operation allocates generated fields. Same key/same canonical request reattaches; same key/different request returns `409 CONFLICT` / `command_invariant_conflict` without effect. An authorized new turn creates a new owner generation and key.

Add only operational reconciliation fields to this ledger: transport disposition, terminal evidence identity, application disposition/lease/backoff, retirement eligibility. Use typed CHECKs and scoped indexes. Exact physical column names are frozen in S0 before generation. This does not duplicate node/run workflow state.

Terminal result/error identity is a versioned canonical value, preserving nested `{code,message,details}` without flattening or string-based classification. Full semantic output is a command-bound immutable host object/manifest `{objectId,generation,sizeBytes,sha256,commandId,hostSessionId,acceptedSequence,terminalSequence}`. Terminal event/receipt contain matching bounded references; owner replay reads and verifies original bytes. Redacted event snippets and `stopReason` alone cannot reconstruct structured results, gate replies or consensus output.

The evidence and application states are separate:

| Dimension | Legal states / transitions | Authority |
| --- | --- | --- |
| Transport | `not_sent → dispatching → acknowledged` or `unknown`; transient attempts/backoff and `reconciliation_required` are recoverable operational states | Transport can record uncertainty; it cannot invent execution success/failure. |
| Command | Existing queued/delivering/accepted/succeeded/failed/fenced vocabulary retained; receipt-first terminal may remain accepted/delivering while awaiting canonical evidence | One terminal reducer consuming validated canonical evidence plus agreeing receipt; a definitive pre-effect refusal has explicit evidence. |
| Application | `pending → applying → applied` or `superseded`; transient retry / poisoned remain durable | Existing domain owner transaction under assignment/owner-generation fence; marker commits with domain writes. |
| Retirement | `retained → eligible → host_confirmed → tombstone` | Eligibility protocol, terminal ACK, application disposition, run state and replay grace. |

All producers of terminal evidence call **one reconciliation reducer**: live command deliverer, receipt recovery, prompt projector, `queryPrompt`, `waitPrompt`, startup/sweep. Query/wait may schedule reconciliation but never implement a second terminal writer. The projector must remain DB-only: receipt fetch/host I/O occurs outside its transaction and deposits validated evidence for a later reducer pass.

Receipt contract v2 includes command ID/kind, host key, run ID, assignment ID/epoch, URL-selected session identity, request schema/digest, phase, original result/error/reference, terminal event identity/sequence. Derive receipt bindings from the stored receipt/fence/session, not the querying client's body. Receipt absence/failed reads are not failure evidence. A canonical terminal with temporarily unavailable receipt waits/retries; verified matching durable receipt evidence already stored in Postgres remains valid after host retirement. Genuine disagreement quarantines the command and blocks owner application without changing a valid terminal outcome to another value.

Historical late evidence may settle the exactly matching old command. It must never update a successor's current session, cost, artifact association, node attempt, HITL, scratch dialog, gate reply or agent state. Maintain the narrow existing stale terminal exception; any extension for an already committed object intent must be explicit, generation-bound, historical-only and tested in S3.

### D2. Prompt owner and recovery windows

Persist the reference before remote dispatch in the same transaction as the owner admission. Use discriminated subvariants under the existing owner families where possible; widen the checked family only if necessary. Resolve references from authoritative rows. A Flow owner always references existing `node_attempts`, `gate_results` or consensus ledger rows; never create another Flow attempt ledger.

| Owner variant and key inputs | Current callers / durable authority to reuse | Recovery window and terminal application | Primary AT-05 case |
| --- | --- | --- | --- |
| Flow agent/judge/orchestrator node: attempt ID, session incarnation, prompt ordinal | `web/lib/flows/runner-agent.ts`, `flows/graph/runner-graph.ts`, `runs/resume-driver.ts`; `node_attempts` + run cursor | Active attempt while Running/NeedsInput; parked NeedsInputIdle remains attached until explicit resumed new command. Apply output/vars/decision, cursor transition and durable successor-readiness intent atomically via the existing graph reducer; its autonomous recovery schedules the existing driver even if the prompt application marker is already complete. | `owner-flow-node` |
| Flow permission resume: exact node attempt, HITL request, resume operation and incarnation | `web/lib/runs/resume-driver.ts:522–527`; stored HITL intent and node attempt | Ordinary restart reattaches its existing action command. Only an actual persisted checkpoint/resume decision creates a new prompt. Preserve exact attempt and permission-delivery evidence; never select an arbitrary open attempt after completion. | `owner-flow-permission-resume`, `owner-flow-restart-no-resume`, `owner-flow-orchestrator-wait` |
| Skill/AI gate: gate result ID, evaluation generation, attempt and gate key | `web/lib/flows/graph/gates-exec.ts:488–535`; `gate_results` | Resume unfinished evaluation even where caller supplied no nodeAttemptId. Persist verdict/evidence and gate completion under generation; do not rerun a paid check. | `owner-gate-skill`, `owner-gate-ai` |
| Consensus verification: node attempt, round, verifier, target | `web/lib/flows/graph/consensus/runtime.ts:401–438`; consensus round/evaluation rows | Apply exactly the intended matrix cell, wake existing consensus reducer; same attempt ID alone is not unique enough. | `owner-consensus-verify` |
| Consensus synthesis: attempt, round, synthesis generation | `web/lib/flows/graph/consensus/runtime.ts:658–697` | Apply synthesis result to its original round; never regenerate a prompt merely because the parent stack died. | `owner-consensus-synthesis` |
| Agent initial/resume/rework turn: run, durable turn generation, incarnation | `web/lib/agents/launch.ts`; runs, sessions, existing trigger/message records | Running/NeedsInput; parked NeedsInputIdle/WaitingOnChildren require their specific existing re-entry and capacity rules. Apply result/public-result contract and completion once, preserving launch snapshot. | `owner-agent-initial`, `owner-agent-persistent-first`, `owner-agent-resume`, `owner-agent-idle-message`, `owner-agent-rework` |
| Agent live message: persisted message/turn ID and incarnation | `sendAgentMessage` and `consumeAgentSession` in agent launch | Do not attach two distinct messages to one command. Apply the exact reply/terminal result and acknowledgment for that message after restart. | `owner-agent-message` |
| Consensus draft agent: child run + consensus round/participant generation | Agent session consumer/consensus draft path in `agents/launch.ts:3915–3921` | Record complete draft artifact and settle the existing child/result path; output lost from stack must not become an empty successful draft. | `owner-consensus-draft` |
| Scratch launch/message/recovery: dialog message/turn ID and generation | `scratch-runs/{service,events,recovery,dialog}.ts` | Scratch dialog Running with run/session still live is a due continuation, not a reason to skip. Persist reply and WaitingForUser once; NeedsInput and idle retain the exact owner until checkpoint/resume disposition. | `owner-scratch-initial`, `owner-scratch-message`, `owner-scratch-recovery` |
| Local-package/Studio assistant scratch: scratch turn plus postprocess action generation | `scratch-runs/service.ts` `postProcessFlowAssistantTurn`, local-package authority | Recover dialog completion and pending postprocessing independently. Revalidate local-package lock/session authority before existing publish/apply side effect; persist action intent/result idempotently. No new package behavior. | `owner-scratch-package-initial`, `owner-scratch-package-message`, `owner-scratch-package-lock-takeover` |
| Gate chat: thread/turn ID, lease generation, session | `services/gate-chat.ts:435,1181,1258`; `gate_chat_turns`/messages | Pending turn across lease expiry, parked/live session and NeedsInput/idle. Reconcile completed command before considering LEASE_EXPIRED. Persist the existing L3 `senseAndRestore` phase and finish it before releasing the pending-turn response fence; exact reply and completed marker then commit together under the existing HITL-first/turn lock order. | `owner-gate-chat` |
| Sync AI resolver: workspace lifecycle operation ID/attempt, resolver round, session | `runs/sync-resolver.ts`, sync target/lifecycle rows | Active resolver can be Running or NeedsInput; mechanical/post-ACP continuation can be parked in Review. Re-enter existing sync state machine with output and fence; Git operations remain local. | `owner-sync-resolver` |

For each row, S0 writes an exhaustive status × mode map using actual enums and existing predicates. Required refusal arms: owner missing, wrong run/project, unsupported phase, terminal/superseded generation, mismatched assignment/session, cancellation/checkpoint in progress, already applied with differing identity. A released/stale assignment permits historical command settlement and an explicit nonapplying disposition only; it does not implicitly authorize prompt-owner mutation. A parked owner that needs an old result must obtain its normal current domain recovery claim and persist an explicit source-command-to-current-application-generation handoff, with authority/cap/status checks, before consuming that immutable result. The old event reducer cannot perform that handoff or mutate current state. A new ACP turn requires a new command; a handoff of already completed evidence must not send ACP again. S0 reconciles this explicit handoff contract with ADR-167; add a decision record only if it changes the accepted authority rule, and test released-terminal recovery and superseded refusal separately.

Recovery priority: reconcile existing command evidence → apply owned terminal result → recover checkpoint/attempt boundary if evidence proves the old turn lost → dispatch a new logical prompt only under the normal re-entry claim. `run_kind` dispatch is exhaustive before entering Flow-only code. A `NeedsInputIdle` or `WaitingOnChildren` → live transition retains the existing cap lock/slot contract; terminal application emits the wake/domain event and releases/promotes capacity exactly as the existing domain operation requires.

Both live waiters and restart workers call the same owner application path. Commit a durable successor continuation/readiness predicate with the owner transaction and marker; recovery must service it even after the originating command is already applied. A post-commit microtask is only a hint. DB mutation and `completion_applied_at` commit together; further external effects use a durable owner sub-operation with their own completion marker and claim. Failed application retries do not replay ACP. Lease expiry alone is not evidence of a failed command.

### D3. Durable bounded projection and reconciliation workers

Use `execution_event_consumers` and existing claim fields; add an indexed `last_served_at`/claim generation only where missing. Register prompt, lifecycle, runtime-object, transcript, artifact and cost projection responsibilities explicitly: wire every current canonical consumer into its owning worker or document and test its equivalent autonomous existing job. Do not assume fixing the three one-shot wrappers proves all read models.

1. Ingest commits canonical rows and contiguous promotion in Postgres. In the same transaction, seed/wake consumer rows for **every distinct run promoted**, including runs released by filling another run's stream gap. ACK follows this commit, regardless of projector success. Duplicate ingest may hint; hints carry no authority.
2. Boot/restart performs bounded keyset backfill of missing registered consumer/run rows from canonical events. Persist the scan cursor. Cursor initialization commits separately from first application; no unbounded all-runs boot transaction.
3. Candidate predicate: registered consumer, nonpoison state, due `next_retry_at`, no live claim, and canonical accepted events beyond cursor. Order by `last_served_at NULLS FIRST`, due/creation time and stable run/consumer key. Claim with `FOR UPDATE SKIP LOCKED`; unique token and DB-clock expiry. Claim **at processing time**, up to two simultaneous workers; do not preclaim eight items then spend their leases waiting in a local queue.
4. One quantum is ≤100 events, ≤1 MiB aggregate payload and ~1 s soft work time; allow one legal maximum-envelope event so byte limit cannot starve it. Each DB apply transaction has a 5 s hard budget enforced by a monotonic client deadline, remaining-budget per-query statement/lock timeouts and explicit in-flight query cancellation. PostgreSQL 16 statement_timeout alone is not a transaction deadline. On deadline/shutdown await rollback before reusing the connection; if cancellation/rollback cannot be confirmed, discard that connection, retain the durable claim and surface service failure. Set a bounded idle-in-transaction timeout as a server backstop. Never commit after the deadline; token/cursor fencing remains mandatory. Test multiple slow statements and a blocked statement, not only one fast apply. Apply pure/domain DB writes and cursor under the same transaction and matching claim token/cursor. No HTTP, file hashing, prompt wait or Git inside that transaction.
5. Commit claim first. Inside apply use an event/batch savepoint; rollback failing effects without losing the outer consumer record. Commit original sanitized error, event, attempts and retry/poison fields. If failure recording is separate, CAS exact claim generation and starting cursor, and require failure sequence > current cursor; stale recorder is a no-op.
6. One quantum per pair then release claim/update `last_served_at`, yielding to other due pairs. Backlog remains due immediately. A 30 s lease exceeds the 5 s transaction budget; no external work runs under it. If an owner sub-operation needs longer, renew its separate 30 s lease at 10 s and fence every persistence boundary.
7. Use postcommit wake hints plus an abortable due timer (at most 1 s idle latency, or next retry deadline) and durable candidate selection. This schedules already committed DB work; it does not poll the runtime filesystem or infer state from a timer. A completed SSE stream or a new event is never required.
8. Deterministic protocol/identity failures poison immediately. Transient item failures retry after 1, 2, 4, 8 s; fifth failure poisons. Backoff helper cap is 300 s for explicitly rearmed extended policies, not a claim that a five-attempt run reaches five minutes. Preserve attempts per intent/rearm generation. Service/DB unavailability records service health and reconnects; it does not poison every event based on failed reads.
9. Poison never advances cursor or silently skips evidence. A bounded operator repair/rearm command must name consumer/run/event/expected cursor/error generation; it clears a repaired failure under CAS and logs audit metadata. Recovery remains visibly degraded until cleared.
10. Shutdown stops new claims, aborts transport waits, finishes/rolls back ≤5 s DB work, releases only owned tokens and closes pools/timers in order. SIGKILL needs no cleanup: after lease expiry another instance claims from durable cursor. Two web instances cannot double-apply or strand another run.

Use the same scheduling primitives for command evidence reconciliation and owner application, with separate predicates and fairness indexes; do not conflate their status sets with event consumers. A transport retry budget may enter `reconciliation_required`, but canonical evidence arriving later still wakes reconciliation automatically. Manual rearm is needed for another exhausted outbound dispatch cycle, never to apply already durable successful evidence.

### D4. One normative resource budget

This table supersedes inconsistent budget descriptions for this work. Binary units are bytes; row and byte ceilings are both enforced. Retain the accepted Stage B 400/512 MiB and row policy rather than tuning production numbers to small tests. S0 validates the footprint on the single-host target; any adjustment requires a stated measured resource reason and updates this one table and all consumers before implementation continues.

| Resource / setting | Normative default / rule | Rationale and enforcement |
| --- | --- | --- |
| ACP frame | 1 MiB **UTF-8**, including framing accounting | Existing intended limit, fixed from JS code-unit comparison. Streaming byte counter refuses before unbounded decode. |
| Canonical envelope / JSON | 1 MiB; depth 16; 256 keys/object; 1,024 array items; 64 KiB/string | Preserve negotiated contract; move required larger semantic bodies into host objects. |
| Producer buffering | ≤2 MiB raw pending bytes per producer, including partial frame and queued frame; ≤10 MiB whole-host output-buffer pool | One fair global permit allocator covers retained raw buffers, decoded strings, SDK handoff and publication queues; per-producer ceilings do not multiply the global pool. Charge actual buffer/string storage and bounded parser/object overhead, with heap-profile validation. Reserve before reading/decoding; at most one SDK frame queued per consumer. Spool incomplete frames under host quota or release/yield permits so partial frames cannot pin all capacity. This is an output-buffer budget, not a whole-process RSS claim. |
| Outbox resume low | 320 MiB / 64,000 regular rows | 80% of soft threshold gives hysteresis. Both retained and unACKed counters must be below low before admissions resume. |
| Outbox soft | 400 MiB / 80,000 regular rows | Refuse fresh mutating work, retain headroom for admitted output. Check retained **and** unACKed independently. |
| Outbox regular hard | 512 MiB / 100,000 regular rows | 112 MiB / 20,000 rows between soft and hard is drain headroom, not an outage-duration promise. Reserve capacity before append/effect. |
| Control/terminal partition | Separate 16 MiB / 1,024 rows; each credited event ≤16 KiB | Byte and row caps agree. Large terminal result/error uses an immutable reference. No ordinary producer may consume unallocated reserve. |
| Emergency floor | 1 MiB / 64 control rows | Kept outside producer wallets for host storage/degraded/stop evidence. |
| Producer terminal wallet | `(18 + declared outputBindingCount)` rows ×16 KiB; existing output-binding maximum 32 | Base lifecycle/teardown/error/overflow references plus one availability per declared output. Up to 50 rows/800 KiB; 960 nonemergency rows support at least 19 worst-case simultaneous producers. Admission uses actual wallets, not the six-run cap. |
| ACK replay grace | 24 hours after confirmed ACK | Never prune unACKed rows or shorten grace to hide capacity pressure. ACKed retained rows still consume real storage. |
| SQLite physical storage | 2 GiB host-state high-water; bounded WAL checkpoints at 64 MiB target, checked with SQLite page/WAL counters | Separate physical overhead from 528 MiB event payload quota; size includes receipts/indexes. Target checkpoint size is not a guaranteed hard limit while readers pin WAL. Refuse effects/pause before physical high-water; close bounded readers and checkpoint without deleting evidence. |
| Host runtime byte quota | 10 GiB hard / 8 GiB soft / 6 GiB resume-low; require ≥1 GiB filesystem free headroom | Separate host objects/raw spools from outbox. Finite host storage cannot promise unlimited output/outage retention; pressure pauses production or produces explicit incomplete-output terminal failure. Never GC required evidence for space. |
| Ordinary manager uploads | 25 MiB/object, 100 MiB/command batch | Preserve normal upload contract. Historical import uses bounded chunks, not this whole-file cap. |
| Read response/range | One range, ≤8 MiB; ≤2 concurrent verification scans, 64 KiB streaming hash buffer each; response spool ≤16 MiB total | Bounded memory/disk; large logs remain accessible through successive ranges. No implicit full-response fallback. |
| Projection | 2 concurrent claims; 100 events/1 MiB/~1 s quantum; 5 s transaction; 30 s lease; ≤1 s idle wake | Fair work and lease arithmetic from D3. |
| Historical import | 100 inventory entries/page, 2 concurrent files, ≤8 MiB chunks, 100 MiB transferred per bounded invocation | Resumable large logs without holding entire history in RAM; persisted keyset/offset after verified chunks. |
| GC | 100 examined rows/quantum, due-keyset cursor advanced for every examined item; 30 s operation lease, 10 s renewal when needed | Protected first pages cannot starve later rows; remote deletes occur outside DB lock. |

**Physical-capacity qualification:** logical SQLite quotas are not exact on-disk caps. Measure max envelope/index/receipt footprint and WAL behavior at configured producer concurrency. Before promising a physical maximum, account for the worst already-admitted burst and filesystem free-space guard; refuse configurations whose hard limits plus reserved in-flight storage exceed the host budget. Disk-full/fdatasync failure can prevent any final record: mark host unavailable, stop affected producers, retain all existing bytes/receipts, fail readiness and require storage repair; do not claim durable terminal evidence was committed when it was not.

**Producer protocol:** bounded byte framer → durable raw/semantic output sink → canonical summary/object registration → SQLite append → optional live hints/ACP delivery. Retain the currently blocked frame and pause upstream without assigning an event sequence until append commits. Respect backpressure from both log stream and ACP tap; wrap/replace the SDK stream adapter at the seam if its eager string/ReadableStream queue defeats the bound. An oversized frame is a typed producer protocol error with bounded preserved diagnostic bytes, never an uncaught process error.

ACP replies and notifications share stdout. `stdout.pause()` pauses both. Do not promise that terminal replies magically bypass a blocked pipe. Normal resume occurs after capacity frees below low; a credited checkpoint/delete can independently cancel deferreds and terminate the child, observe exit, and commit bounded terminal evidence. Turn-only cancellation remains pending while the pipe is blocked until adapter confirmation or an explicitly authorized checkpoint/delete; never silently escalate cancel into destructive teardown. Bulk deferred release uses a bounded summary, not unbounded per-permission reserve events.

Before terminal publication, a per-producer ordering barrier freezes normal appends, drains every already-captured frame into preserved bytes and wallet-funded references in order, or records exact incomplete-output evidence. Only then append prompt/session terminal; no later normal append from that incarnation is legal. The base wallet includes two pending-output/overflow references; if several frames were captured, seal them into one ordered segment with counts/offsets rather than emitting unbounded references. Test pressured stdout → checkpoint with a captured unsequenced frame and assert its representation precedes terminal. Wallet reservation, event spend and receipt transitions are transactional in SQLite. Return unused credits when producer completion is durable; actual committed reserve rows remain counted through ACK+grace. Ordinary control admissions use available regular capacity; while pressured allow only the serialized, credited teardown chain. Repeated command IDs replay rather than consume new credits. Exhausted credit refuses new effects; already promised wallets cannot be stolen by another producer. Preserve full semantic output in objects before success; object failure yields explicit required-output/evidence failure rather than an empty successful result.

Settings are validated at boot and exposed as bounded capability metadata where peers need limits. Planned env groups: `MAISTER_EVENT_OUTBOX_{LOW,SOFT,HARD}_BYTES`, corresponding `_ROWS`, `MAISTER_EVENT_OUTBOX_CONTROL_{BYTES,ROWS}`, `MAISTER_EVENT_ACK_GRACE_MS`, `MAISTER_EXECUTION_HOST_STATE_MAX_BYTES`, `MAISTER_RUNTIME_OBJECT_{LOW,SOFT,MAX}_BYTES`, `MAISTER_RUNTIME_MIN_FREE_BYTES`, and `MAISTER_PROJECTION_{CONCURRENCY,BATCH_ROWS,BATCH_BYTES,LEASE_MS}`. Fixed protocol bounds remain constants advertised by capability versions. Enforce low < soft < hard, wallet/reserve arithmetic, safe integers, bounded config sizes and deployment parity. Test-only reduced budgets are explicit fixtures of the same validated policy; they never alter defaults.

### D5. Runtime-object lifecycle, security and integrity

Use one reducer over immutable object identity `{objectId,generation,hostId,runId,kind,MIME,origin}` and separately sealed metadata `{sizeBytes,sha256}`. Origin is an explicit union: native command/output allocation with required assignment/epoch/session/command provenance, or authorized historical import `{importId,manifestHash,itemId}` without a fabricated active assignment. Any uniquely proven historical assignment is optional provenance. Import availability uses verified host import receipt plus deterministic `legacy_import` canonical evidence; it is not a fake native host event. The import adapter stores this proof using the existing 0131/0133 fields and import lanes/host manifest, so it works before forward schema additions. Pending rows may have null sealed fields; declared expected size/hash, when present, are immutable independent fields. Compare intent identity first, then establish sealed metadata once. Same metadata replay is idempotent; different metadata/generation conflicts without overwrite.

| State / evidence | Legal action | Refusal / recovery |
| --- | --- | --- |
| No intent | Reserve with durable command/owner intent | Unsolicited foreign object event is quarantined; never infer run/project from body. |
| Pending, upload not complete | Write exclusive private temp/chunks; persist upload progress | ACK loss queries exact object/command; retry same bytes/key. Changed declared bytes conflict. |
| Bytes sealed, event/receipt pending | Reconcile durable host seal to missing receipt/event | No duplicate file generation; manager catalog remains pending until matching canonical availability. |
| Available event before ACK | Fill sealed fields via object reducer | Valid pending identity succeeds; no impossible null-and-equal conjunction. |
| Available ACK before event | Persist matching evidence and wake reconciliation | ACK does not forge a canonical event; retry replay until event arrives. |
| Released/superseded assignment | Retain exact old intent's bytes/metadata as historical evidence | Never attach to current successor. Historical object evidence exception requires exact committed intent, not a general stale-event bypass. |
| Available, verified missing/corrupt | Host persists typed state and outbox evidence; manager projects state | No success response with stale digest. Read failure does not delete metadata or silently retry a different object. |
| Deleting | Durable delete intent/tombstone precedes unlink | New references denied under same object/generation lock; unlink/recovery retries same operation. |
| Deleted | Keep catalog and idempotency tombstone | Replayed available cannot resurrect; identical delete succeeds; read reports typed gone. |

**Seal and read:** flush/close producer writable handles; seal to a distinct private immutable inode by tmp+rename and record identity. Never label the actively appended log inode immutable; seal segments and a manifest with ordering/total length for continued logs and full prompt output. Preserve structured result transport (`MAISTER_OUTPUT_FILE`/sentinel and `node_attempts.vars`) separately from required verifier evidence (`MAISTER_ARTIFACT_DIR` and typed evidence associations).

For each read, open with no-follow semantics, validate regular-file/device/inode/size and initial stat, stream the entire representation through SHA-256 while copying only the selected ≤8 MiB response bytes to an exclusive 0600 private spool. Compare total size/hash and final stat before sending success headers. Serve the exact spool descriptor, unlinking its path while open where supported; do not verify then reopen the original path. This adds bounded disk I/O to catch same-size tampering and avoids a hash-then-mutate race on returned bytes. Cap scans/spools; use typed busy/retry responses. Large multipart historical representations hash ordered sealed chunks; no whole-file RAM buffer.

A segmented log/history is exposed as **one ordinary catalog object and existing execution-object locator**, with the full logical size and SHA-256 of concatenated original bytes. Segment order/offset/length/hash descriptors are host-private. Range reads cross segments transparently; Repr-Digest hashes original file bytes, never the descriptor or concatenated segment hashes. No new unresolved manifest locator leaks to artifact/attachment consumers.

On ENOENT return a typed missing outcome; on nonregular/symlink/identity/digest mismatch return corrupt. Persist host state/outbox evidence before claiming that transition; if host persistence fails, return storage-unavailable and record service degradation, not a falsely durable missing/corrupt state. DB application follows canonical evidence without blocking the HTTP error response on projection. Manager validates representation identity and hashes actual response bytes in its bounded spool before forwarding success; a corrupt peer stream is never labeled verified solely because its headers agree.

**Download policy:** untrusted arbitrary object content is `application/octet-stream`, `Content-Disposition: attachment` with sanitized ASCII filename and RFC-compatible encoded filename, `X-Content-Type-Options: nosniff`, `Content-Security-Policy: sandbox; default-src 'none'`, `Cache-Control: private, no-store`. Apply to 200/206 on host and manager proxy and to artifact payload routes that expose the same content. Never inline HTML, SVG, XML, script, PDF or supplied MIME. Existing UI text previews may fetch and render escaped text; if a passive image preview is necessary, use an explicit independently validated passive MIME path, not a generic inline-MIME switch. S3 must inspect consumers so the download change does not break ordinary artifact use.

**Digest contract:** for 200 the body digest and representation digest agree when bytes are identical; for 206 Content-Digest covers the transferred slice, Repr-Digest the full verified object; a strong ETag binds generation/hash. No compression/transcoding on this route. Invalid/multiple/out-of-range requests return 416 with typed range error, without full-body fallback. See [RFC 9530 §2–3 and Appendix B.3](https://www.rfc-editor.org/rfc/rfc9530.html#appendix-B.3). Tests hash returned bytes independently; header equality is only a metadata check.

**Retention:** expired is eligibility, not deletion authority. Preserve live/checkpointed session input/output, unresolved command requests/results, unapplied owners, pending import proofs, required verifier evidence, artifact/result references, and delivery holds. Use the existing confirmed local-delivery or PR-merge policy and persisted delivery snapshot/confirmation; `Done` or an open/closed-unmerged PR alone is insufficient. Result-only delivery requires its actual acknowledgment policy, not a fabricated merge. When no authoritative delivery confirmation exists, retain with an actionable protected reason. Optional operational diagnostics can follow their explicitly weaker policy; do not reuse it for required evidence.

GC advances a keyset/last-examined marker even for protected rows. New reference creation and delete eligibility use the same object/generation lock so a reference cannot appear after destructive claim. Recheck eligibility/fence before remote effect; store deleting command identity and retry state before dispatch. Fairness applies to deleting/missing/corrupt cleanup queues too. Catalog/tombstones outlive missing/deleted payloads.

### D6. Command retirement and bounded retry policy

Add an idempotent host-admin retirement-eligibility operation through ExecutionHosts, keyed by command ID plus request/outcome digest and eligibility generation. It conveys proof metadata, not a client assertion that time elapsed. Manager derives eligibility under command/owner/run locks; host checks its receipt phase, terminal event's ACK watermark and stored request/outcome identity. Record the host acknowledgment durably before either side removes recoverable evidence.

Eligibility requires: confirmed terminal receipt/event agreement; terminal event ACKed; owner applied or explicitly superseded with no remaining obligations; run terminal under the command-retention predicate; no retained object/request/result/continuation dependency; replay grace elapsed. Application success and superseded disposition are distinguishable. Live/accepted/unknown/poisoned/unapplied commands remain retained regardless of age. `turn_lost` startup repair runs before any pruning and preserves the original command ID.

After eligibility, compact to a small tombstone containing identity/digests/outcome disposition, not an executable request. Retain tombstones while a retry can be legal for that binding; once an assignment/host fence is durably beyond it, stale requests are fenced before side effects. Do not claim infinite idempotency from a finite age TTL. If tombstone bounds cannot be met, refuse further admission and surface retention pressure; never silently forget a still-valid key.

Guard run/assignment deletion and inbound FK cascades as part of this protocol: `execution_commands.run_id` and assignment references currently cascade. Refuse hard deletion with a typed protected-evidence conflict while protected commands/objects/import proofs exist. Parent deletion can proceed only after the ordinary retirement protocol has discharged every hold; adding an archive identity is outside this correction. Do not permit cascading around retirement eligibility. Add a real run-delete versus terminal-unapplied-command race. Host failure or eligibility ACK loss leaves manager recovery evidence intact. Retry the same eligibility operation. A late agreeing success after owner supersession can settle historical evidence and become eligible later; it does not apply to the successor. A missing receipt for a still-retained command is explicit reconciliation work, not an endless wait without diagnostics and not a fabricated terminal failure.

### D7. Identifier classification and refusal contract

Classification vocabulary: **URL-selected** = `url-param`; **principal-derived** = `auth-context`; **server-derived** = `server-state`; **body-supplied** = `body-controlled`. Internal header fields are body-supplied for trust analysis. A valid UUID/regex never proves a cross-resource relationship.

| Route/message affected | Consumed identifiers and authoritative derivation | Refusal / authorization |
| --- | --- | --- |
| `POST /sessions` | Body command/fence/workspace/profile/instruction/output object IDs; host key from private store, run/assignment from accepted fence, workspace/object bindings from registry | Validate full cross-binding before create. No raw runtime path added. Retain existing adopt-only repository path boundary. |
| `POST /sessions/{id}/prompts` | URL session ID; body command/fence/content object IDs; derive run/step/project and incarnation from session registry, compare fence/objects; owner remains manager-derived | Wrong run/session/object generation → 409 typed mismatch, no ACP call; wrong host/fence → existing fenced refusal. |
| `POST /sessions/{id}/input`, `/cancel`, `/checkpoint`; `DELETE /sessions/{id}` | URL session; body command/fence/request ID or cancel target; derive run/step/project from registry and validate pending deferred ownership | Repeated identical decision replays; different decision/key conflicts; explicit missing deferred proven by authoritative receipt/session state uses the domain's existing gone disposition. Failed reads are retryable. |
| `GET /commands/{commandId}` and proposed `POST /commands/{commandId}/retirement` | URL command; receipt binding server-derived; retirement body contains expected digest/evidence generation, compared to host receipt/ACK | Existing loopback trusted-admin transport only; this is not remote enrollment/auth. Wrong identity →409; not eligible →409; absent retained evidence → typed reconciliation condition. |
| `GET /runtime-events` and `POST /runtime-events/ack` | Cursor/stream supplied by request; selected transport and health derive host; stream identity/private high-water server-derived | Stream mismatch or ACK beyond proven frontier refused. ACK never depends on projection success. |
| Host canonical event | Body envelope IDs are untrusted peer input; selected transport derives host, Postgres joins derive run/project/assignment/session/command/object ownership | Exact dedup/content checks; stale audit retention; only documented historical exceptions can settle old records. Unknown type/schema/identity quarantined. |
| `POST /runtime-objects` | Body object/command/fence and owner declarations; host derives run/assignment and validates immutable object scope | Reservation conflict/foreign reference →409; pressure →503 before effect. |
| `PUT /runtime-objects/{objectId}/content` | URL object; command/assignment/epoch/generation in headers; metadata derives run/host/MIME/expected content | Reject size/digest/header errors before effect where possible; partial upload remains intent-scoped and retryable; foreign generation →409. |
| Host `GET /runtime-objects/{objectId}` and `/content`; `DELETE /runtime-objects/{objectId}` | URL object; range body/query/header values validated; registry derives binding/path; delete command fence checked against authorized object operation | Read uses private host path only internally; typed missing/gone/corrupt/range outcomes. Deletion requires current object eligibility/operation generation, not a fabricated active run assignment. |
| Web `GET /api/runs/{runId}/runtime-objects` and `/{objectId}/content` | URL run/object; user ID principal-derived; project/host/assignment/object and filename from joined rows | Gate by response data class: repository-derived raw bytes require `readRepoFiles`; only safe redacted list metadata may use readBoard. Foreign object is not disclosed. Include positive allowed tests. |
| Web artifact payload route | URL run/artifact; artifact locator and project server-derived, execution object derived through locator | Same raw-content gate and headers as direct object read; no path/body bypass. |
| Scratch/agent message and gate-chat entry routes/services | URL parent resources, principal actor; body message/content/user idempotency token; owner ID/generation derived/created under owning DB row lock | Persist original content and owner intent before remote effects. Do not accept arbitrary ownerKind/ref or cross-project IDs. User retry token is scoped to principal+owner. |
| Historical import administrative path | Operator selects the maintenance Unix socket locally; URL import/item/chunk IDs and body generation, manifest digest, expected size/hash and opaque destination IDs are untrusted inputs compared to the registered manifest; host identity is server-derived | Maintenance socket only, protected by OS ownership/permissions and an enabled frozen import session. No TCP import route or arbitrary source-path field. Administrative CLI derives affected runs/projects/associations from DB; old run need not have an active assignment. |

Failure classification is explicit and shared by consumers:

| Observation | HTTP/actionable result | Durable effect / retry rule |
| --- | --- | --- |
| Invalid local header/body/schema before dispatch, including diagnosed Undici construction error | Typed `ACP_PROTOCOL` or `CONFIG`, internal 500; safe reason `transport_request_invalid` | `not_sent` evidence; no receipt lookup retry as if a remote effect occurred. Correct construction then dispatch original intent. |
| Definitive host 4xx refusal before effect with agreeing identity | 409 for conflict/fence, 413 size, 416 range, existing 400 precondition shape as applicable | Persist refusal evidence; no owner success. Fenced drivers yield without any current-domain writes. |
| Host 503 explicitly proving nonadmission/pressure | 503 with retryability/reason | Retain intent; bounded same-operation policy. A new logical attempt requires explicit owner decision, never automatic fresh UUID merely to bypass dedup. |
| Network/timeout/non-JSON 5xx or parsed error whose effect is unknown | 503 `EXECUTOR_UNAVAILABLE`, command handle + `admission_unknown` where caller supports durable acceptance | Preserve unknown/retry state and original request/key; no Failed/Crashed solely from transport. Existing 202 is reserved for proven host admission. |
| Receipt or canonical terminal missing/unavailable | Queryable pending/reconciliation-required state | Retry evidence retrieval; operator diagnostic after budget; incoming durable evidence can still complete automatically. |
| Receipt/event/request identity disagree | 409/typed `ACP_PROTOCOL` reason `prompt_terminal_conflict` | Quarantine operation; no terminal rewrite, no owner application; operator repair/rearm with exact generation. |
| Verified missing/corrupt/tombstoned object | Existing mapped typed missing, 422 corrupt, 410 deleted; map exact reason/status in OpenAPI before implementation | Preserve catalog, host failure evidence and retention; never substitute empty content. |
| Local DB/service unavailable | 503/service failure | Roll back current DB work, release/expire only owned claim; no inferred terminal state. |

Preserve `MaisterError`/`SupervisorError` conventions. Add a strict reason union and new codes only if existing codes cannot express an actionable domain distinction. Update code-to-HTTP and EN/RU error presentation together; never branch on message text. Document bounded safe diagnostics with request IDs/operation kind/status/cause code, not the prohibited request body.

### D8. Remote-effect / database failure tables

These tables are normative for every changed distributed transition. Network work is outside Postgres transactions. Every operation has an intent before dispatch and an application marker after evidence, with a CAS that checks both owner status and exact generation. Do not describe this as atomic two-database commit.

| Boundary / crash point | Persisted state | Retry owner and reconciliation | Compensation / poison / delayed success |
| --- | --- | --- | --- |
| Before intent commit | No admitted operation | Caller may retry the same user request | No remote call permitted; validation refuses cleanly. |
| Intent committed, before dispatch | Frozen request/key, owner ref, pending marker | Command/owner worker claims same ID | Cancel untouched intent only under owner lock; no fake host outcome. |
| Dispatch sent, no receipt/effect known | Unknown transport, attempt timestamp | Deliverer then durable evidence worker, same request | No rollback of possible effect; partition beyond budget remains recoverable. |
| Host accepted receipt committed before effect | Accepted command and terminal wallet | Host executes once or records `turn_lost` on restart without live action | Never replay ACP merely because in-memory promise disappeared. |
| Host effect/seal/tombstone exists, receipt/event missing | Host intent and effect identity | Host startup repairs receipt/event from exact private state; manager queries original ID | Orphan effect is retained/claimed for forward settlement; conflicting evidence poisons. |
| Host effect complete, HTTP ACK lost | Receipt/event/outbox durable; manager may remain unknown | Same-ID retry/query returns agreeing evidence | No new effect/key; source bytes and references stay held. |
| Canonical event before HTTP ACK | Canonical DB evidence plus original intent | Prompt/object reducers fold independently | Valid event never poisons because ACK has not populated nullable fields. |
| Ingest commit before ACK or projection | Canonical rows and watermark durable | Stream reconnect replays; due worker applies without new events | Duplicate no-op; no host prune until confirmed ACK+grace. |
| Terminal evidence before owner DB application | Complete evidence and pending owner marker | Owner worker reconstructs exact output; claims existing domain generation | No ACP retry; interrupted apply transaction rolls back both domain state and marker. |
| DB application before downstream local effect/wake | Domain state plus durable pending action/wake | Existing domain driver/sub-operation performs effect using same generation/key | Pure hints may be lost; durable query rediscovers work. External success then DB failure settles forward. |
| Assignment/owner superseded during remote effect | Old operation retained, new generation authoritative | Historical reducer settles old evidence; current-state CAS refuses | Delayed success cannot overwrite binding/output/current owner; teardown only exact orphan via authorized current cleanup fence. |
| Retry budget exhausted / deterministic poison | Durable reason, attempts, next/rearm generation | Autonomous incoming-evidence apply; explicit repair/rearm for another outbound budget | Never delete source/evidence, skip poison event, or claim execution failed from failed reads. |

Per-effect instantiation makes the above concrete:

| Effect family | Intent and idempotency key | Host receipt/effect/canonical evidence | DB application, stale handling, compensation |
| --- | --- | --- | --- |
| Workspace adopt/release (existing Stage A boundary only) | Existing command+assignment, immutable workspace request | Existing workspace registry handle/released marker + receipt | Apply handle only to owning assignment. Late old adoption becomes historical/orphan cleanup; never overwrite successor or transfer Git ownership. Release cannot be compensated by resurrecting a handle. |
| Session create/resume | Command/request + expected logical session/incarnation + assignment | Created host session, receipt and lifecycle evidence | `applyCreateAck` and ACP-ID writers share assignment/session CAS. Historical success settles command; a new cleanup command names the exact old session after authoritative orphan check. No broad delete-by-run compensation. |
| Prompt | Owner admission + exact request/command + terminal output wallet | Accepted receipt/event → one ACP call → terminal output object + matching receipt/event | One reducer then owner transaction. Unknown holds capacity according to actual domain state; cancellation/supersession blocks current apply. Lost caller never redispatches under a new ID. |
| Permission input | HITL response intent with unmarked delivery, command/deferred identity | Exact decision replay or explicit cancellation/rejection, receipt | Mark responded/delivered only after evidence, under same HITL/generation. Persistence failure on deferred-creation path invokes explicit cancel/reject release; if unreachable persist release intent and retry. |
| Cancel/checkpoint/delete | Existing command, target session and original prompt ID, owner stop intent | Serialized credited teardown; cancel deferreds; checkpoint/observed child exit; terminal prompt/session evidence | Keep checkpoint/resume and run status coherent. Repeated identical command joins/replays. Turn-only cancellation cannot claim child termination. No released/superseded owner overwrite. |
| Object reserve/upload/seal | Pending catalog + request digest/generation + exact byte identity | Temp/chunk progress → flush/verify/rename → immutable registry → receipt/available event | Object reducer separates intent from seal. Unknown keeps bytes; after supersession retain historical object but do not associate to successor. Only private unsealed temp may be removed on failed write. |
| Object delete/GC | Locked eligible object, deleting intent and command/operation generation | Durable tombstone → unlink → deleted evidence/receipt | Metadata preserved. Unlink failure retries same operation; ACK loss queries tombstone. Late available never resurrects. No attempt to undo an unlink. |
| Object read-detected missing/corrupt | Existing immutable registry generation + read validation context | Host commits detected failure/outbox; response is independent of projection | Manager applies only exact old generation, retains associations with typed unavailable outcome. Unexpected persistence failure yields storage unavailable. |
| Historical import chunk/seal | Authorized import session/manifest/source fingerprint/chunk index + destination ID | Host chunk receipt/size/hash progress → full-object verify/seal + available evidence | DB verifies remote readback before locator CAS/lane proof. Copy success+DB crash is adopted by same manifest ID; source changes poison lane, never delete source to pass. |
| Retirement eligibility | Manager command/outcome/owner/ACK proof + eligibility generation | Host verifies terminal phase/ACK and stores eligibility receipt | Manager retains evidence until host confirmation; compact only afterward. Lost ACK repeats exact eligibility request; late terminal for superseded owner remains historical. |
| Existing sync/package assistant postprocessing | Durable owner action ID + expected lifecycle/package lock generation | Existing local operation outcome/marker; no new remote Git interface | Restart re-enters existing reducer; exact content result reapplies idempotently. Success beyond irreversible point settles forward. This plan does not move or redesign workspace/Git effects. |

Deferred inventory must cover ACP permission promises, prompt wait subscriptions, output stream backpressure waits, retry timers, HTTP body streams and worker claim timers. Each resolve/cancel/checkpoint/delete/fence/shutdown/DB-failure path must release or durably hand off responsibility. A process-local wait aborted by request disconnect does not cancel the durable command. Tests force releasing-side persistence/transport failure and assert explicit release at the real supervisor or a durable retry intent plus eventual release; logging alone is not a release.

### D9. Historical import and guarded upgrade protocol

The operator procedure is bounded and executable after S4. Normal web startup never mounts/reads the old runtime root. Choose a **maintenance-only HTTP listener on a local Unix socket inside the existing supervisor process**, reached by the operator CLI through an `ExecutionHostsAdmin` transport adapter. This adds no daemon, public authentication or TCP import route. The socket lives in an operator-owned 0700 directory with 0600 socket permissions; deployment must keep the normal web process outside that OS authority and qualification proves it cannot connect. The CLI temporarily reads approved historical sources. The supervisor loads an operator-selected frozen manifest locally, enables its exact import ID/generation/digest, and accepts only corresponding opaque item/chunk identities. Source paths never enter protocol bodies, logs or ordinary web services. Shutdown disables admission; restart requires explicit re-enablement of the same manifest and resumes its durable ledger.

The administrative protocol consists of `GET /imports/{importId}` for bounded progress, `PUT /imports/{importId}/items/{itemId}/chunks/{chunkIndex}` for an exact length/hash-bound chunk, `POST /imports/{importId}/items/{itemId}/seal`, and `DELETE /imports/{importId}/admission` to revoke further writes while retaining proof. Each request carries the enabled import generation and manifest digest; the host compares every item, offset, destination identity and declared size/hash with its manifest. Seal returns the ordinary object identity/full-representation digest and a durable import receipt. Same chunk/seal identity replays; conflicting bytes or generation returns a typed conflict. Readback uses the existing bounded object-read contract through the administrative adapter. A revoked generation cannot be reopened by an HTTP request. Use bounded request deadlines and same-ID reconciliation on timeout; none of these calls create an active assignment or bypass normal session authorization.

**Pre-cutover persistence:** use existing `execution_data_plane_imports` from 0131 for all five lanes and bounded progress. `last_source_position` carries a strict versioned compact cursor including manifest hash/position; `source_fingerprint` is the full lane manifest digest. A supervisor-private SQLite import session/item/chunk ledger records detailed manifests and byte progress. It is operational import state, not another command or Flow ledger. New Postgres constraints/columns remain forward migrations after 0136; the pre-0134 importer must work without them.

**Inventory and deterministic mapping:**

| Source class at Stage A main | Discovery / mapping | Destination and proof |
| --- | --- | --- |
| `run.events.jsonl` | Incremental byte/line offsets; sessionName/nodeAttemptId/command/fence attribution from line and authoritative rows | Deterministic imported event IDs, full event count/fingerprint plus the complete original file as a raw-transcript object; canonical ordering/provenance without synthesizing native host live events. |
| `cost.jsonl` | Preserve session/run/step/attempt identity, token/cost facts and source positions | Canonical cost facts + rollup equality and the complete original file preserved as a cost-diagnostic object. Normalized facts alone are not byte-preservation proof. |
| `<step>.log` and nested raw logs | Actual main workspace registry/spawn producer; default file artifact locators are rooted under legacy run directory, not worktree | Immutable log segments/manifest, full bytes/read ranges, equivalent artifact locator and transcript association. Empty/large ordinary logs are valid. |
| Scratch `uploads/<scope>/<safeName>` and attachments | Persisted `storagePath`, declared MIME/name/size/hash; realpath confined to approved legacy root | Object-backed attachment preserving message/scope association and bytes. No guessed filename-only matching. |
| File artifact/evidence locators | Derive from artifact's original run root, kind, node attempt, evidence role and locator; include files discovered recursively | Exact object locator, validity/producer/required-evidence associations retained; any missing required payload blocks lane. |
| Session/incarnation/checkpoint metadata | DB sessions/assignments/ACP IDs, receipts and event provenance; preserve `session.json`/checkpoint files **if discovered** | Canonical historical associations plus preserved metadata bytes. No current main producer was found for those two filenames; do not fabricate them as routine outputs. |
| Transcript and manager-owned inline/gate/HITL artifacts | Existing durable DB content and deterministic event/log reconstruction | Preserve manager authority; compare exact message/role/order/content and associations. Do not move manager-authored state to host objects just because it is JSON. |
| Unknown/duplicate/discovered source | Classify each file and reference, including duplicate references to one file | Same bytes may share content chunks; distinct logical source/association IDs remain. Unclassified or conflicting source blocks completion. |

Item identity is a deterministic digest of manifest version, frozen source identity, run/association identity, relative-path identity digest, byte size and SHA-256. Raw paths remain only in the host-private manifest/operator source map; manager proof carries opaque IDs and hashes. Include exact old row fingerprints/locator values in the protected preservation record so resumed CAS can prove it is replacing the same association. Hash source before and after copy; open no-follow where appropriate and compare file identity; a mutable/changed source invalidates the manifest and lane. Never silently switch to the new bytes under the same key.

The host import path accepts a registered manifest, file/chunk index and declared content identity. Copy streams in ≤8 MiB chunks, stores offsets and hashes durably, verifies complete ordered file/segment manifest, and seals through the same runtime-object lifecycle. Logs larger than 25 MiB are segmented/chunked and remain fully readable as a typed historical representation. Do not impose the ordinary whole-upload limit on existing log history. Retry at chunk boundaries, distinguish an identical duplicate from a conflicting chunk, and never mark uploaded just because bytes were sent.

**Procedure and crash-safe checkpoints:**

1. Take a preliminary Postgres/host/source/adapter backup before maintenance. The rollback anchor is a **second, final coordinated snapshot after the drain/source freeze in step 2**, covering Postgres, host identity/SQLite/outbox/receipts/object roots, legacy runtime and relevant adapter journals. Record backup digests, software revisions and migration high-water. Prove restore to disposable storage before cutover. Do not restore only one side of a split checkpoint.
2. Enable maintenance/drain using the existing admission boundary plus an explicit upgrade maintenance fence. Stop scheduler launches, resumes, new scratch/gate/agent turns, destructive GC and package postprocessing that can mutate inventoried sources. Drain active work or checkpoint/stop through accepted domain controls; `Review`, `NeedsInput*`, waiting children and active sync/package claims require explicit treatment. Satisfy the unchanged 0134 active-legacy status guard; do not merely relabel live runs terminal. Stop old web binaries before source freeze. Capture final associations, close writers and fsync/freeze source bytes. Now create the final coordinated backup set from step 1 and restore-test that exact post-drain set before proceeding.
3. Run the staged migrator through **0133 only**, using the existing filtered migration-root technique and verified journal hashes. Install/start the stabilization supervisor in restricted import mode. Pre-0134 manager CLI uses the compatibility projection of existing schema, not runtime services requiring forward columns.
4. Inventory all legacy runs, all five lanes, every source file/reference and owner association with paged scanning. Register the immutable host manifest; commit lane `state=pending` with manifest digest and `phase=inventory|copy|verify` in the versioned cursor (the pre-0134 state enum is `pending|complete|missing|failed`). No missing source counts as an empty lane. A genuinely absent lane gets an explicit inspected-empty proof with zero expected items, not a blanket zero.
5. Copy chunks through the authorized import path; host verifies/seals, manager performs real object metadata and byte readback and hash comparison. Persist progress after each committed chunk/item/page. A stopped invocation exits with resumable position and bounded unresolved counts; retry uses the same import session and manifest.
6. In bounded DB transactions, replace locators/attachment references and historical session associations only after verified destination bytes; compare exact original row fingerprint and owner identity. Commit source→object association proof with the locator change. Do not discard artifact validity, kind, nodeAttemptId, message association, or delivery hold. A completed lane revalidates its full fingerprint on rerun.
7. Scratch with one provable assignment follows 0134's existing preservation. For a nonnull mirror with multiple historical assignments, derive a **unique exact session/command/event binding**, preserve the original mirror and row digests in the durable manifest, materialize matching canonical session/incarnation rows, verify the round-trip, then CAS-clear only that proven migrated mirror before unmodified 0134. Never delete assignments or clear an unproven mirror. Truly ambiguous/conflicting histories refuse with remediation; preserve sources/backup for explicit source mapping repair. A user-supplied mapping must be evidence-bound and audited, not a force-success flag.
8. Apply **0134 only** with all its original guards. Reverify canonical associations against the **original** frozen manifest afterward; 0134 may rewrite the scratch lane to a no-mirror proof, which alone is insufficient. Restore/complete the lane's verified original source counts and association fingerprint through the importer before proceeding.
9. Verify all five lanes: `events`, `transcript`, `cost`, `runtime_objects`, `scratch_session`; compare expected/imported counts and bytes, hashes, metadata, locators, association multiplicities and no unresolved items. Verify backups and source freeze again. Persist complete only when every lane's real proof holds. Keep 0135's existing five-lane preflight intact; add stronger orchestrator checks, never relax its SQL to accommodate broken input.
10. Run unmodified **0135–0136**, then apply the new forward correction migrations. Enforce an application/schema capability gate that refuses old writer binaries after cutover; use a DB backstop for newly mandatory immutable owner/request invariants so stale writers cannot silently create invalid rows. `db:generate` must report no schema changes at the final schema. No hand-adjusted migration ledger/hashes.
11. Start current web with host runtime root/source snapshot inaccessible. Run history/transcript/cost/artifact/HITL/resume qualification and reconcile sealed orphan items/tombstones from every partial upload. Turn off import mode and remove its one-time authority after success. Source/backup deletion is a separate operator retention action after confirmed acceptance, never part of generating proof.

**Planned CLI contract** (implemented in S4, not available or executed during planning): add `db:migrate --stage execution-ab-additive` (ends after 0133), `--stage execution-ab-associations` (0134 only), `--stage execution-ab-finalize` (guarded 0135 onward); invalid high-water/order refuses. Extend `execution-data-plane:import-legacy` with distinct `inventory`, `copy`, `verify`, `finalize-proof` subcommands, `--import-id`, `--batch-size`, `--max-bytes` and `--resume`. The operator source root is a local CLI/environment input confined to inventory/import; API bodies use manifest item IDs. Commands print opaque IDs, bounded counts and remediation codes, not raw source paths. A dry inventory emits no complete proofs. One CLI mode does one operation; implement separate functions instead of boolean multimode helpers.

Executable qualification invokes these commands individually against a restored real Stage A database, interrupts each phase, restarts supervisor/CLI, completes guarded cutover, and compares readback to the untouched original snapshot. Include already-canonical databases, stopped pre-0134 databases and fresh installs. If an already-cut-over database lacks byte/association proof, inventory the retained source/backup and repair forward; if sources are unavailable, report unrecoverable evidence and do not invent successful preservation.

### D10. Operation-scoped filesystem inventory and real isolation

Replace file-level allowlisting with entries keyed by source file + enclosing function + resolved filesystem/child-process callee + operation kind + ownership class + rationale. Scan `.ts`, `.tsx`, `.mts`, `.cts`, `.js`, `.mjs` production roots, including `web/app`, `web/lib`, components that can import server utilities, scripts, instrumentation and imported shared helpers. Enumerate aliases/wrappers and child-process filesystem effects. Use TypeScript AST where practical; unresolved dynamic calls require explicit classification. Tests and migration-only sources are separately identified, not silently excluded from the ownership model.

Classes: manager-owned DB/config/package/evaluation artifacts; supervisor runtime bytes/state; repository/worktree/Git operations retained for Stage C; temporary operator import only. Mixed files contain multiple entries. A new host-runtime read in an already approved file must fail the guard. This static test supplements a real denied-access environment; it cannot prove isolation by itself.

Use disjoint private roots under separate process identities or Linux mount namespaces. Shared repository/worktree access remains permitted; supervisor SQLite/log/object/adapter-private roots are absent or inaccessible to web. Merely choosing different path strings or chmod under the same root user is insufficient. Negative control: a web-process access attempt to a seeded known private sentinel returns EACCES/ENOENT; positive controls prove supervisor access and authorized HTTP object read still work. Test harness may manage both roots; production web may not.

Extend `web/test-support/real-supervisor.ts` and add one real web-process harness with isolated ports, DBs, process groups and persistent restart roots. Reuse `web/test-support/pg-container.ts` as the sole Postgres constructor; no shared development database/ports or broad process killing. A test-only network proxy can drop ACKs, block receipts/SSE, delay old responses and cut streams deterministically without changing production branches. Fake ACP is permitted for deterministic protocol effects; the supervisor, HTTP stack, SQLite, Postgres, projector and owner entrypoints must be real.

### D11. Runtime/deployment compatibility

Choose an explicit binary HTTP stack: the project's **pinned Undici 8.4.1** API with an explicit dispatcher, its matching Request/Response types and explicit abort/body cleanup. Remove the native-fetch/legacy-dispatcher mixing on this path. Do not patch installed dependency source, globally install a dispatcher, retry malformed local header construction as a remote uncertainty, or add a runtime-dependent fallback. Existing JSON/SSE paths must retain their timeout semantics; audit every shared adapter caller before changing types.

Proposed supported application runtime range is **Node >=24.15.0 <25** for this release. This consciously narrows the old unqualified Node 24 claim because earlier patch versions have not been qualified; it is not evidence that all older patches are broken. The fix must pass the diagnosed 24.15 runtime, not simply raise the floor to 24.19 to erase its failure. Qualify 24.15.0 and 24.19.0 explicitly, select a passing reproducibly pinned Node 24 image tag/digest at implementation, and enforce the floor in root/web/supervisor engines, runtime preflight, CI, operator instructions and Dockerfile. Node 26 in the planning shell is outside this acceptance matrix. Record runtime, `process.versions.undici`, installed dependency version and image digest in qualification evidence.

| Surface | Wiring obligation in S1/S3/S4 |
| --- | --- |
| Runtime config/validation | Shared documented values, strict integer/relationship validation; supervisor receives host/outbox/object/import bounds, web receives worker/transport bounds. Environment override round-trip set → removed/default → set is tested at boot. |
| `.env.example`, `supervisor/.env.sample`, `deploy/maister.env.example` | Add settings to the actual consuming process template, comments about ownership and safe defaults; no host-private paths sent to browser/manager APIs. |
| `deploy/maister-web.service`, `deploy/maister-supervisor.service` | Verify EnvironmentFile, shutdown budgets, Node resolution and disjoint host-state configuration. Update stale shared-runtime prose only within this boundary. |
| `Dockerfile` | Pin qualified Node tag/digest; copy all required workspace manifests/config needed by frozen install, propagate environment defaults or documented override contract and verify image health/binary upload. Do not claim the image is the default deployment topology. |
| `compose.yml`, `compose.production.yml` | Remain Postgres-only. Add explicit parity comments pointing to host-process env/config and stating no web/supervisor environment block exists. Validate rendered compose remains unchanged in service/port topology. Do not inject host variables into Postgres. |
| `compose.override.yml` | Absent, so no fabricated edits/service. State this intentional non-applicability in wiring acceptance. Dedicated test isolation config is test-only and named separately. |
| `.github/workflows/ci.yml` and package scripts/engines | Runtime matrix, discovery gate and mandatory scoped A/B real-integration qualification; avoid silently relying on the current label-gated web integration lane. |
| Docs | `docs/configuration.md` is canonical env table; `getting-started.md`, `deployment.md`, `supervisor.md` explain runtime minimum, storage pressure and guarded upgrade. No duplicated conflicting budget tables. |

## SDD/TDD and validation topology

S0 first writes complete requirements, route/message schemas, owner/refusal/recovery-window tables, state machines, schema constraints and primary acceptance mapping in the canonical docs. Mark missing behavior Designed, preserving accepted guarantees. No production implementation begins with unresolved required owner arms, a contradictory budget or an undefined destructive recovery window.

For every behavior task: **RED** executes its named primary scenario against the defect and records the discriminating failure; **GREEN** makes the minimal correction; **REFACTOR** stays within changed ownership boundaries while all affected tests remain green. If RED already passes, produce concrete counterevidence and update the finding, or prove the guard with a focused local mutation that the test kills. Do not weaken safety assertions to match broken behavior. No future-increment intentionally failing tests are committed as active tests in an earlier deployable increment.

Test inventory and mutation intent:

| Lane | Owner / runner | Planned file or existing suite to extend | Primary responsibility |
| --- | --- | --- | --- |
| Host output/pressure | E; supervisor Vitest `integration` | Existing `supervisor/src/__tests__/runtime-event-outbox.integration.test.ts`; new `runtime-output-pressure.integration.test.ts`; fixtures under `supervisor/test/fixtures/` | AT-01/02 with real ACP child, SQLite restart, actual bounded pipe behavior. |
| Canonical worker | E; web Vitest `integration` + real PG | Existing `web/lib/execution-host/events/__tests__/ingest.integration.test.ts`; new `projection-worker.integration.test.ts` | AT-03/04 real scheduling/claim/apply, first cursor failure, two consumers/runs, restart without new event. |
| Command reducer/transport | C/Q; web Vitest `integration` + real supervisor/PG | Existing `web/lib/execution-host/__tests__/{command-recovery,deliverer,lifecycle-regression}.integration.test.ts` | AT-06/07/08/10/17. Replace V3 receipt-only failure and keep V7b as minimum-runtime regression. |
| Owner restart | C; web Vitest `integration` + real peer/PG | New `web/lib/execution-host/__tests__/prompt-owner-recovery.integration.test.ts`, split by domain only when setup/size requires; existing gate/consensus/agent/scratch/sync suites | AT-05 full variant matrix; run real owner entrypoints, not invented fixture-only dispatch functions. |
| Object reconciliation/retention | O; web Vitest `integration` + PG/real peer | Existing `runtime-object-retention.integration.test.ts`; new `runtime-object-lifecycle.integration.test.ts` under `web/lib/execution-host/__tests__/` | AT-09/14, event order and more-than-page retention, reference/delete race. |
| Host object integrity | O; supervisor Vitest `integration` | Existing `supervisor/src/__tests__/runtime-objects.integration.test.ts` | AT-13/15 no-follow/inode/hash/range/actual body evidence and restart after seal/tombstone. |
| Historical upgrade | M; web Vitest `integration` + real PG/peer | Existing `web/scripts/__tests__/import-legacy-execution-data-plane.integration.test.ts`, `web/lib/db/__tests__/migration-0135-canonical-data-plane-cutover.integration.test.ts`; new forward migration suite | AT-11 CLI invocation/interrupt/readback, complete historical source preservation, unchanged guards and fresh/upgrade paths. |
| Boundary inventory | Q; web Vitest `unit` | Existing `web/lib/execution-host/__tests__/runtime-data-boundary-inventory.test.ts` and fixture | AT-16 supplementary pure source classification; prohibited added operation in an already allowed mixed file kills guard. |
| Isolated processes | Q; web Vitest `integration` | New `web/test-support/__tests__/execution-ab-isolation.integration.test.ts` plus real harness | AT-16 real permissions/namespaces and process death; uses ordinary production web initialization, worker and transport. |
| Browser outcomes | Q/O; dedicated Playwright real-supervisor config/project | New `web/e2e/execution-ab-lifecycle.spec.ts` and `execution-ab-content.spec.ts`; retain existing `execution-host-contract.spec.ts` as supporting fake-peer coverage | AT-12 and user-visible history/HITL/cancel/resume/completion with unsafe artifact MIME. |
| Pure invariants only | Owning maintainer; existing unit projects | Only canonical request/identity transformation, bounded framing, range/digest parsing, pure reducer tables where stable | No mock-only replica of a real integration scenario; minimum added unit coverage. |

Current `web/vitest.workspace.ts` discovers lib/app/scripts/test-support/e2e integration tests and has `passWithNoTests` scripts. Supervisor integration discovers `src/**/*.integration.test.ts`. Playwright default `AUTHED_SPEC` is an explicit regex; new names do not automatically join authenticated tests. S0/S5 must wire the new real project and prove discovery. `vitest list` and `playwright test --list` must show each promised file/case; an empty successful run fails acceptance.

Phase gates use the actual package scripts, as separate commands:

```bash
pnpm --filter maister-web typecheck
pnpm --filter @maister/supervisor typecheck
pnpm --filter maister-web exec eslint .
pnpm --filter @maister/supervisor exec eslint .
pnpm --filter maister-web test:unit
pnpm --filter maister-web test:integration
pnpm --filter @maister/supervisor test:unit
pnpm --filter @maister/supervisor test:integration
pnpm validate:contracts
pnpm validate:docs
pnpm --filter maister-web db:erd --check
```

Run full unit/integration suites at each completed increment, on the selected supported runtime; scoped new tests first. Build the web/qualified image when runtime/startup/transport/deployment changes; run dedicated real-browser scenarios in S3/S5. Tests introduced under new families require a runner include/config change in that same increment. Use the supported minimum/runtime matrix for AT-17 and affected binary tests. Do not rerun all tests repeatedly without intervening changes or unresolved failures.

Baseline failures are tracked by exact test names/error signatures and environment, never count deltas. Fix A/B failures in scope. A genuinely unrelated harness failure needs a narrowly scoped quarantine with explicit reason and tracked follow-up, reviewed in the phase gate; never quarantine AT-01–17 or claim the original full suite is green while quarantined failures remain. Existing notes about dirty-resolution/recursive harness/E2E failures are historical hints, not current verified exclusions. Qualify tests on disposable roots/DBs and capture stdout/stderr/reports as verifier artifacts outside the code worktree; do not use structured result transport as report storage.

S2.6 gate checkpoint: skill and AI gate dispatch now admit exact evaluation/attempt/session/assignment owners, decode full verified command output and commit the existing gate transition with the command application marker. Re-entry reuses the original command before prompt rendering; old assignment results cannot satisfy a successor. Application failure preserves the running gate and session and yields the graph without fabricating a failed verdict. The fake host now emits lifecycle creation and request-v2 output objects/receipts using the production codecs; legacy terminal receipts remain accepted until their event pointer exists. RED gate-owner and SIGKILL evidence is retained in `s2-6-gate-owner-red.json` and the diagnostic reports. GREEN: 43/43 gate/parser/routing cases (`s2-6-gates-final.json`), including both real-supervisor SIGKILL cases, actual lease expiry, a verdict crossing the 400 kB frame boundary after almost 2 MB of output, N+1 refusal and application-poison preservation. Mandatory A/B web: 100/100 on Node 24.15, no skips (`s2-6-gates-ab-web.json`). Strict web types and scoped lint pass. These tests re-enter the gate reducer after owner recovery; they do not yet prove autonomous parent-graph continuation. Node, permission-resume, orchestrator-wait and durable cursor/successor recovery remain required before S2.6 completion. No schema change or global owner activation is included.

S2.6 failed-create lifecycle contract: new `session.exited`/`session.crashed` canonical payloads include the host record's `createdByCommandId`, existing `sessionName`, and nullable `acpSessionId`. If no incarnation exists, the lifecycle reducer must match the persisted `session.create` command's run, host, assignment/epoch and logical name before recording a historical terminal incarnation. This path never applies a create ACK or changes an existing current session binding. It may create a binding-free logical row when that name has no row yet. Missing/mismatched creator evidence remains a deterministic error. A later created/ACK event for that dead incarnation cannot activate it. A checkpoint terminal requires an already initialized incarnation; missing initialization cannot create a resumable active slot. No table or workflow ledger is added.

S2.6 in-flight permission recovery contract: an owned node's permission schema stores `flowPrompt` with version, command ID, exact attempt/ordinal, assignment and incarnation IDs. Replaying the same command/request pair reuses that HITL row, including its delivered state; it cannot select another attempt's stored answer. Recovery of an already admitted current-assignment turn may reacquire the Flow driver while the run is NeedsInput, but leaves that status and ordinal unchanged until delivery is proved. The event consumer resumes the original host-session stream; it does not create another session or prompt. A graph re-entry with an open owned node is continuation of that visit, never an operator resume. Its status may advance only after the exact permission delivery commits. The chosen response retains `_delivery` (command ID, exact permission payload and host session ID) in the same transaction that queues `session.input`. Reattachment validates that immutable identity and reuses the original command ID and issue time; a host receipt replay settles delivery and HITL together. `_audit` retains the confirmed delivery/source/assignment/incarnation IDs. The initial two delivery crash cases exposed duplicate input creation and a lost-ACK timeout (`s2-6-permission-delivery-first.json`, 0/2); persisted input replay now passes both windows. The mandatory A/B web gate passed 127/127 in 12 suites without skips (`s2-6-live-permission-ab-web-first.json`), followed by 14/14 existing permission/cap/resume/crash regressions. The expanded final permission matrix passed all 12 selected cases (`s2-6-permission-matrix-final.json`): eight real-process/transport/DB fault windows plus four existing permission ledger cases; 18 unrelated Flow cases were filtered. Those windows cover death before/after HITL commit, before input effect, before input ACK, definitive service refusal with explicit operator retry, failed permission persistence with eventual deferred delivery, lost HTTP reply, and concurrent responses. The concurrent RED exposed an extra cancellation by the delivery CAS loser; exact-command conflicts now rejoin the winning delivery. Failed persistence yields the driver for durable replay. No additional table or ordinal is introduced. This remains an additive S2.6 checkpoint; global activation is deferred to S2.12. Checkpointed/new-assignment permission resume and released-source handoff remain separate pending contracts.

## Implementation tasks and ownership

S2.6 pre-prompt create contract: a new nullable private `execution_commands.create_intent` stores version 1, a closed Flow node/gate owner descriptor, its operation key, create generation, optional superseded command ID, and the original canonical create envelope plus SHA-256. It is operational command intent in the existing receipt ledger; `node_attempts` remains the sole Flow execution ledger. A unique current-assignment operation/generation index prevents duplicate creation. Workspace adoption and capability materialization precede the transaction; exact normalized request, command, and owner admission commit before any create effect. Ordinary restart replays the same ID, issue time and immutable request without re-materializing runtime inputs. Every live ACK, receipt recovery and canonical created event checks the exact current attempt/ordinal or gate evaluation and latest admitted create generation before changing session bindings. Terminal historical evidence remains recordable after authority is lost. Unknown transport results preserve the create for replay beyond an individual delivery budget; they never authorize a new session. A fresh fallback generation requires the previous resume create's definitive CHECKPOINT refusal and records that source command; a stale workspace handle requires an explicit definitive host refusal before readoption and a replacement generation. The existing continuation worker discovers create intent before prompt admission, including a CLI parent's gate, and enters the same leased graph driver. Driver lease loss cannot commit a new create admission or overwrite a newer binding. No global worker activation occurs before S2.12.

S2.6 pre-prompt implementation checkpoint: `0146_flow_session_create_intents` adds the private command intent with a closed owner/generation shape and digest/fence constraint. Node and AI/skill gate callers use the lazy owned-create entrypoint before rematerializing inputs. Its exact current owner and latest create generation fence HTTP ACK, receipt recovery and canonical created projection. Lost authority yields a historical `lost` incarnation with no activation; no successor binding changes. The common recovery pass retains owned creates without a receipt and may settle their existing receipt under run-first locks. Unknown outcomes retain the same command beyond the old delivery budget, with bounded backoff. Explicit CHECKPOINT and released-handle refusals admit one persisted successor generation; `sessionFallback` distinguishes an actual resume fallback from workspace readoption. Existing attempts/evaluations also let the Flow worker recover before create admission. RED: death after host create but before manager ACK stranded the graph (`s2-6-create-red.json`, 0/1). GREEN focused evidence: the original four before-effect/before-ACK node/gate windows pass (`s2-6-create-first.json`, 4/4); before-admission death passes for both (`s2-6-create-before-admission-final.json`, 2/2); old ordinal/evaluation refusal plus four lost create replies pass (`s2-6-create-stale-unknown.json`, 3/3); real ACP session-not-found and released workspace handle replacement pass (`s2-6-create-definitive-final.json`, 2/2). The expanded first migration qualification exposed PostgreSQL operator precedence in a JSON key-removal CHECK; parentheses were corrected in the uncommitted forward migration before these final cases ran. Both strict typechecks, 102 documentation links, three Mermaid diagrams and ERD drift validation pass. The full mandatory web gate executed 140 cases without skips: 138 passed and two new before-effect assertions incorrectly required queued instead of preservation of the observed delivering/queued state (`s2-6-create-ab-web.json`). After correcting only those assertions, both windows and both definitive-refusal regressions pass (`s2-6-create-recovery-final.json`, 4 selected cases, 33 filtered). Thus every case in that gate has passing execution evidence; the combined full run itself is retained as failed. The full supervisor retry passed 88/88 without skips (`s2-6-create-ab-supervisor-final.json`); its first run had one prompt-completion timeout, which also passed independently before the full retry. Final scoped lint is clean. S2.6 remains incomplete.

S2.6 traversal-boundary contract: the persisted 500-attempt ceiling gates admission of a fresh visit, including retries, but never re-entry into an already admitted visit. Recovery of visit 500 must finish its original action/gates exactly once; attempting to append visit 501 still fails with CONFIG and leaves the ledger at 500 rows. A CLI node or command-check gate receives the leased graph driver cancellation signal. A signal already aborted prevents spawn; loss during execution immediately kills the entire detached process group, including descendants that ignore SIGTERM, removes listeners/timers, and yields through the existing Flow-driver fence. The stale driver must not fail/complete the attempt, gate or run. Timeout behavior retains its existing cleanup grace. Qualification uses real graph drivers, Postgres state, process death and live child PIDs; no schema or new execution ledger is required.

S2.6 traversal-boundary checkpoint: the ceiling now checks only fresh visits, and CLI actions/gates receive the leased driver's abort signal. RED evidence retains the already-aborted spawn failure (`s2-6-cli-abort-red.json`), the 500th-visit recovery failure (`s2-6-boundaries-red.json`), and both actual assignment-replacement hangs (`s2-6-cli-graph-red-final.json`). Initial fixture-only failures are not counted as product evidence: pre-seeding history violated fresh launch discovery, and the CLI successor fixture initially read the legacy run host field instead of the current assignment host. GREEN: all four new graph cases pass (`s2-6-boundaries-green.json`), all 19 CLI cases pass, and all 42 existing graph/gate/driver-claim integrations pass. The full mandatory web A/B gate now passes 144/144 in 12 suites without skips (`s2-6-boundaries-ab-web.json`), including every pre-prompt create and permission regression from the previous checkpoint. Strict web types, scoped lint, nine documentation links and two Mermaid diagrams pass. No schema or global worker activation change is included; S2.6 remains incomplete.

S2.6 gate-permission continuation contract: the existing private HITL `flowPrompt` source becomes a closed node/gate union. The node shape remains unchanged; the gate shape contains version 1, `variant: gate_ai|gate_skill`, command ID, node-attempt ID, admitted prompt ordinal (currently zero), gate ID, exact evaluation ID, assignment ID and incarnation ID. All response admission, input replay and ACK writes verify the current binding and latest evaluation; an older evaluation cannot consume or clear its successor's permission. Both live and reattached gate prompts run the same permission event consumer and retain the original HITL/input identities. The Flow worker may reacquire a NeedsInput graph for that exact gate without authorizing an operator resume. It reuses the parent's completed action and the existing gate evaluation. Even if the verdict applies before a lost input ACK is recovered, no session cleanup, gate routing or cursor move may bypass unresolved permission delivery. Unknown delivery replays the original command; a separately authorized retry of a definitive refusal retains the existing source checks. No new table, gate evaluation, ACP turn or ordinal is introduced on restart. Qualification covers both gate variants at HITL persistence and input delivery boundaries, plus stale-evaluation denial; checkpointed/new-assignment gate resume remains part of the separate explicit-resume contract.

S2.6 gate-permission checkpoint (qualified): `graph/node-permission.ts` is generalized as `graph/prompt-permission.ts`; node source shape remains compatible while gate sources retain evaluation and variant. Live/reattached gates share the existing event consumer and input replay. NeedsInput discovery distinguishes an open gate from an open node so the parent's action completion remains reusable. A terminal gate with unresolved permission reattaches delivery before cleanup/routing. RED: the initial selector matched no cases (`s2-6-gate-permission-red.json`, zero executed); the corrected actual SIGKILL-before-HITL case failed as intended (`s2-6-gate-permission-red-final.json`, 0/1). The first three node/AI-gate/skill-gate before-HITL cases pass, including a real transaction containing a later evaluation that rejects the original source (`s2-6-gate-permission-first.json`, 3 selected, 46 filtered). Scoped lint and strict types pass after explicit source-variant narrowing. The first lost-input-ACK matrix passed one case and stranded the skill gate (`s2-6-gate-permission-ack-first.json`, 1/2): the application adapter could mark the exact current owner superseded while the run was NeedsInput. Gate application now retains a valid verdict in NeedsInput; traversal remains blocked on delivery. The test now forces application before ACK recovery for all three owner variants. Reattachment reconciles already admitted `_delivery` inputs directly before waiting, through the same preparation and ACK helper; it cannot depend on a fast completed-prompt waiter giving the SSE consumer time to replay. The first forced-order run lacked an independent canonical-event reader and used a 20-second deadline shorter than the killed reader’s 30-second lease (`s2-6-gate-permission-ack-forced.json`, 0/3); this was a harness failure. With the real reader restarted independently and the actual lease allowed to expire, all three node/AI-gate/skill-gate cases apply before the input ACK, then replay the original delivery and finish once (`s2-6-gate-permission-ack-forced-second.json`, 3 selected, 46 filtered). All four existing permission-ledger cases pass (`s2-6-gate-permission-ledger-final.json`, no skips). Strict web types, scoped lint, nine documentation links and two Mermaid blocks pass. The mandatory full A/B web gate passes 152/152 in 12 suites without skips (`s2-6-gate-permission-ab-web.json`), including all 16 node/gate permission windows and the existing graph/create/owner regressions. No supervisor production code changed since its qualified 88-case gate. Checkpointed permission resume, released-source handoff and global activation remain pending; S2.6 is not complete.

S2.6 final admission-fence qualification: node, AI gate and skill gate admission must roll back the immutable prompt INSERT when the driver lease expires while that statement is blocked. Qualification suspends the real driver process after the database trigger reaches INSERT, lets its actual lease expire without renewal, then resumes it. No prompt or domain completion may commit; the next driver must recover the original visit and finish with one admitted prompt. The test must fail when only the post-INSERT `admission.assertCommit` callback is temporarily removed, and the source must be restored byte-for-byte before normal verification. The isolated preparation run passed all three cases (`s2-6-admission-expiry-first.json`). In the production-driver suite, removal of only the post-INSERT callback made all three cases fail because one stale prompt committed instead of zero (`s2-6-admission-expiry-mutation.json`, 0/3 selected). The ledger was restored byte-for-byte (SHA-256 `8e2e0a6cdd2aeff493bb9f66b3da61d588ae0162202b6b34905eb6bffa4cf63a`); all three cases then pass with one successful successor continuation (`s2-6-admission-expiry-final.json`, 3 selected, 49 filtered). Strict web types and scoped test lint pass. Production code is unchanged from the qualified 152/152 mandatory gate; these three additional tests are separately qualified, not represented as a combined 155-case run. This closes the pending final-admission-fence proof; checkpointed permission resume and released-source handoff still keep S2.6 open.

S2.6 checkpointed node-permission resume contract (next increment): select the exact current attempt through the owned HITL source, never by an arbitrary open row or step-only match. For an unanswered checkpointed permission with no admitted input effect, the normal NeedsInputIdle capacity claim must persist `action_resume` with version 1, `kind: permission`, source command/assignment, current assignment, new prompt ordinal, original ACP resume handle, HITL request ID, source request ID and chosen option ID, while rebinding the same node attempt and clearing only its previous action snapshot. Use the existing `permission_resume` command-owner variant. Ordinary restart consumes this already admitted authorization and never increments the ordinal. Reissued permission delivery may reuse the original HITL only after exact persisted source/choice/current-generation validation; completed output returns to the existing graph reducer, gates and cursor. A prior admitted input must first be classified using its original historical receipt and prompt evidence; it cannot authorize a new turn merely because its manager ACK is missing. Handoff of confirmed completed source evidence and gate checkpoint/resume remain separate required arms of S2.6. Only forward migrations may widen the committed 0145 shape. Global workers remain dormant until S2.12. The first real resumable-ACP test will qualify the capacity claim and one attempt/two prompt generations through a command gate and successor.

S2.6 action snapshot fields for the next forward migration: `node_attempts.action_completion` stores version 1, source command ID, admitted prompt ordinal, bounded action result and original structured-output payload; `node_attempts.action_prompt_ordinal` is nonnegative and must match any stored completion. The snapshot is internal attempt state, not a second execution ledger or node-success marker. Only a separately authorized turn may advance the ordinal and replace the completed action. No graph finish may re-dispatch an action merely because the completion marker was already applied.

S2.6 finish-continuation contract before the next forward migration: `node_attempts.finish_continuation` is nullable version-1 JSON with `targetNodeId` (string or null), `injectedVars` (object), `sessionPolicy` (`resume`, `new_session` or null), and `autoRetry` (boolean). It snapshots the already selected outgoing edge and its input context on the closing source attempt, alongside the run cursor. It is internal graph continuation metadata, not another execution ledger or a new ACP-turn authorization. Recovery restores the latest source attempt's continuation only for its selected current cursor; a new operator/assignment generation must use its own existing domain claim. Ordinary successful closes store empty context, rework stores the selected comments and session policy, and retry stores its normal bounded retry decision. The successor attempt remains the ordinary `node_attempts` row. Permission/orchestrator turn identity and released-source handoff are implemented and qualified by the S2.6 increments above; autonomous global worker activation remains S2.12.

S2.6 orchestrator resume contract before the next forward migration: nullable `node_attempts.action_resume` version-1 JSON records `kind: orchestrator`, `sourceCommandId`, `sourceAssignmentId`, `assignmentId`, `promptOrdinal`, and `resumeSessionId`. The existing WaitingOnChildren claim locks the run, checks capacity under the scheduler lock, mints its new assignment and advances the exact parked attempt ordinal in that same transaction, retaining the source identity while clearing its old action completion. Initial/legacy attempts have no resume metadata. Ordinary recovery reads this already authorized intent; it never increments an ordinal. The worker must discover an admitted intent even before its first prompt exists. A child wake deferred by capacity persists the existing `resume_requested_at` readiness marker and keeps the parent parked. Park closes the active execution assignment atomically with the existing node/run pause. Superseded sources cannot authorize a turn. This records a new ACP turn; handoff of already completed evidence and permission-specific resume identity remain separate requirements. The first orchestrator qualification used a fixture without advertised resume capability; its rejected create produced a terminal lifecycle event without an incarnation and poisoned lifecycle projection (`s2-6-orchestrator-first.json`). A resumable fixture qualifies the normal path. The refused-create regression subsequently reproduced the poisoned cursor (`s2-6-failed-create-lifecycle-red.json`, 0/1). Host-derived creator identity now lets lifecycle projection preserve a historical terminal incarnation without rebinding the logical session. A first failed create produces a binding-free logical row. Unknown/mismatched creator evidence is rejected, and a late create ACK cannot reactivate the dead incarnation. All 55 cases in the four affected manager integration suites and all 88 mandatory host A/B cases pass without skips (`s2-6-failed-create-{web,supervisor}-final.json`); both strict typechecks, scoped lint and eight contract parsers pass. Permission/source/pre-prompt recovery still remain.


Task checkboxes record verified completion. `Depends` names hard prerequisites; tasks in different increments may overlap only as stated under delivery order. Every task applies the global logging/data-safety contract. Schema/transport/API changes are coordinated by D; workers may own disjoint modules but must not revert another owner's changes. Proposed files below are intentionally new; existing paths are the starting points, not permission to refactor unrelated code.

### S0 — Requirements, contracts and test topology

- [x] **S0.1 — Reconcile baseline, requirements and decisions.** Owner D; Depends: none. Revalidate HEAD/main and audit R01–R17, migration/ADR lineage and active instructions. Files: this plan, `docs/system-analytics/execution-{event-plane,prompt-lifecycle,runtime-objects,data-cutover}.md`, `execution-hosts.md`, existing A/B plan status/as-built mapping. Deliver complete AB-01–17 enforcement/test mapping and correct false Implemented/test-traceability claims with Designed tags. No production changes. Logging: validator output lists requirement/decision IDs and unresolved counts only. Acceptance: no finding removed without code/test counterevidence; no assumed current results from old checkboxes.

- [x] **S0.2 — Freeze normative schemas, state/refusal/recovery tables.** Owner D with C/E/O; Depends: S0.1. Files: supervisor/web OpenAPI, host/supervisor/browser AsyncAPI, `docs/database-schema.md`, `docs/db/execution-hosts-domain.md`, `artifacts-domain.md`, affected owner analytics. Deliver D1–D9 field-level contracts, constraints, cross-resource classifications, exhaustive owner windows and failure matrices before code. Reconcile protocol v2 activation/old-row drain; add an ADR only for a direction change, using implementation-time number allocation. Logging: safe schema/anchor/error pointers. Acceptance: every state/effect has an enforcing lock/CAS/reducer and primary test; no unspecified owner branch.

- [x] **S0.3 — Freeze topology, resource and deployment contract.** Owner Q/E; Depends: S0.1. Files: test-runner configs as specification targets, `docs/configuration.md`, `getting-started.md`, `deployment.md`, `supervisor.md`, test-database/execution analytics. Deliver D4 budgets, Node floor/matrix, runtime-root denial method, per-lane discovery commands, fault-injection synchronization points and actual host-env wiring table. RED cases are specified but future broken tests are not activated yet. Logging: runtime/version/budget/harness ID and discovery counts. Acceptance: leases cover real budgets; private-root negative control is possible; Postgres-only compose preserved.

- [x] **S0.4 — Reconcile specification gates and initial failure sets.** Owner D/Q; Depends: S0.2, S0.3. Files: `scripts/validate-contracts.mjs` contract coverage only if needed, this plan, affected canonical docs and planned scenario inventory. Run doc/contracts/ADR links and test discovery; record exact existing runnable test names, not claimed aliases. Clarify outdated rule prose through the owning context/rule workflow during implementation, without copying false behavior into requirements. Logging: gate name, revision, exact failed test/pointer, outcome. Acceptance: S0 specs complete and internally consistent; no production implementation; S1–S5 task/test dependencies executable.

S0 acceptance: normative specs and owner/refusal windows are complete; all design-level validators green. Rollback: documentation-only revert, no state impact. Release: a docs-only commit; no runtime compatibility promise changes until the corresponding implementation gate passes.

### S1 — Host output, binary transport and durable projection

- [x] **S1.1 — Repair and qualify binary HTTP stack (AT-17).** Owner Q; Depends: S0.4. Files: `web/lib/supervisor-client.ts`, ExecutionHosts transport types, existing command-recovery V7b and real-supervisor fixture. RED on Node 24.15 captures safe cause code and proves supervisor received no request for invalid local construction. GREEN uses explicit pinned Undici binary fetch/dispatcher, strict headers/body length/digest and cleanup/timeout semantics. REFACTOR shared binary handling only. Logging: runtime/library version, command/object ID, byte count, HTTP/cause code, dispatched/not-sent, duration. Acceptance: real reserve/upload/read on minimum and 24.19; no unknown-outcome retries for proven local construction failures.

- [x] **S1.2 — Bound ACP framing and publication (AT-01).** Owner E; Depends: S1.1 and the object contract frozen in S0.2. Files: `supervisor/src/{spawn,acp-client,registry,runtime-event-publisher,runtime-events,prompt-runtime-objects}.ts`, host output integration fixture/test, minimal `web/lib/execution-host/events/runtime-object-projector.ts` intent/available equality correction and manager reference decoding/read consumers. E coordinates the host slice with O on the object slice. RED actual oversized/multibyte stdout/tool messages with sibling session. GREEN bounded byte framer/backpressure-aware log/ACP handoff, immutable output segments and bounded reference events, explicit producer-local failure boundary. Bring the minimal usable intent/seal/availability and exact-byte read path into S1, including R09 event-before-ACK equality; a frozen future S3 contract is insufficient. Wire reference-bearing session/browser/transcript/result consumers using their explicit content authorization so new references are consumable. REFACTOR only shared framing/summary functions. Logging: frame bytes, producer/session/command, object ref, pressure/error reason; no line/body. Acceptance: byte/memory cap, sibling survival, real manager retrieval/semantic reconstruction of exact referenced bytes before S1 release, no successful empty substitute for required output. S3.1 completes the wider monotonic object lifecycle rather than postponing this prerequisite.

- [x] **S1.3 — Unify outbox accounting and terminal wallets (AT-02).** Owner E; Depends: S1.2. Files: `supervisor/src/{host-state,command-receipts,runtime-event-publisher,http-api,main}.ts`, new validated limits module, outbox tests. RED retained-only pressure, unACKed pressure, row-first/byte-first cap, reserve starvation and concurrent producers with maximum outputs. GREEN one transactional accounting/reservation function, regular/control partitions, wallet spend and safe pause/teardown/hysteresis. REFACTOR existing duplicated checks. Logging: retained/unACKed bytes+rows, wallet/reserve counters and state transitions. Acceptance: no event loss/uncaught hard-limit exception, no effect without credit, ACKed rows still counted, promised terminal work progresses.

- [x] **S1.4 — Persist consumer initialization and first failure (AT-04).** Owner E; Depends: S0.4. Files: `web/lib/execution-host/events/projector.ts`, `ingest.integration.test.ts`, `schema.ts` and new forward migration if claim index/field needed. RED absent cursor failure and stale recorder race on real PG. GREEN separate cursor/claim commit and savepoint/CAS error preservation; deterministic/transient classifications. REFACTOR transaction helper only. Logging: consumer/run/event/cursor/generation, bounded original reason, attempts, retry time. Acceptance: no missing-consumer exception masks cause; stale failure never poisons progressed work.

- [x] **S1.5 — Build autonomous fair projection worker (AT-03).** Owner E; Depends: S1.4. Files: new `web/lib/execution-host/events/projection-worker.ts`, existing projector wrappers, canonical consumer/ingest services, `instrumentation.ts`, scheduler activation seam. RED >2 batches/no-new-ingest, retry deadline, multi-run gap promotion and two worker instances. GREEN D3 candidate/claim/quantum/timer/shutdown logic and bounded boot backfill; wire every canonical consumer responsibility. REFACTOR one scheduling primitive. Logging: due/backlog counts, service health, claim/generation, lag and poison counters. Acceptance: actual startup/claim path drains backlog fairly without waiting for SSE lifetime or projection-dependent ACK.

- [x] **S1.6 — Deployment wiring and runtime floor.** Owner Q/D; Depends: S1.1, S1.3, S1.5. Files: root/web/supervisor `package.json`, lockfile, config validators, `Dockerfile`, both compose files, `.env.example`, `supervisor/.env.sample`, deploy env/units, CI and operator docs. RED minimum/image transport test, invalid config and set/remove/reset boot configuration. GREEN exact D11 wiring, pinned runtime/dependency, worker shutdown budget and Postgres-only compose comments. REFACTOR duplicate runtime-version checks only. Logging: accepted config hash/limits/runtime IDs, never raw env. Acceptance: each new setting is wired to its consumer; absence of compose.override explicitly checked; default image actually built/tested.

- [x] **S1.7 — Migrate affected assertions and pressure/restart coverage.** Owner Q/E; Depends: S1.2–S1.6. Files: `supervisor/src/__tests__/{spawn,runtime-events}.test.ts`, outbox/runtime-event-ack integrations, web ingest/command-recovery/reference-consumer integration and runner includes; reference consumers include `web/lib/runs/run-transcript-projector.ts`, canonical session-stream/browser mappers and structured-output readers reached by S1.2. Replace assertions equating a raw stdout line with unbounded canonical content, retained-free admission and one-shot projection with the new invariant. Retain meaningful old ordering/idempotence tests. RED/GREEN/REFACTOR applies to new boundary cases, not snapshot churn. Logging: exact scenario names/versions/failed invariant. Acceptance: discovery nonempty, no skipped AB case; all S1 ATs executed on their real seams.

- [x] **S1.8 — S1 integration and specification gate.** Owner D/Q; Depends: S1.7. Files: S1 contract/docs surfaces, generated schema/ERD where changed, this plan's evidence links. Run full phase gates, runtime matrix and adversarial review of pipe/claim/ACK seams; repair discovered in-scope failures before exit. Logging: gate/revision/runner/runtime/outcome and safe artifact IDs. Acceptance: AB-01–04/17 green; canonical ingest ACK decoupled from projection; one-host launch/HITL/checkpoint remains working. No new summary report task; record proof in the owning spec/plan matrix.

S1 rollback: additive schema is backward-readable; retain outbox/receipts/objects and cursor state. Pause admissions/drain new output-reference producers before any binary downgrade. Once new event schema/control-wallet data is persisted, roll forward with a compatible fixed host; never truncate SQLite or restore only one store. Release only from the completed S1 gate.

### S2 — One command reducer, durable owners, unknown outcomes and retention

- [x] **S2.1 — Add exact request and application constraints.** Owner C/D; Depends: S1.8. Files: `schema.ts`, `execution-host/{contracts,commands,ledger,redact}.ts`, new forward migration/snapshot/journal, DB integration tests. RED same-key/different-request and missing required v2 owner/request; GREEN D1 private canonical request and content-bound uniqueness, typed owner refs, application/transport fields and indexes. REFACTOR reused canonicalization only. Logging: schema version/digest/owner IDs, never canonical JSON. Acceptance: request+owner admission atomic; snapshot round-trip dispatches identical request; old redacted-only rows classified, not populated with fabricated hashes.

- [x] **S2.2 — Unify terminal evidence and request-bound receipt identity (AT-06).** Owner C; Depends: S2.1. Files: `execution-host/{deliverer,recovery,commands,signals}.ts`, `events/prompt-projector.ts`, `supervisor/src/{command-receipts,host-state,http-api,types}.ts`. RED receipt-first success/rejection and nested details conflict; GREEN one reducer with matching command/session/fence/request/outcome/reference identity; host receipt v2 includes URL target. REFACTOR remove second prompt-terminal writers. Logging: evidence source/digest/phase/agreement reason. Acceptance: V3 assertion migrated; matching receipt alone never settles prompt authority; old-row quarantine/drain is explicit.

- [x] **S2.3 — Keep unknown admission recoverable (AT-07).** Owner C; Depends: S2.2. Files: deliverer/recovery/query/wait and transport error types, real partition tests. RED accept→ACK loss→all reads blocked past retry budget; GREEN separate transport unknown and durable due-reconciliation status, same request/ID replay, incoming evidence wake after outbound budget exhaustion. REFACTOR shared retry classifier. Logging: attempts, deadline, last safe cause/status, admission certainty. Acceptance: no domain failure or extra ACP turn; handle stays actionable after web restart.

- [x] **S2.4 — Fence every create/ACP binding write (AT-08).** Owner C; Depends: S2.2. Files: `execution-host/create-ack.ts`, deliverer/recovery, `runs/active-run-session.ts`, agent/scratch create callers and `runs/sync-resolver.ts`. RED late N ACK/live callback and recovery after N+1; GREEN lock authoritative run/assignment/logical-session/incarnation, return applied/stale disposition and require it at every post-ACK write. REFACTOR one binding application helper. Logging: old/current assignment and outcome, no paths. Acceptance: historical evidence retained, successor host/ACP IDs/status untouched; stale callback cannot pass through independent setters.

- [x] **S2.5 — Build durable owner application dispatch.** Owner C; Depends: S2.1–S2.4. Files: new `execution-host/prompt-owners.ts`, `prompt-owner-recovery.ts`, query/wait/recovery activation and typed registration at application layer. RED terminal-before-apply, DB rollback and competing workers; GREEN command-scoped output verification, claim/CAS and existing domain adapter dispatch; avoid lower-level imports cycling into every domain. REFACTOR stable scheduling with S1 primitives. Logging: owner variant/generation, apply state, output identity, claim/retry/poison. Acceptance: live waiter and restart worker share the path; exact domain write and marker atomic; no second workflow ledger.

- [x] **S2.6 — Flow node and gate owner continuations.** Owner C-Flow; Depends: S2.5. Files: `flows/runner-agent.ts`, `flows/graph/runner-graph.ts`, `flows/graph/gates-exec.ts`, `runs/resume-driver.ts`, existing graph/gates integration suites. RED `owner-flow-node`, permission-resume/restart-no-resume/orchestrator-wait and both gate variants; GREEN persist attempt/gate-specific keys before dispatch, recover full output/results/verdict and route through existing graph/gate reducer. REFACTOR lost stack continuation into single-purpose owner functions. Logging: attempt/gate/evaluation/command and cursor transitions. Acceptance: no rerun of accepted prompt, no skipped gate/cursor move, `node_attempts` stays sole Flow ledger.

- [x] **S2.7 — Consensus verifier/synthesis/draft continuations.** Owner C-Consensus; Depends: S2.6 and agent adapter interface from S2.8. Files: `flows/graph/consensus/runtime.ts`, its ledger helpers, relevant `agents/launch.ts` draft sections (coordinate with S2.8), consensus integrations. RED each `owner-consensus-*` variant with same node attempt but different round/target. GREEN persist cell/synthesis/draft generation and full output artifact reference, apply existing consensus row/child result once. REFACTOR deterministic key/output decoder. Logging: round/verifier/target/participant IDs and result disposition. Acceptance: cannot cross-apply within a matrix or invent empty draft after restart.

- [x] **S2.8 — Agent turn and message continuations.** Owner C-Agent; Depends: S2.5. Files: `agents/launch.ts`, `agents/consume-session.ts` if an existing helper is extracted there, existing agent/message/result integrations. RED initial/resume/rework/live-message variants before terminal and before apply. GREEN durable turn identity, original public-result snapshot/bytes, message acknowledgment and existing completion/re-entry transitions, including parked/waiting windows. REFACTOR narrowly extract owner callbacks, no agent platform redesign. Logging: run kind/turn/message/assignment/result-contract identity. Acceptance: no stale result, duplicate message or cap-bypassing resume; draft adapter interface available to S2.7.

- [x] **S2.9 — Scratch and package-assistant continuations.** Owner C-Scratch; Depends: S2.5. Files: `scratch-runs/{service,events,recovery,dialog,state}.ts`, local-package postprocess integration. RED scratch launch/message/recovery and package postprocess windows with live host session after web restart. GREEN durable dialog turn and WaitingForUser/reply application; journal existing postprocess action with lock/generation recheck and forward settlement. REFACTOR stack-only completion code into shared owner apply. Logging: scratch/turn/action IDs and safe status/authority refusal. Acceptance: next message allowed exactly when original turn completes; no repeated package action or lost assistant reply.

- [x] **S2.10 — Gate chat, sync resolver and deferred release.** Owner C-Interactive; Depends: S2.5. Files: `services/gate-chat.ts`, `runs/{sync-resolver,sync-target,sync-recovery}.ts`, related HITL/session/keepalive/reconcile callsites. RED gate completed before lease expiry recovery, terminal-before-L3-restore and restore-before-reply-commit crashes, sync Running/NeedsInput/Review windows and releasing-side DB failure. GREEN evidence-first owner recovery before LEASE_EXPIRED, durable L3 sense-and-restore before releasing the pending-turn fence, exact reply+marker and existing sync claim/agent_running→verifying/finalization continuation; explicit pending-permission release/handoff on every failed persistence/cancel/checkpoint/fence path. REFACTOR only shared known owner semantics. Logging: turn/lease/operation/deferred ID and release result. Acceptance: no lost gate reply, parked sync strand or invisible deferred; no Workspace/Git ownership changes.

- [x] **S2.11 — State-aware command/receipt retirement (AT-10).** Owner C; Depends: S2.6–S2.10. Files: `execution-host/{commands,recovery}.ts`, `supervisor/src/{host-state,command-receipts,http-api}.ts`, command retirement contract/client and retention tests. RED long-lived accepted, unknown, terminal-unapplied, run-delete/cascade race and eligibility ACK-loss; GREEN D6 protocol, startup repair-before-prune, compact protected tombstones and bounded eligible scan. REFACTOR remove age-only delete predicates. Logging: eligible/protected reason, terminal ACK/owner disposition, command digest and reclaimed counts. Acceptance: no valid retry loses idempotency; eligible evidence reclaims only after both sides confirm.

- [x] **S2.12 — Mandatory-owner activation, assertion migration and gate.** Owner D/C/Q; Depends: S2.7–S2.11. Files: all S2 schemas/contracts/analytics, recovery integration and domain suites, `reconcile.ts`, runtime capability checks and new forward invariant migration. Explicitly classify/drain pre-v2 accepted/unknown/redacted-only rows; reconstruct only from uniquely provable original sources, otherwise preserve/quarantine/refuse upgrade. Atomically activate v2 after all callers/recovery adapters support it; enforce new-row owner/request constraints/old-writer rejection. Migrate V3 and lost-owner expectations, execute full phase suites and adversarial stale/partition review. Logging: activation version, unresolved old-row count, exact owner cases/gate outcome. Acceptance: every D2 variant executed; no indefinite old prompt path or successful upgrade with unprovable active requests.

- [x] **S2.13 — S2 documentation truth pass (user-required gate item, 2026-09-08).** Owner D/Q; Depends: S2.12 and a green S2 gate. After every S2 task is complete and the gate is fully checked, re-read the ADRs, `docs/system-analytics/`, the architecture prose and the component/interaction diagrams AGAINST the implementation and correct every description that no longer matches: component responsibilities, who owns which write, the manager↔host interaction sequences, the prompt-owner and retirement protocols, and any status label still reading Designed for something now Implemented. This is a verification pass over what the code actually does, not a summary of what the S2 commits claimed; a doc that merely sounds right is not evidence. It is separate from and additional to the S5.4 audit, which covers the whole Stage A/B surface. **Scope extension (user, 2026-09-08): the published documentation site under `site-docs/` is in scope too**, both language trees (EN and `ru/`), so operator-facing pages describing the supervisor, execution hosts, deployment and troubleshooting are checked for the same compliance — a public page that describes a boundary the code no longer has is the same defect with a wider audience.

S2 acceptance: AB-05–08/10 green across all real owner origins and status windows; create binding and terminal authority have one enforcing path. Additive commits may deploy with activation disabled before the completed S2 release; they do not advertise repaired owner semantics. The transition window is one staged upgrade with admissions drained, not an indefinite runtime fallback. Rollback before activation can retain additive fields; after v2 requests/owner state exist, keep v2-compatible binaries and roll forward. Never discard protected requests/receipts to make downgrade work.

### S3 — Runtime-object reconciliation, security, integrity and retention

- [x] **S3.1 — Reconcile intent, seal and availability (AT-09).** Owner O; Depends: S1.8, S2.2. Files: `execution-host/{client,runtime-objects}.ts`, `events/runtime-object-projector.ts`, schema/new forward migration if needed, host registry. RED ACK/event order including released/superseded binding; GREEN separate declared/established metadata and one monotonic reducer with historical-intent fence. REFACTOR duplicate equality predicates. Logging: object/generation/binding/evidence source and conflict reason. Acceptance: valid pending-before-ACK never poisons; stale association cannot mutate successor; tombstone cannot resurrect.

- [x] **S3.2 — Immutable seal and read verification (AT-13).** Owner O; Depends: S3.1. Files: `supervisor/src/{runtime-objects,host-state,http-api}.ts`, read/seal fixture tests. RED same-size tamper/unlink/symlink/concurrent read mutation and interrupted seal. GREEN distinct immutable segments, descriptor identity, bounded full-hash/range spool and durable missing/corrupt evidence. REFACTOR file-descriptor lifecycle helpers. Logging: object/generation/expected-observed sizes/hash agreement and typed failure, no pathname or bytes. Acceptance: no verified success from altered source and no descriptor/spool leak; response independent of projector availability.

- [x] **S3.3 — Correct range/message integrity end to end (AT-15).** Owner O/Q; Depends: S3.2. Files: supervisor HTTP, `web/lib/supervisor-client.ts`, `execution-host/runtime-objects.ts`, web object proxy/range helpers, existing host runtime-object integration. RED actual 206 slice hash versus whole digest; GREEN separate Content-Digest/Repr-Digest/ETag and bounded manager response verification. REFACTOR shared strict header parsing. Logging: range/size/generation/digest match and transfer outcome. Acceptance: tests compute bytes independently; invalid/multiple ranges stay refused, no compression or full-body fallback.

- [x] **S3.4 — Safe download headers and authorization (AT-12).** Owner O/Q; Depends: S0.4; integrate with S3.3. Files: `web/app/api/runs/[runId]/runtime-objects/[objectId]/content/route.ts`, artifact payload route, scratch content callers, host content route, new browser scenario. RED HTML/SVG script sentinel and denied/positive raw-content grants; GREEN D5 attachment/nosniff/CSP/cache/filename policy and response-class auth; preserve valid ordinary download behavior. REFACTOR one header policy per service. Logging: media class/policy/authorized IDs and refusal, never original unsafe filename/header. Acceptance: real browser cannot execute uploaded active document in MAIster origin.

- [x] **S3.5 — Reconcile seal/delete gaps and partial resources.** Owner O; Depends: S3.1–S3.3, S2.11. Files: host runtime-object startup recovery, manager client/recovery and new object lifecycle tests. RED seal→receipt gap, tombstone→unlink gap, ACK loss and delayed old-generation availability. GREEN exact-intent repair through existing command/receipt path, private temp cleanup and no-resurrection reducer. REFACTOR one object evidence reducer. Logging: recovered object/command/generation, phase and orphan disposition. Acceptance: no required bytes removed or available state invented after uncertain transfer.

- [ ] **S3.6 — Fair GC with delivery/reference holds (AT-14).** Owner O; Depends: S3.5, S2.11. Files: `runtime-object-retention.ts`, existing delivery/artifact readiness/retention predicates, scheduler sweep and PG integration suite, new index/progress migration if needed. RED protected first page, later eligible rows, reference-vs-delete racer and Done/open-PR required evidence. GREEN D5 keyset progression, shared object lock, durable delete claim and exact delivery confirmation holds. REFACTOR concern-specific eligibility predicate. Logging: examined/eligible/deferred/deleted counts, protected reason and cursor age. Acceptance: every later eligible object reached, no deletion based solely on Done or expired timestamp.

- [ ] **S3.7 — Object deployment and assertion migration.** Owner D/O/Q; Depends: S3.2–S3.6. Files: config/host env/Docker/compose parity surfaces, object/read limits, existing `runtime-objects.test.ts`, host runtime-object and retention integration suites, response consumers. Migrate whole-digest-on-206, metadata-only integrity, inline MIME and first-page assumptions explicitly. RED/GREEN/REFACTOR reduced test budgets follow normative ratios. Logging: validated object/spool quota and scenario gate IDs. Acceptance: deployment settings symmetric, browser/file consumers use safe path, every promised test discovered.

- [ ] **S3.8 — S3 specification and integration gate.** Owner D/Q; Depends: S3.7. Files: runtime-object and artifact contracts, error taxonomy, database/ERD, capabilities/evidence/retention analytics and this matrix. Run full phase suites plus real browser/content tests and adversarial read/delete/event-order review. Logging: revision/scenario/runner/outcome. Acceptance: AB-09/12–15 green, required evidence remains distinct from result transport, default one-host artifact flow works.

S3 rollback: retain catalog, immutable generations, private files and receipts. New header/digest contract requires compatible proxy/host as a pair; never downgrade to unsafe inline MIME. Before activating stricter object writes, additive catalog changes are backward-compatible; after activation use roll-forward repair. Temporarily pause GC on rollback/uncertain retention; do not relax delivery holds.

### S4 — Full historical import and guarded upgrade

- [ ] **S4.1 — Staged migration/maintenance preflight.** Owner M; Depends: S3.8. Files: `web/lib/db/migrate.ts`, migration-root/check helpers, importer entrypoint, operator CLI specification/tests. RED fixture at main 0130 must stop after 0133 without running 0134/0135; active legacy work and invalid ledger high-water refuse. GREEN explicit stage subcommands and maintenance/admission/GC drain fence using existing control boundaries. REFACTOR existing filtered migration-root helper. Logging: migration tags/hashes, stage/import ID and unresolved counts. Acceptance: no new >0136 schema needed pre-cutover; committed guards/hashes unchanged.

- [ ] **S4.2 — Inventory and durable source manifest.** Owner M; Depends: S4.1. Files: importer split into bounded inventory/mapping functions under `web/scripts/`, host-private import ledger/schema and fixtures. RED ordinary logs/attachments/nested evidence/duplicate refs/unknown sources and source change; GREEN D9 paged complete inventory with source and row fingerprints, five lanes, exact baseline attribution and inspected-empty proof. REFACTOR deterministic mapping only. Logging: lane/source opaque ID, size/count/hash state, missing/conflict reason. Acceptance: all files and associations accounted for; no blanket allowlist or zero-object success.

- [ ] **S4.3 — Authorized chunk import and seal.** Owner M/O; Depends: S4.2, S3.8. Files: new `supervisor/src/import-admin.ts` maintenance socket listener, supervisor boot/config, `ExecutionHostsAdmin` contract/transport, host-state import progress and operator CLI. RED denied normal-web socket access, disabled/revoked admission, >25 MiB source, chunk duplicate/conflict, source changes, ACK loss and restart; GREEN the D9 maintenance protocol, manifest-bound ≤8 MiB transfer, resume/checksums, sealed segments/full representation and ordinary object readback. REFACTOR reuse object seal/digest primitives. Logging: import/item/chunk IDs, offset/bytes/retry/outcome. Acceptance: no arbitrary path endpoint, no active-assignment forgery, no whole-history buffering, no source mutation/deletion.

- [ ] **S4.4 — Locator and association preservation.** Owner M; Depends: S4.3. Files: importer association writers, artifacts/scratch attachment DTO/locator readers, existing canonical incarnation helpers, import PG tests. RED copy-success→DB-death, concurrent source-row edit and provable multi-assignment scratch history; GREEN exact old-row CAS, preserve validity/kind/attempt/message scope, unique historical session mapping and audited mirror migration protocol. REFACTOR pure source-to-association mapping. Logging: old/new opaque locator identity, association digest/count and refusal. Acceptance: all original associations retained; ambiguous mapping blocks; no deleting assignments or unproven mirror clearing.

- [ ] **S4.5 — Per-lane verification and guarded final proof.** Owner M; Depends: S4.4. Files: importer verify/finalize subcommands, existing import lanes, migration integration suites. RED missing byte/hash mismatch/changed already-complete source/incorrect post-0134 scratch count; GREEN full streamed readback, original-manifest association comparison and durable counts/positions before complete. Run unmodified 0134 separately then reverify, then permit unmodified 0135/0136. REFACTOR proof reducer only. Logging: lane expected/verified counts+bytes and proof version, no raw content. Acceptance: unchanged 0135 preflight succeeds because data is preserved; false proof or missing lane refuses.

- [ ] **S4.6 — Complete baseline upgrade, interruption and restore tests (AT-11).** Owner M/Q; Depends: S4.5. Files: `web/scripts/__tests__/import-legacy-execution-data-plane.integration.test.ts`, migration-0135 suite, real Stage A history fixtures/harness. Replace the current removal of `plan.log` before success with copy/hash/readback; include ordinary empty and >25 MiB logs, transcripts/cost, file evidence, uploads, multiple sessions/checkpoints and optional discovered metadata files. RED/GREEN/REFACTOR actual CLI execution at every D9 crash boundary. Logging: fixture/import/restart stage/gate and byte/hash comparisons. Acceptance: complete positive migration with sources untouched, partial resume, duplicates, changed/missing source refusal, fresh install and restore of the full snapshot set.

- [ ] **S4.7 — Forward schema and stale-writer/cutover guard.** Owner D/M; Depends: S4.6. Files: `schema.ts`, new migration SQL+journal+snapshot triple(s), generated ERD, migration/boot compatibility checks. Generate forward corrections and mandatory operational constraints; do not edit 0131–0136. Reconcile main lineage and only uncommitted identifiers, enforce startup capability/schema floor and DB old-writer rejection for mandatory new invariant writes. RED old writer and malformed owner/import transition; GREEN guards and orphan reconciliation. Logging: version/constraint/migration hash and rejected writer class. Acceptance: exact current migration chain applies from main and fresh DB, snapshot continuity, `db:generate` produces no changes; no silent loss on already-canonical repair path.

- [ ] **S4.8 — Upgrade/deployment and S4 gate.** Owner D/M/Q; Depends: S4.7. Files: `docs/{deployment,configuration,getting-started,supervisor,database-schema}.md`, execution cutover analytics/ERD, env/import-mode wiring, A/B plan as-built map and this plan. Document and execute staging, backups, drain, copy, verify, guarded cutover, mount removal and import-authority revocation on disposable installation. Run full phase gates and adversarial migration review. Logging: stage/proof/gate IDs and preserved/unresolved counts. Acceptance: AB-11 green; no ordinary log deletion, no fake proof, default one-host install preserved.

S4 rollback: before 0134, keep sources and all import checkpoints; stop maintenance importer and restore/restart compatible Stage A only from the consistent pre-upgrade snapshot after confirming no new effects escaped drain. After destructive 0134/0135, default is forward repair; a downgrade requires restoring Postgres **and** host/source/adapter snapshots as one fenced installation, never merely reverting code. Keep post-backup effects quarantined for reconciliation; do not apply older state over live new producers. Sources/backups remain until qualification and retention permit removal.

### S5 — Isolated lifecycle qualification and final consistency

- [x] **S5.1 — Enforce operation-scoped filesystem ownership.** Owner Q; Depends: S0.4 (can develop earlier); final Depends: S4.8. Files: existing runtime-data boundary inventory/test, operation scanner helpers, scripts/instrumentation classification. RED prohibited host-runtime read injected into an already allowed mixed file and a previously omitted `.tsx`/script root. GREEN D10 function/callsite inventory plus unresolved-call refusal; REFACTOR stable AST extraction only. Logging: source/function/operation class and violation code, no runtime path data. Acceptance: no blanket filename exemptions; migration-only reads have explicit lifetime/authority.

- [ ] **S5.2 — Real process death, partitions and denied roots (AT-16).** Owner Q; Depends: S1–S4 gates. Files: `web/test-support/real-supervisor.ts`, new real web/network/isolation harness, `web/test-support/pg-container.ts` integration usage, lifecycle tests. RED old implementation fails denied-root negative control/restart continuity; GREEN real startup/worker/owners complete acceptance matrix under process kill/restart and long partition. REFACTOR reusable setup/teardown, no production test flags. Logging: PID group/runtime/root-role/boot/case/outcome and safe traces. Acceptance: both roots disjoint and web access actually denied; no shared-dev DB/port, orphan process, or mock-supervisor substitution.

- [ ] **S5.3 — Browser lifecycle and runtime/transport matrix.** Owner Q; Depends: S5.2 and S3 browser policy. Files: new dedicated Playwright real-supervisor config, lifecycle/content specs, CI/scripts, minimum Node/image qualification. Exercise launch→stream→HITL→checkpoint→resume→cancel/completion/history, each owner-visible reply and active MIME download after restarts. Run AT-17 on minimum 24.15, 24.19 and selected image with exact binary/dependency versions. Logging: scenario/revision/runtime/image/transport phase and result. Acceptance: browser outcomes come from real web/supervisor; default fake-peer E2E remains supporting regression only; no full-Node-24 claim from one passing patch.

- [ ] **S5.4 — Final specification, lineage and acceptance gate.** Owner D/Q; Depends: S5.1–S5.3. Files: all affected canonical docs/analytics/API/ERD/error/config surfaces and this plan; no new summary-report deliverable. Recheck current main/HEAD; revalidate changed findings, migration order/constraints/snapshot hashes and ADR anchors, including prose provisional numbering. Run full suites/gates, verify every AT row with actual runner/case/artifact evidence, and adversarial review of final fix cycles. Logging: exact gate/ref/scenario/evidence state and unresolved risk IDs. Acceptance: all AB-01–17 and final matrix rows satisfied; no known A/B defect remains hidden by quarantine or Implemented prose; Stage C is still unimplemented.

S5 rollback: qualification-only changes can revert independently, but evidence cannot be treated as passing after reverting a tested fix. Runtime state retains S1–S4 recovery/retention guarantees. Release only a commit whose concrete scenario matrix and specification gates correspond to that exact revision.

## Commit sequence and dependency order

Every checkpoint contains 3–4 focused tasks. Commits within an increment may be additive or dormant until its activation task; **independently deployable release phases are S0, S1, S2, S3, S4, S5**, each with its own gate/rollback above. Do not deploy a half-activated owner or partial destructive migration sequence. No push/merge/deployment is authorized by this planning request.

| Checkpoint | Tasks | Suggested conventional commit | Deployment condition |
| --- | --- | --- | --- |
| C01 | S0.1–S0.4 | `docs(execution-host): specify A/B stabilization contracts` | Docs-only. |
| C02 | S1.1–S1.4 | `fix(execution-host): bound output and preserve projection failures` | Additive/inactive capability changes; validate existing one-host behavior. |
| C03 | S1.5–S1.8 | `fix(execution-host): drain canonical projections durably` | S1 release, full gate/runtime wiring. |
| C04 | S2.1–S2.4 | `fix(execution-host): reconcile command evidence and fence bindings` | Additive owner-v2 storage remains unadvertised until all owners wired. |
| C05 | S2.5, S2.6, S2.8, S2.9 | `feat(execution-host): persist prompt owner continuations` | Domain adapters paired with their callers; no partial global activation. |
| C06 | S2.7, S2.10–S2.12 | `fix(execution-host): complete owner recovery and safe retirement` | Drained S2 activation, all variants/full gate. |
| C07 | S3.1–S3.4 | `fix(runtime-objects): reconcile seals and secure verified reads` | Host/proxy protocol deployed as compatible pair. |
| C08 | S3.5–S3.8 | `fix(runtime-objects): reconcile deletion and preserve delivery holds` | S3 release; GC enabled only after eligibility proof. |
| C09 | S4.1–S4.4 | `feat(execution-host): import complete historical runtime bytes` | Maintenance tooling only; destructive stages remain guarded. |
| C10 | S4.5–S4.8 | `fix(execution-host): prove historical preservation before cutover` | S4 release; operator performs reviewed staged procedure. |
| C11 | S5.1–S5.4 | `test(execution-host): qualify isolated A/B lifecycle recovery` | Final stabilized release after exact-revision evidence. |

Hard order: S0 → S1 → S2 → S3 → S4 → S5. Permitted preparation after S0: S1.1 binary transport and S1.4 consumer storage can proceed independently; S3.4 safe-header implementation can proceed against the frozen API while S1/S2 develop; S5.1 scanner and the isolated harness can be prepared early. S3.1 may be developed after S1 and the S2 reducer contract but integrated only after command evidence semantics stabilize. S2.6, S2.8, S2.9 and S2.10 own disjoint domains after S2.5; S2.7 coordinates its draft section with S2.8. Shared `http-api.ts`, `host-state.ts`, `schema.ts`, transport and contract documents have one designated integrator and sequential merges. No simultaneous migration-number allocation.

## Documentation/contract delivery matrix

S0 writes the full Designed contract. Each listed increment updates its own as-built state and test links in the same commit group; S5 only closes remaining consistency gaps. API parser success is necessary but not behavioral proof.

| Surface | Canonical artifacts | Increment / enforcing tests |
| --- | --- | --- |
| Output summaries, budgets, ACK/projection and degraded worker | `docs/api/async/execution-host-events.asyncapi.yaml`, `supervisor-sse.asyncapi.yaml`, `web-runs.asyncapi.yaml`; supervisor OpenAPI capability/error schemas; `execution-event-plane.md` | S1; AT-01–04/17. |
| Async prompts, receipt/request identity, unknown outcome, owner continuation, retirement | `docs/api/supervisor.openapi.yaml`, affected web response schemas, host AsyncAPI; `execution-prompt-lifecycle.md`, `execution-hosts.md` | S2; AT-05–08/10. |
| Runtime-object intent/seal/state/download/range/delete | Both OpenAPI specs, host/browser event schemas; `execution-runtime-objects.md`, `artifacts.md`, `capabilities.md` | Implemented: native intent/seal convergence (S3.1, AT-09; 13 real lifecycle cases), host seal/read verification (S3.2, AT-13; 18 real host cases), manager range/body integrity (S3.3, AT-15; real HTTP, Postgres/host and Chromium cases hash bytes independently) and safe downloads (S3.4, AT-12). Crash-gap and retention corrections remain Designed (S3.5–S3.8, AT-14). |
| Error codes/reasons/status mapping and user actions | `docs/error-taxonomy.md`, wire schemas, `web/lib/errors-core.ts` and supervisor types; affected EN/RU message catalogs | Each behavior increment; exact typed error and safe-display assertions. |
| New fields/constraints/indexes and operational ledgers | `web/lib/db/schema.ts`, new SQL/journal/snapshot; `docs/database-schema.md`, `docs/db/{execution-hosts-domain,artifacts-domain,runs-domain}.md`, generated `erd.dbml` | S1/S2/S3/S4 as needed; real PG constraint/race/migration tests; no second Flow ledger. |
| Owner status windows and wake/cap behavior | `docs/system-analytics/{runs,sessions,scratch-runs,hitl,reconciliation-gc,agents,branch-sync,flow-graph,artifacts,capabilities}.md`; `consensus.md`, `studio-ai-assistant.md` and gate-chat sections of sessions/HITL analytics where actual callers change | S2/S3; per-origin AT-05 and stale/parked tests. Use existing domain files/sections rather than creating duplicate analytics. |
| Historical source preservation and cutover | `execution-data-cutover.md`, `deployment.md`, `getting-started.md`, `supervisor.md`; CLI/script inventory in root/web instructions | S4; AT-11 and unchanged 0134–0136 guard tests. |
| Architecture and instructions | `docs/architecture.md`, `.ai-factory/ARCHITECTURE.md`, root/web AGENTS/CLAUDE and applicable backend rules through owner workflow | S0 Designed and S1–S5 verified slices; remove stale file/ring/long-HTTP authority descriptions only where this work proves replacement. |
| Runtime/config/image/deployment | `docs/configuration.md` canonical table; `.env.example`, supervisor/deploy env, units, Dockerfile, compose pair, CI | S1/S3/S4; invalid config + set/remove/reset, real minimum/image runtime and rendered topology. |
| ADR status/decisions | `docs/decisions.md` hub + existing record amendment/status, or new record only if direction changes | Allocate at implementation after checking main; S5 anchor/bijection verification. Do not preassign a number here. |

## Final acceptance matrix

All rows start **Planned / unverified**. Implementation fills exact test case, runner/runtime/revision and artifact reference; a source link or fake happy path does not change status to Passed.

| Scenario | Primary test(s) | Mandatory distinguishing observation | Release gate |
| --- | --- | --- | --- |
| Oversized/multibyte ACP output with sibling | AT-01 | Supervisor survives; sibling progresses; full required output object or typed incomplete-output outcome, continuous committed sequences | S1 |
| Retained-only and unACKed-only pressure | AT-02 | Correct admission refusal, bounded memory/disk, real producer pause/resume, credited terminal/cancel/checkpoint/exit progress | S1 |
| >2-batch web restart with no new event | AT-03 | Terminal after event 200 and all cursors advance autonomously through actual startup worker | S1/S5 |
| Last-event transient retry and multi-run gap | AT-03 | Due retry serviced without new ingest; every run promoted by gap fill is reached fairly | S1 |
| First consumer failure and stale recorder | AT-04 | Original error durable on first attempt; independent consumer progresses; later recorder cannot poison advanced cursor | S1 |
| Each prompt origin before terminal and before owner apply | AT-05 variants | One ACP action and exactly one correct domain output/transition for **every D2 row**, including gate/consensus subvariants | S2/S5 |
| Receipt first, canonical first, nested errors, genuine mismatch | AT-06 | Only agreeing canonical+receipt terminal applies; exact nested identity retained; genuine mismatch quarantined | S2 |
| ACK/receipt/SSE partitions beyond budgets | AT-07 | Unknown remains recoverable through web restart; reconnect yields one correct result and no duplicate prompt | S2/S5 |
| Delayed old create ACK/recovery/ACP ID setter | AT-08 | N evidence settles historically; every N+1 binding/owner field unchanged | S2/S5 |
| Event-before-ACK object; release/supersession | AT-09 | Available once for exact intent, never wrong owner or null-predicate poison | S3 |
| Aged accepted/unknown/unapplied commands | AT-10 | No premature prune; actual host restart emits turn_lost where warranted; eligible compact tombstone rejects replay effects | S2 |
| >1-page object GC and reference/delete race | AT-14 | Later eligible rows deleted; live/required/open-PR/Done-unconfirmed evidence retained | S3 |
| Unsafe HTML/SVG and safe filename/grant | AT-12 | No script or authenticated side effect in real browser; authorized valid file still downloads; foreign/raw-content denial holds | S3/S5 |
| Post-seal tamper/truncate/unlink/symlink/race | AT-13 | No stale-digest success; actual host missing/corrupt record/event reaches catalog even after restart | S3 |
| Full vs partial digest | AT-15 | Independently hashed slice Content-Digest and full Repr-Digest semantics, exact body and 416 refusal | S3 |
| Full Stage A history incl >25 MiB logs | AT-11 | Original sources untouched, byte/hash/message/cost/artifact/session association equality through actual guarded migrations and mount removal | S4/S5 |
| Import partial failure/resume/duplicates/source changes | AT-11 | Durable per-item/lane positions; identical replay no-op; changed/missing/ambiguous source blocks destruction | S4 |
| Consistent backup/restore and old-writer refusal | AT-11, forward migration tests | Full multi-store restore works; old writers cannot create new invalid state; no manual SQL hash edits | S4 |
| Real supervisor restart | AT-05/07/10/13/16 | Persistent host key/stream/receipts/objects; accepted lost turn explicit, no repeat ACP; owner state recovers | S5 |
| Real web restart and simultaneous worker claims | AT-03/05/16 | Boot through production initialization, fair due work, no duplicate apply, no process-local continuation dependency | S5 |
| Web runtime-root access denied | AT-16 | Negative control EACCES/ENOENT, supervisor positive control, lifecycle/history/object access succeeds through HTTP/PG | S5 |
| V7b minimum runtime and image | AT-17 | Node 24.15.0 and 24.19.0 exact versions plus pinned image pass real native-stack replacement; diagnosed invalid-content-length captured in RED, not excused by a newer pass | S1/S5 |
| Single-host lifecycle regression | Real AT-16 + browser lane | Launch, replay, HITL, idle checkpoint/resume, cancellation, completion, history and artifact download remain coherent without Stage C features | Every release/S5 |

## Risk register and completion conditions

| Risk | Impact / mitigation | Owner / blocking criterion |
| --- | --- | --- |
| K01 Shared ACP stdout cannot prioritize control while paused | Bound framing/SDK queues; wallet-backed independent checkpoint/delete; explicitly pending turn-only cancel | E; no S1 pass without real saturated-pipe test. |
| K02 Logical quotas understate SQLite/WAL/receipt disk | Measure physical amplification/in-flight bound and free headroom; refuse/pause, never delete protected evidence | E; failed physical bound or non-durable claimed terminal blocks release. |
| K03 Immutable request/output incomplete or secret-bearing | Exact private canonical request, source-held credential refs, command-bound verified output; old-row drain/quarantine | C; no invented digest/output or unsafe DTO/log exposure. |
| K04 Hidden prompt origin or side-effect continuation | Enumerate all callers/subvariants and status×mode windows; exhaustive registry/type checks and one real case each | C; missing variant/parked arm blocks S2. |
| K05 Old async callback bypasses central fence | Grep all session/ACP-handle/current-owner writes, require applied/stale result, race live and recovery callers | C; any successor mutation blocks S2. |
| K06 Retention and new references race | Shared object-generation lock, state-aware receipt protocol, delivery holds, no Done shortcut | O/C; premature deletion or forgotten idempotency key blocks release. |
| K07 Full-hash read overhead | Bounded concurrency/range spools and load qualification; no weakening integrity into metadata-only check | O; unbounded resources or unverified success blocks S3. |
| K08 Historical sources/associations missing or changed | Frozen inventory, source/row fingerprints, backups, loud refusal and explicit mapping repair | M; no destructive cutover with unresolved item, no source deletion to manufacture proof. |
| K09 Forward migration needed before old destructive chain | Compatibility CLI uses existing 0131 lanes + host manifest; staged migrator through 0133/0134; new SQL only after combined chain | M/D; fresh and Stage A upgrade must both execute successfully. |
| K10 Version/engine/contract skew | Explicit 24.15 floor, pinned Undici/image, capability activation after consumers ready, stale-writer backstop | Q/D; newer-patch-only pass or mixed incompatible peer blocks activation. |
| K11 Flaky/shared-state tests mask defects | Disposable PG/ports/roots, actual processes, exact failure sets, nonempty discovery, no A/B quarantine | Q; absent/unexecuted critical case is a failed gate. |
| K12 Scope growth into Stage C/D or new workflow engine | Keep repository/Git owners and existing domain reducers; operational claim/receipt/import state only | D; unrelated ownership/placement/auth/relay work removed from this delivery. |

Completion requires every finding's primary test and final matrix row to pass at the release revision, complete owner/refusal/recovery tables to match enforcing code/constraints, all required suites/spec/lineage gates green with honest failure-set handling, and operator upgrade/rollback instructions verified on real preserved histories. Review each fix cycle adversarially, especially event ordering, late success, activation and destructive cleanup. Stage C planning begins only after this A/B prerequisite is accepted; no implementation is performed by this plan.
