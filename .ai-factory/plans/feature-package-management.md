# Implementation Plan: Package management P0–P2 (extraction cleanup + packages[] import + platform catalog)

Branch: feature/package-management
Created: 2026-06-12
Design: `docs/pv/package-management.md` (owner-approved 2026-06-12). Supersedes the deferred `.ai-factory/PLAN.md` single-import fast plan.
Pre-done: the `maister-plugins` repo is already filled and tagged — `packages/aif` @ `aif/v2.0.0` (commit 804f79f), manifests validated through the maister loader (10/10).

## Settings

- Testing: yes — TDD (unit `*.test.ts`, integration `*.integration.test.ts` testcontainers, e2e playwright where UI is touched)
- Logging: verbose — pino DEBUG at decision points, INFO at lifecycle events, WARN on degradations; never log secrets or full env values
- Docs: yes — mandatory docs checkpoint; **Phase 0 is analytics-first** (skill-context rule): system-analytics + ERD + OpenAPI land BEFORE code phases, tagged (Designed), flipped to (Implemented) at close-out

## Roadmap Linkage

Milestone: "M33. Package management" (new — added to ROADMAP.md by T0.7)
Rationale: packages are the distribution unit for delivery processes; M33 unblocks sane dogfood wiring (M20) and external installs.

## Conventions (gates)

- Typecheck: `pnpm --filter maister-web typecheck` · Unit: `pnpm --filter maister-web test:unit` · Integration: `pnpm --filter maister-web test:integration` (testcontainers) · E2E: `pnpm --filter maister-web test:e2e` (kill ports 3100/7788 first; shared e2e DB — never `--last-failed`)
- Lint: scoped ONLY — `pnpm exec eslint <paths>` (repo-wide `pnpm --filter maister-web lint` runs `--fix` and reformats ~60 files — forbidden)
- Docs: `pnpm validate:docs` (changed) / `pnpm validate:docs:all`; OpenAPI: `npx @redocly/cli lint docs/api/web.openapi.yaml`
- Errors: `MaisterError` with code only (`CONFIG | FLOW_INSTALL | PRECONDITION | CONFLICT` cover this milestone — no new codes)
- Migrations: `pnpm db:generate` only (never hand-write SQL next to a stale snapshot — drizzle `--custom` gotcha); update `fakeDb` stubs for every new table
- Worktree cwd trap: Bash `cd` resets — always absolute paths under `.claude/worktrees/charming-cohen-f27a60/`
- Surgical changes; no `any`; atomic writes via `@/lib/atomic`

## Key decisions (baked, do not re-derive)

1. **Field name `packages[]`** in `maister.yaml` (user-renamed from `flow_packages[]`): `{id, source, version, path?}`. Additive — `flows[]`/`capability_imports[]` untouched.
2. **New module dir `web/lib/packages/`** (`manifest.ts`, `install.ts`, P2: `catalog.ts`, `attach.ts`) — keeps `flows.ts` focused.
3. **Revision inheritance**: `installFlowPlugin`/`installRevision` (web/lib/flows.ts:999/594) and `installCapabilityRevision` (web/lib/capabilities/import.ts:399) gain optional `resolvedRevisionOverride`. Package install computes ONE revision — git: `rev-parse HEAD` of the tag clone; local: `localDirectoryContentDigest(pkgRoot)` (flows.ts:407) — and passes it to every sub-install. Fixes the capability local-rev asymmetry (today: hash of source STRING, import.ts:58–65) and keeps `runs.flow_revision` pinning immutable for git packages.
4. **P0 test fixtures = v1-verbatim snapshot** at `web/test-fixtures/aif-flows/` (the 5 flow dirs + schemas only, ~30 KB; NO capability bundle; provenance README pointing at maister-plugins). Tests exercising engine behavior keep passing unchanged; the package contract guard for v2 lives in maister-plugins (P3 brief). No assertion churn.
5. **Discovery** = `git ls-remote --tags <url>` (filter `<name>/v*`) + depth-1 default-branch clone to tmp for `packages/*/maister-package.yaml` scan; result cached as jsonb on the source row + `last_checked_at`; per-source failure degrades to stale cache + WARN, never blocks the page. NO new scheduler job kind (avoids the 4-point kind-registration fan-out) — startup debounce in `web/instrumentation.ts` (staleness > `MAISTER_PACKAGE_DISCOVERY_STALE_HOURS`, env int hours, default 24, zod-parsed once) + manual refresh button.
6. **Attach atomicity** (multi-store rule): install/intent rows happen BEFORE (idempotent, content-addressed); the attach itself = ONE `db.transaction` writing flows rows + capability rows + `project_package_attachments` + FK group links; `maister.yaml` write-back happens AFTER commit (fs side-effect, best-effort: failure → WARN + UI notice, DB stays truth). Crash windows: (a) install done / attach tx not committed → revision orphan, harmless, GC'able; (b) tx committed / yaml not written → stale yaml, healed by next attach/detach/upgrade write-back or manual edit. Detach = symmetric single tx behind a live-run guard (PRECONDITION if any non-terminal run pins a member revision).
7. **Package-level trust**: one operator decision per package revision; the same tx fans `trust_status`/`exec_trust` onto member `flow_revisions` + `capability_imports` rows. `setup.sh` NEVER runs at install — only via the existing `runRevisionSetup`/`trustExecutable` path after the trust flip (physical fetch≠execute separation preserved, flows.ts:727, import.ts:394).
8. **Typed ingestion on attach**: package manifest inventory (skills/agents walked at install into `package_installs.manifest`); `mcps[]` templates → project MCP catalog entries flagged `package-provided` (env values `env:NAME` refs only), removed on detach; `restrictions[]` (manifest v1 section `{id, paths[]}`) → `capability_records` kind=`restriction` with `material.paths` per attached project (unlocks `must_not_touch` → aif v2.1.0, out of this plan).
9. **yaml write-back is comment-preserving**: parse with `yaml` Document API, mutate only `packages[]`, serialize, `atomicWriteText` (web/lib/atomic.ts). The dirty file is the user's to commit.
10. **Local package versions are first-class**: install from a local dir → version label `local-<digest12>`, catalog-marked `local`, attachable like any version. UI local-path attach is `manageCatalog`/admin-gated; file:// has no path allowlist today (existing posture for flows[]) — documented boundary, not widened silently.
11. **Numbers**: ADR-**087**, migration **0047** (main = f34769fd owns ADR-086 + 0046). T0.1 rebases first so numbering is taken against live main; later sibling merges may force the standard renumber dance (journal `when` + snapshot transplant gotchas).
12. **Identifiers** (trust-boundary rule) for every new route: `slug` = url-param → project row; `sourceId`/`packageInstallId` = body ids resolved to server rows (404 on miss, project-scoped joins); `localPath` (admin attach-local only) = body-controlled abs path, zod abs-path shape + role gate, used only as fs read root for install (same trust class as registration's `path`). NO body-controlled value ever composes a cache/symlink path — those derive from server-validated `flowRefId`/`resolvedRevision` as today.
13. **Deployment wiring = exactly one env var** (owner decision 2026-06-12): `MAISTER_PACKAGE_DISCOVERY_STALE_HOURS` (default 24) → `.env.example` committed default + web service `environment:` block in `compose.yml` (+ `compose.production.yml` passthrough if needed; the repo has NO `compose.override.yml`) + `docs/configuration.md` env table — dedicated task T3.11 per skill-context rule. No new ports, sidecars, binaries, or webhook event types (package lifecycle events = future work).

## Contract surfaces (traced per skill-context rule)

| Surface | Spec file |
| --- | --- |
| `maister.yaml` `packages[]` + `maister-package.yaml` contract | `docs/configuration.md` (+ design doc `docs/pv/package-management.md`) |
| New tables `package_sources`, `package_installs`, `project_package_attachments`, FK cols on `flows`/`capability_imports` | migration `0047` + `docs/database-schema.md` + `docs/db/projects-domain.md` + `docs/db/erd.md` |
| Admin routes `/api/admin/package-sources*` (CRUD/refresh/install) + project routes `/api/projects/{slug}/packages*` (attach/detach/upgrade/trust) | `docs/api/web.openapi.yaml` + `docs/system-analytics/packages.md` |
| Domain doc (entities, FSMs, flows, expectations, edge cases) | `docs/system-analytics/packages.md` (new, R5) + surgical cross-refs in `flow-packages.md`/`flows.md` |
| ADR | `docs/decisions.md` ADR-087 |
| aif plugin doc | `docs/flow-aif-plugin.md` (extraction rewrite) |
| `MAISTER_PACKAGE_DISCOVERY_STALE_HOURS` env var | `docs/configuration.md` env table + `.env.example` + `compose.yml` (T3.11) |
| Error codes | none added — reuse `CONFIG/FLOW_INSTALL/PRECONDITION/CONFLICT` (no `error-taxonomy.md` change) |

## Commit Plan

- **Commit 1** (T0.1–T0.7): `docs(packages): ADR-087 + analytics/ERD/OpenAPI design pass for package management`
- **Commit 2** (T1.1–T1.3): `chore(aif): extract plugins/aif to maister-plugins — fixtures, dogfood rewire, docs`
- **Commit 3** (T2.1–T2.3): `feat(packages): package manifest schema + packages[] config + revision-override seam`
- **Commit 4** (T2.4–T2.6): `feat(packages): installPackage orchestrator + registration expansion + dogfood single-import`
- **Commit 5** (T3.1–T3.3): `feat(packages): platform catalog schema + sources/discovery + attach/detach/upgrade services`
- **Commit 6** (T3.4–T3.6): `feat(packages): typed ingestion + yaml write-back + admin/project API routes`
- **Commit 7** (T3.7–T3.11): `feat(packages): settings + project UI, startup discovery + env wiring, e2e`
- **Commit 8** (T4.1–T4.2): `docs(packages): flip to Implemented + close-out gates`

## Tasks

### Phase 0: Rebase + analytics-first design pass (no code)

- [x] **T0.1 Rebase onto live main.** `git rebase main` (f34769fd; our 3 commits are docs-only — expect clean). Baseline-prove: typecheck + unit green post-rebase before any new work. Logging: n/a.
- [x] **T0.2 ADR-087 — Package management.** `docs/decisions.md`: multi-flow packages from git monorepos (`maister-package.yaml`, per-package tags `<name>/vX.Y.Z`), two-scope platform-install/project-attach, DB+UI source of truth with declarative `maister.yaml packages[]` bootstrap + write-back, local versions, package-level trust fan-out. Annotate ADR-021 (F3 stance superseded for the package case). One ADR, immutable numbering caveat noted.
- [x] **T0.3 Domain doc `docs/system-analytics/packages.md`** (R5 structure): entities (package source / discovered package / package install / attachment / local version); state machines (`package_installs.package_status` reuse of Discovered→Installing→Installed→Failed→Removed; attachment lifecycle attach→update-available→upgraded→detached); process flows (discovery, install, attach incl. tx boundary + yaml write-back, detach guard, upgrade, trust fan-out, local-version install); Expectations ≤12 normative bullets (incl. "attach group writes are ONE transaction", "`setup.sh` NEVER runs before the trust flip", "yaml write-back failure never rolls back the attach"); Edge cases with `MaisterError` codes. Surgical cross-ref updates: `flow-packages.md` (link, note the package grouping above revisions), `flows.md`. All tagged (Designed). Gate: `pnpm validate:docs`.
- [x] **T0.4 ERD/db docs (Designed):** `docs/database-schema.md` narrative + `docs/db/projects-domain.md` + `docs/db/erd.md` — `package_sources`, `package_installs`, `project_package_attachments`, nullable `flows.package_install_id` + `capability_imports.package_install_id` FKs, unique constraints (`(source_url, name, resolved_revision)` on installs; `(project_id, package_name)` on attachments).
- [x] **T0.5 OpenAPI (Designed):** `docs/api/web.openapi.yaml` paths — `GET/POST /api/admin/package-sources`, `PATCH/DELETE /api/admin/package-sources/{id}`, `POST /api/admin/package-sources/{id}/refresh`, `POST /api/admin/package-installs` (install source@tag|localPath), project `GET/POST /api/projects/{slug}/packages` (attach), `DELETE .../packages/{attachmentId}` (detach), `POST .../packages/{attachmentId}/upgrade`, `POST .../packages/{attachmentId}/trust` — bodies, status codes (409 PRECONDITION/CONFLICT rows per the failure tables in T3.6), examples. Gate: redocly zero errors.
- [x] **T0.6 `docs/configuration.md`:** `packages[]` field (shape, collision rules, bootstrap + write-back semantics) + `maister-package.yaml` v1 contract (flows/capabilities/mcps/restrictions sections, env:NAME-only rule); note: no new env vars.
- [x] **T0.7 ROADMAP.md:** add unchecked `M33. Package management` milestone (one paragraph, links to design doc + this plan).

### Phase 1: P0 — extraction cleanup (maister side)

- [x] **T1.1 Test fixture snapshot + repoint.** Create `web/test-fixtures/aif-flows/{dev,bugfix,evolve,roadmap,init}/` as a VERBATIM copy of current `plugins/aif/flows/*` (v1 manifests; + `README.md` provenance: "test fixture snapshot; canonical package lives in maister-plugins"). Repoint (path-only change, zero assertion churn): `web/lib/__tests__/aif-flows.test.ts:14`, `web/lib/__tests__/aif-intake-schema.test.ts:13`, `web/lib/flows/__tests__/aif-manifest-m12.test.ts`, `web/lib/flows/__tests__/settings-fixtures.test.ts`, `web/lib/flows/__tests__/package-authoring.test.ts`, `web/lib/flows/__tests__/authored-flow-cli.test.ts`, `web/lib/flows/graph/__tests__/rework-comments.test.ts` (`web/app/(app)/flows/__tests__/page-contract.test.ts` is a mock string — leave). CLI scripts `web/scripts/validate-authored-flow.ts` + `import-flow-package-draft.ts`: make `--source-dir` REQUIRED (clear error with hint), drop the `../plugins/aif` default. Runnability gate: full `test:unit` + `test:integration` green. Logging: n/a (test/fixture move).
- [x] **T1.2 Delete `plugins/aif` + rewire dogfood.** `git rm -r plugins/aif`. Root `maister.yaml`: replace the 6 dead `peaceful-mclean` file:// sources with `/repos/maister-plugins/packages/aif/{capability,flows/*}` (still 6-entry wiring until T2.6; header comment updated — machine-specific dogfood artifact). `.ai-factory/PLAN.md`: replace STATUS header → "SUPERSEDED (2026-06-12) by docs/pv/package-management.md + .ai-factory/plans/feature-package-management.md" (keep body for history). Logging: n/a.
- [x] **T1.3 Docs truth pass.** `docs/flow-aif-plugin.md`: rewrite source-of-truth section (external repo maister-plugins, per-package tag `aif/v2.0.0`, v2 bump summary link, consumption today = 6-entry file://, after P1 = one `packages[]` entry); sweep stale `plugins/aif` references in `docs/` + root/web `CLAUDE.md` files. Gates: `pnpm validate:docs`, full suite green checkpoint.

### Phase 2: P1 — `packages[]` single-import backend (TDD)

- [x] **T2.1 Package manifest schema + loader.** `web/lib/packages/manifest.ts` (+ schema additions in `web/lib/config.schema.ts`): `maisterPackageManifestSchema` = `{schemaVersion: literal(1), name: capabilityRefIdSchema, metadata?: flowMetadataSchema-shape, flows: [{id, path}].min(1), capabilities?: [{id, path}], mcps?: [{id, transport: stdio|http, command?, args?, url?, env?: env:NAME-refs, description?}], restrictions?: [{id, paths: string[].min(1)}]}` `.strict()`; relative-subpath guard on every `path` (no `..`/abs — reuse SAFE_PATH_SEGMENT discipline); dup-id rejection per section; mcps env values MUST match `/^env:[A-Z0-9_]+$/` (CONFIG otherwise). `loadMaisterPackageManifest(pkgRoot)` mirrors `loadFlowManifest` (read `maister-package.yaml`, YAML parse, zod, CONFIG on any reject). TDD first: valid round-trip; missing flows; `../x` + `/abs` paths; dup ids; bad env ref. Logging: DEBUG load (name, flow count), WARN on validation reject (path + zod issues).
- [x] **T2.2 `packages[]` in `maister.yaml`.** `config.schema.ts`: `packageEntrySchema = {id: capabilityRefIdSchema, source: string.min(1), version: NEW slash-allowed tag zod `/^[A-Za-z0-9._+/-]+$/` + no `..` + max 128 (`versionTagSchema` does NOT fit — it rejects `/`), path?: relative-subpath}`; `packages: z.array(...).default([])` on `maisterYamlV2Schema`. `web/lib/config.ts` `loadProjectConfig`: dup `packages[].id` check + cross-list id collision (`packages ∪ flows ∪ capability_imports`) → CONFIG. TDD: parses git-source + file://-source entries with `path`; bad path / dup / cross-collision throw; existing `flows[]`-only configs unchanged (backward compat). Logging: DEBUG packages count in the existing `maister.yaml loaded` line.
- [x] **T2.3 `resolvedRevisionOverride` seam.** `web/lib/flows.ts`: add optional `resolvedRevisionOverride?: string` to `InstallFlowPluginArgs` (≈line 68) → thread into `installRevision` (≈594), used in BOTH source branches in place of computed digest/sha (validate 40-hex/40-char shape first). Same param on `installCapabilityRevision` (`web/lib/capabilities/import.ts` ≈399) + its caller seam in `installAndIngestCapabilityImports`. TDD: local-source install with override lands cache at `<id>@<override>` and `flow_revisions.resolved_revision = override`; absent override = today's behavior (regression). Logging: DEBUG when override applied (flowId, override prefix 12).
- [x] **T2.4 `installPackage` orchestrator.** `web/lib/packages/install.ts`: `installPackage({source, version, path?, projectId, projectSlug, workspaceRoot?, db, signal?})` → (1) resolve pkgRoot ONCE: local dir (`file://`/abs; honor `path` subdir) OR `git clone --branch <version> --depth 1` to tmp (reuse `gitClone`) + `path` join; (2) revision = git ? `gitRevParseHead` : `localDirectoryContentDigest(pkgRoot)`; (3) `loadMaisterPackageManifest(pkgRoot)`; (4) per manifest flow → `installFlowPlugin({source: file://<pkgRoot>/<flow.path>, version, flowId: flow.id, resolvedRevisionOverride: revision, ...})` + validate `loadFlowManifest(...).name === flow.id` (CONFIG); (5) per capability → capability import path with same override; (6) `finally` rm tmp clone. Returns `{revision, flows: InstallResult[], capabilities: [...]}`. ONE clone per package. **Version-label sanitization (CRITICAL):** `versionTagSchema` (web/lib/flow-paths.ts:22, `/^[A-Za-z0-9._+-]+$/`) rejects `/` — sub-installs receive `version.replaceAll("/", "-")` (`aif/v2.0.0` → `aif-v2.0.0`); the RAW tag is used only for `git clone --branch` and the package row. TDD (spy-based: mock both installers + gitClone; tmp fixture package with 2 stub flows + bundle built by the test): per-flow dispatch with correct sub-source/override; name↔id mismatch CONFIG; tmp cleanup on success AND on sub-install throw; `path` subdir variant. Logging: INFO resolve (source, version, sha/digest, pkgRoot), INFO per sub-install (flowId), WARN+rethrow on sub-failure after cleanup.
- [x] **T2.5 Registration expansion.** `web/app/api/projects/route.ts` phase (d) (≈line 343): after the `flows[]` loop + capability imports, `for (const pkg of config.packages) await installPackage(...)`; failures unwind through the EXISTING compensation (project delete cascade + `.maister/<slug>` cleanup + git cleanup, ≈411–456) — no new compensation paths. SET/CLEAR boundary stated in code comment + docs: registration is one-shot bootstrap (same as `flows[]` today); ongoing sync = P2 attach/detach with write-back. Integration test (testcontainers, extend `web/app/api/__tests__/projects-register.integration.test.ts` pattern): maister.yaml with one `packages[]` fixture (tmp dir, 2 flows + bundle) registers 2 flow rows + capability row, all sharing `resolved_revision`; a failing package (bad manifest) → registration fails + compensation leaves no rows/dirs. Logging: INFO packages expansion (pkg id → installed flow ids).
- [x] **T2.6 Dogfood single-import + ops CLI.** Root `maister.yaml`: collapse 6 entries → `packages: [{id: aif, source: /repos/maister-plugins, path: packages/aif, version: aif/v2.0.0}]` (file:// for local dogfood; the git URL + same tag is the production shape). `web/scripts/install-package.ts` mirroring `install-flow.ts` (slug, source, version, path → `installPackage`) for smoke use. Manual smoke (documented in task output, not CI): fresh registration against the dogfood yaml installs 5 flows + bundle with the package digest as revision. Logging: CLI pino INFO results.

### Phase 3: P2 — platform catalog (TDD)

- [x] **T3.1 Migration 0047 + fakeDb.** (fakeDb: no shared stub module — per-test fakes extended reactively; type fixture in `schema.m14.test.ts` migrated) `web/lib/db/schema.ts`: `package_sources` (id uuid, url text unique, enabled bool default true, note?, discovered jsonb default [] — `[{name, tags[], manifestDigest?}]`, last_checked_at?, created/updated); `package_installs` (id, source_url, name, version_label, resolved_revision, manifest jsonb — incl. inventory, manifest_digest, installed_path, package_status enum-reuse, trust_status, created/updated; unique `(source_url, name, resolved_revision)`); `project_package_attachments` (id, project_id FK cascade, package_install_id FK restrict, attached_at, unique `(project_id, package_name)` via denormalized name col); nullable `flows.package_install_id` + `capability_imports.package_install_id` (FK set null). `pnpm db:generate` (0047); migrate-up integration test; extend `fakeDb` stubs with the 3 tables (known gotcha). Logging: n/a (schema).
- [x] **T3.2 Sources + discovery service.** `web/lib/packages/catalog.ts`: sources CRUD (create/update/delete with attached-installs usage guard → CONFLICT); `refreshSource(id)`: `git ls-remote --tags <url>` (60s timeout, redactUrl on errors) → filter `<pkgName>/vX.Y.Z`; depth-1 default-branch clone to tmp → scan `packages/*/maister-package.yaml` (loader from T2.1) → write `discovered` jsonb + `last_checked_at`; failure → keep stale cache, WARN, return typed degraded result (never throw to page). Update-available derivation: attachment's `version_label` semver < max discovered tag for that name. TDD: tag filtering, scan, degraded path, update-badge derivation (pure fn). Logging: INFO refresh (source, packages found, tags count), WARN degraded (cause class, no secrets).
- [x] **T3.3 Install + attach/detach/upgrade + trust fan-out.** `web/lib/packages/attach.ts`: `installPackageRevision` (platform-scope: T2.4 internals minus project wiring; writes `package_installs` row two-phase Installing→Installed); `attachPackage(projectId, packageInstallId)` = ensure sub-revisions installed (idempotent), then ONE `db.transaction`: flows rows (per manifest flow, enabled pointer at the member revision) + capability import row + capability_records ingestion (T3.4) + attachment row + FK group links; pre-guard inside the tx: a manifest flow id colliding with an EXISTING standalone `flows` row of the project (`flows_project_ref_uq`) → CONFLICT, no partial group (config-level collision checks cover only maister.yaml-declared ids — UI attach needs this DB guard); `detachPackage` = live-run guard (any member revision pinned by non-terminal `runs.flow_revision_id` → PRECONDITION) then single tx removing group rows (member `flow_revisions` stay — GC owns them); `upgradeAttachment` = install new revision beside + tx flip of group pointers; `trustPackageRevision` = one tx fanning `trust_status`/`exec_trust='trusted'` to member flow_revisions + capability_imports, THEN `runRevisionSetup` per member with pending setup (after-tx side-effect; setup failure → member revision Failed, attach stays, surface PRECONDITION on launch as today). Crash-window table in code comment per decision #6. TDD integration (testcontainers): attach idempotency, detach guard, upgrade pointer flip leaves in-flight pins intact, trust fan-out + setup ordering (spy), partial-failure leaves no committed intermediate state. Logging: INFO each lifecycle op (ids, revision), WARN guard refusals with reason.
- [x] **T3.4 Typed ingestion.** (implemented inside attach.ts — inventory at install, mcp/restriction records in the attach tx, `origin: package-attachment` CLEAR-exemption in catalog.ts) At platform install: walk bundle dirs → inventory `{skills: [ids], agents: [ids]}` into `package_installs.manifest`. At attach (inside the T3.3 tx): `mcps[]` templates → project MCP catalog entries (reuse project-MCP creation path; flag `origin: 'package'` + attachment FK; env refs validated `env:NAME` at manifest load); `restrictions[]` → `capability_records` kind=`restriction`, `material: {paths}`, source `flow-package`; detach removes BOTH sets in its tx (SET on attach / CLEAR on detach / re-SET on re-attach — all three asserted). TDD integration covering the triple. Logging: INFO ingestion counts per kind.
- [x] **T3.5 `maister.yaml` write-back.** `web/lib/packages/yaml-writeback.ts`: load project `maister.yaml` via `yaml` Document API (`yaml@^2.9.0` already a web dep — nothing new; comment-preserving), upsert/remove the `packages[]` entry (id, source, version, path), serialize → `atomicWriteText`. Called AFTER attach/detach/upgrade tx commit; failure → WARN + typed `writeBack: 'failed'` in the route response (UI notice), DB remains truth. TDD: SET (attach adds entry, comments preserved), CLEAR (detach removes), re-SET (upgrade rewrites version), unwritable file → degraded result not throw. Logging: INFO write-back (path, op), WARN on failure (cause class).
- [x] **T3.6 API routes.** Admin (requirePlatformAdmin, mirror `app/api/admin/acp-runners/*`): `GET/POST /api/admin/package-sources`, `PATCH/DELETE /api/admin/package-sources/[id]`, `POST /api/admin/package-sources/[id]/refresh`, `POST /api/admin/package-installs` (`{sourceId, name, version}` | `{localPath, path?}` — abs-path zod, admin-only per decision #12). Project (authorizeManagePackages pattern from `flow-packages/_lib.ts`): `GET/POST /api/projects/[slug]/packages` (attach `{packageInstallId}`), `DELETE /api/projects/[slug]/packages/[attachmentId]`, `POST .../[attachmentId]/upgrade` (`{packageInstallId}`), `POST .../[attachmentId]/trust`. Each route: identifiers labeled (decision #12), failure table (404 unknown id project-scoped; 409 PRECONDITION live-run/untrusted; 409 CONFLICT usage-guard; 422 CONFIG manifest), responses match T0.5 OpenAPI exactly (drift = bug). TDD: route unit tests (authz, zod rejects) + integration happy paths. Logging: INFO mutations (actor, ids), WARN refusals (code, reason).
- [ ] **T3.7 /settings UI.** `components/settings/package-sources-panel.tsx` + modal (mirror `acp-runners-panel`/`acp-runner-modal` create|edit|delete), platform catalog section: discovered packages × versions per source, installed revisions list, Install + Refresh buttons, last-checked + degraded-stale indicator. i18n EN + RU (REQUIRED — both catalogs). View-only tables, edits in popups (admin UI convention). Component tests via `renderToStaticMarkup` pattern. Logging: client-side none (server actions log).
- [ ] **T3.8 Project UI + whole-package viewer.** Project packages tab: package cards grouping member flows (replace flat list where attached), Attach-from-catalog picker (+ admin local-path attach), Detach/Upgrade/Trust actions, `UpdateAvailable` badge, write-back-failed notice. New RSC viewer page `/projects/[slug]/package-installs/[attachmentId]` (or extend existing packages route — implementer's call, keep `flow_ref_id` viewer intact): manifest metadata, member flow list → existing per-flow viewer links, skills/agents/mcps/restrictions inventory, version/revision/source, trust + setup state. `installed_path` NEVER projected to client (ADR-075 rule). i18n EN+RU. Component tests for the grouping + badge logic. Logging: n/a client.
- [ ] **T3.9 Startup discovery debounce.** `web/instrumentation.ts`: after existing sweeps, fire-and-forget `refreshStaleSources()` (enabled sources with `last_checked_at` null or older than `MAISTER_PACKAGE_DISCOVERY_STALE_HOURS` — env int hours, default 24, zod-parsed once at module load; sequential, per-source try/catch). TDD unit on the staleness filter (env default + override). Logging: INFO sweep summary (refreshed n, degraded m). Env wiring → T3.11.
- [ ] **T3.11 Deployment wiring: `MAISTER_PACKAGE_DISCOVERY_STALE_HOURS`.** New env var per skill-context deployment rule: `.env.example` (committed default `24`), web service `environment:` block in `compose.yml` (+ `compose.production.yml` passthrough if applicable; NO `compose.override.yml` in repo), `docs/configuration.md` env-vars table row. Acceptance: each file carries the var; absent-var default works. Logging: n/a.
- [ ] **T3.10 E2E.** New specs `e2e/platform-package-sources.spec.ts` (settings CRUD + refresh against a seeded LOCAL file:// source fixture — seed builds a tmp package repo dir; no network) + `e2e/project-package-attach.spec.ts` (attach → flows appear on project, detach guard message). ADD both stems to the `AUTHED_SPEC` regex in `web/playwright.config.ts:25` (known gotcha — spec is silently unauthenticated otherwise). Kill 3100/7788 before runs; no `--last-failed`.

### Phase 4: Close-out

- [ ] **T4.1 Docs flip + checkpoint.** Flip (Designed)→(Implemented) across T0.3–T0.6 artifacts; update `docs/pv/package-management.md` status line (P0–P2 shipped; P3–P6 briefs remain); run `/aif-docs` mandatory checkpoint; `pnpm validate:docs:all` + redocly + (if async touched — none expected) asyncapi.
- [ ] **T4.2 Full gates + dogfood smoke.** `pnpm --filter maister-web typecheck` + `test:unit` + `test:integration` + targeted `test:e2e` (new specs + flow-package-viewer) + scoped eslint on every touched path. Dogfood: register maister via the T2.6 `packages[]` yaml → 5 flows + bundle land with package revision; UI: add `maister-plugins` as source (file:// local; git URL once pushed) → refresh → install `aif/v2.0.0` → attach to a second project → launch `aif-dev` smoke to first HITL. Record results in plan completion notes; then `/aif-verify`.

## Open questions — RESOLVED (owner, 2026-06-12)

1. Фикстуры P0 — v1-verbatim снапшот: **ок** (decision #4 stands).
2. Attach по локальному пути из UI — только admin/manageCatalog: **ок** (decision #10 stands).
3. Staleness discovery: **env + дефолт** — `MAISTER_PACKAGE_DISCOVERY_STALE_HOURS` default 24, committed in `.env.example` (decisions #5/#13 updated; T3.11 added).
4. `restrictions[]` в манифесте v1: **ок** (decision #8 stands).
