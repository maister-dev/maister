# Implementation Plan: Experiments → Evaluation Lab Cut-over

Branch: `feature/experiments-cutover` (worktree `../mAIster-feature-experiments-cutover`)
Created: 2026-07-21

## Settings

- Testing: **yes — TDD mandatory.** Every implementation task runs RED → GREEN → refactor. Tests cover required functionality + edge cases with minimum overlap; NO trivial tests (no "constructor works", no unit duplicates of integration coverage).
- Logging: **verbose** (pino; DEBUG flow detail in services, INFO for state changes, ERROR with full context; `LOG_LEVEL`-driven).
- Docs: **yes** — mandatory docs checkpoint; SDD: Phase 0 freezes specs BEFORE code, later phases keep them in lockstep.
- Integration contract: hold branch LOCAL; commits per phase; NO AI/Co-Authored-By trailers; owner integrates via rebase + FF.
- Lint: `eslint --fix <files>` on touched files only — never bare `pnpm lint`.

## Roadmap Linkage

Milestone: **M47 + M48 "UI batch" remainders IN FULL + legacy retirement** (closes the controlled-launch seam production adapter + creation-UI E2E from M47, the pairwise EXECUTION path + standardization routes/UI from M48, and removes the ADR-124 legacy studio; M46/M47/M48 all flip to `[x]`).
Rationale: ROADMAP tags M47/M48 `[~]` with remainders "owner-deferred with the UI batch" — this plan IS that batch (owner decision 2026-07-21: include everything), plus full legacy removal.

## Owner Decisions (locked 2026-07-21 — do not re-litigate)

| # | Decision | Choice |
|---|----------|--------|
| D1 | Ext experiments API + MCP tools | **Immediate removal.** No 410 window, no 1:1 ext replacement (judge path already covered by attempt-bound `evaluation_*` tools; OpenClaw unaffected). |
| D1a | Judge agent (revised 2026-07-21, round 3) | **REFIT, not delete; id STAYS `core:experiment-judge`** (no rename — existing panel bindings keep working; file stem `experiment-judge.md` unchanged). maister-plugins: rewrite the agent's instructions onto attempt-bound `evaluation_*` tools (its current instructions speak the removed `experiment_get`/`experiment_advise`). Web fixture updated in lockstep. Only the legacy judge ROUTE, ext routes, actor gate, and MCP experiment tools are removed. Panels reference judges by free `agentId` (config-schemas.ts:10) — nothing else changes. |
| D7 | maister-plugins release (2026-07-21) | **New core package tag ships IN THIS CYCLE** (allocate next `core/vX.Y.Z` after `git tag -l 'core/*'` in maister-plugins; refit commit + tag are plan deliverables, not deferred owner steps). |
| D2 | Legacy DB tables | **Plain DROP.** No working experiments exist on any installation; no diff-snapshot preservation into new structures. Migration still re-runs the idempotent `evaluation_backfill_from_experiments()` immediately before DROP as a zero-cost safety valve (no-op on empty tables; satisfies the preserve-or-refuse-loudly migration rule), then drops tables + function. `evaluation_studies.legacy_experiment_id` + `legacy_snapshot` columns STAY (historical provenance + `migratedBadge`). |
| D3 | Launch UI scope | **Everything needed, inline.** Full controlled-launch functionality with recipes created inline in the Study Lab launch dialog (all legacy parity axes) + reuse of an existing study recipe in a new batch. No standalone recipe CRUD page. *(Recipe = the immutable "variant passport": flow + packagePin + slot-keyed runner/model + capability overlay + execution policy. Table `evaluation_recipes` already exists; launch-batch items reference `recipeId` — recipes are load-bearing, only their UI wrapper was in question.)* |
| D4 | E2E depth | **Stub-supervisor seeded Playwright** per project convention. New `evaluation-lab.spec.ts`; delete `experiment-comparison.spec.ts`; migrate `forked-package-loop.spec.ts` off the legacy create API. |
| D5 | M48 remainder scope (2026-07-21) | **IN this plan.** Pairwise execution path (match provisioning, match-verdict persistence, `computeTournament` routing, remove the fail-closed gate at dispatcher/start.ts:184-188) + standardization routes/UI over the complete lib (`checkStandardizationEligible`/`standardizeRecipe`/`getCurrentStandardizedRecipe`/`rollbackStandardization`). |
| D6 | Reason rename (2026-07-21) | **Confirmed:** `experiment_member` → `launched_lineage` with full consumer fanout (T3.1). |

## Verified Current State (inventoried 2026-07-21 — 4 parallel explorations; re-verify only if suspicious)

**Goal:** one section (Evaluations) owning observed comparison AND controlled variant launches; no `/experiments` tab, routes, lib, tables, ext ops, MCP tools; external + internal contracts migrated.

- **Controlled-launch core is LIB-ONLY** (zero callers in `web/app`): `web/lib/evaluations/{launch-batch,recipes,recipe-schema,preflight,materialization}.ts` + tables `evaluation_launch_batches`/`evaluation_launch_batch_items` (migration 0112). Key surfaces:
  - `createControlledLaunchBatch({studyId, projectId, requestedByUserId?, idempotencyKey?, items:[{recipeId, replicateCount?}]}) → {batchId, deduped, itemCount}` — durable intent, digest-checked idempotency (CONFLICT on mismatch), CONFIG on kill-switch/empty.
  - `runControlledLaunchBatch(batchId, seam) → {launched, failed, skipped}` — CAS-claims queued items; `retryFailedBatchItems(batchId, maxAttempts)`.
  - `LaunchRunSeam` interface (launch-batch.ts:35-46) — contract FROZEN incl. `launchKey` idempotency; **no default adapter exists** (documented co-evolve debt: adapter = `launchRun({autoPromote:false, ...})`).
  - `preflightControlledRecipe(input) → {ok, refusals[], warnings[]}` — PURE core, 15 typed refusal codes; `PreflightContractLoaders` injectable seam — **live loaders not written**.
  - `resolveRecipeSlotBindings` + `buildMaterializationSnapshot(recipe, resolved) → {slots, capabilityOverlay, materializationIntent, digests}`.
  - Kill switch `controlledRecipesEnabled()`: env `MAISTER_CONTROLLED_RECIPES_ENABLED`, default ENABLED, documented in `docs/configuration.md:1200`. No new env vars needed by this plan (deployment-touchpoint rule satisfied by a verification task).
  - **Verified 2026-07-21 (improve pass):** `launchRun` already accepts `autoPromote?: boolean` (services/runs.ts:354,1669) — the seam contract is real, not aspirational. The batch lib ALREADY enforces study-status gates and ALREADY has stuck-`launching` crash recovery. `launchKey` = the batch item id (:31,:541 — stable across retries; the item PK is never rewritten, only its status/version/runId/attempt columns are). Do NOT re-implement any of this — wire, document, and test-cover it.
  - **CORRECTED 2026-07-21 (implement-time inventory — the improve-pass claim above was too generous; these are the load-bearing details):**
    1. **There are THREE study-status predicates, not one.** Create uses a DENY-list (`:139-144`, `archived|decided` → `CONFLICT`); drive (`:292`,`:307-312`) and retry (`:731-741`) use an ALLOW-list (`draft|open`). They agree only because the enum is exactly those four (schema.ts:2084). A missing study is `PRECONDITION` at create but `terminalize/STUDY_NOT_LAUNCHABLE` at drive.
    2. **The study-status gate PRECEDES the idempotency replay** (`:139` before `:146`) — replaying a batch into a `decided` study throws `CONFLICT`, it does NOT return `deduped:true`. The dedup path returns at `:161` BEFORE the recipe loop, so a replay re-validates nothing.
    3. **`requestDigest` covers ONLY the items array** (`:97-102`) — not studyId/projectId. `stableStringify` preserves array order, so REORDERING items with the same key is a `CONFLICT`.
    4. **Stuck-`launching` recovery is NOT governance-gated and has NO lease/age/heartbeat filter** (`:365-432`, runs before the kill-switch check at `:463`). It matches ANY item in `launching` and cannot distinguish a crashed drive from a live concurrent one. **Its convergence rests entirely on the UNENFORCED `launchKey` idempotency contract — spec this as an assumption, not a guarantee.** Adopt-branch (`:391-411`) omits the `version` predicate the re-queue branch (`:412-431`) carries.
    5. **`preflight.ts` and `materialization.ts` are NEVER imported by `launch-batch.ts`.** The batch FSM does NOT enforce the launch chain — only `parseControlledRecipe`. Every contract-drift/trust/slot refusal must be enforced by the ROUTE (T1.1/T1.3) or the seam (T1.2). Do not spec the batch lifecycle as enforcing preflight.
    6. **`retryFailedBatchItems` has ZERO production callers** — T1.3's retry route is the first, and `maxAttempts` has no default, so the route MUST supply a documented constant. `attempt` is incremented in exactly ONE place (`:638`, the seam-failure path); governance-terminalized items never consume budget, so a study flipped `open→decided→open` can be retried indefinitely. At exhaustion an item silently falls out of the WHERE with no marker, log, or distinct status.
    7. **Known pre-existing defects to DOCUMENT, not fix** (out of scope; record in `evaluations.md` Edge cases): `failed += 1` (`:648`) is unconditional though its UPDATE is status-guarded without `.returning()`, so the returned count can overstate; the launched-item finalize (`:610-622`) has `WHERE id` only despite a comment claiming a status guard; `finalizeBatchStatus` (`:682-690`) has no CAS predicate though schema.ts:3072 documents `version` as the batch CAS guard; `completedAt` is re-stamped on every non-pending finalize and never cleared; retry never calls `finalizeBatchStatus`, leaving stale `status` + `completedAt`; duplicate `recipeId` entries in one request collide as a raw 23505, not a typed error.
    8. **Logging today = 8 statements (1 `info`, 7 `warn`), no `error`/`debug`.** Four carry a `[FIX]` prefix that is aif-fix debug residue, not a production convention. The `reason` field is overloaded: at `:491` it is a typed admission enum, at `:650` it is the RAW error message (kept log-only because it can embed host paths) whose sanitized twin is the separate `code` field.
- **Legacy web surface (to remove):** pages `projects/[slug]/experiments{,/[experimentId]}`; components `web/components/experiments/*` (12 files + 4 tests); routes `api/projects/[slug]/experiments`: GET/POST list-create, GET detail, POST launch/abandon/conclude/judge, GET comparison, GET pin-options; i18n group `experiments` (158 keys EN=RU); ProjectTabs key `experiments` (project-tabs.tsx:12,36,62,80-81 + test).
- **Legacy ext/MCP surface (to remove):** ext `GET .../experiments/{experimentId}` (`experiments:read`) + `POST .../advisory` (`experiments:advise`), both hard-gated to `agentId === "core:experiment-judge"` (`lib/experiments/constants.ts:3`, `advisory.ts:53-59`); MCP `experiment_get`/`experiment_advise` (mcp/src/tools.ts:67,79 + resolveRouting ~712 + TOOL_OP map in tool-contract.test.ts:99-106 derefing `external/operations.openapi.yaml:726,774`); scopes in `types/token-scopes.ts:42-43`, `AGENT_TOKEN_SCOPES`, `PROJECT_ACTION_BY_SCOPE` (ext-handler.ts:109-110), UI labels in `token-actions.tsx:333-335`, `integrations-panel.tsx:182-184`, `personal-tokens-panel.tsx:56-57,150-153`; authz actions `readExperiments|manageExperiments|concludeExperiments` (authz.ts:58-60). MCP facade runs `mcp/dist/main.js` → `pnpm --filter @maister/mcp build` after src edits.
- **Internal coupling (to decouple):** `syncExperimentStatusForRun` + `captureExperimentDiffSnapshotForRun` called from **9 production modules, ~30 sites**: state-transitions.ts (5), keepalive-sweeper.ts (4), runner-graph.ts (6 + materialization-delta imports :211-215,1891-2092), promote.ts (3), hitl.ts (5), workbench-lifecycle/service.ts (2), agents/launch.ts (1), orchestrator/cascade.ts (1), services/runs.ts (1) + 7 test files `vi.mock("@/lib/experiments/status-sync")`. Launch path: services/runs.ts `deriveExperimentMembershipFromSource`, `EXPERIMENT_MEMBER_LAUNCHABLE_STATUSES` (:135-139), `experimentRuns` insert (:1735-1744), draft→running CAS (:1757-1773). Predicates: membership.ts:8 `isExperimentMemberRun` leg of `isLaunchedLineageRun`; auto-promote handler SQL prefilter `NOT EXISTS experiment_runs` (auto-promote.ts:136-139); `NOT_APPLICABLE_REASONS "experiment_member"` (evaluate.ts:53-55); GC `experimentNotBlocked` (workspace-gc.ts:226-237; evaluation hold mirror already exists :238-244); recipe-schema.ts:20 imports `experimentCapabilityOverlaySchema` from `lib/experiments/variant-config`; sync-target.ts `isExperimentMember` view field (:132,163-197,687-769). `supervisor/` has ZERO experiment refs.
- **DB:** tables `experiments` (schema.ts:1941-2000) + `experiment_runs` (:2002-2057, holds diff_snapshot columns); NO experiment columns on runs/workspaces/tasks; `task_activity`/`inbox_items` `event_kind` CHECK includes `experiment_concluded` (KEEP — historical rows); `domain_events` has no experiment kind; `evaluation_studies.legacy_experiment_id`+`legacy_snapshot` (:2096,2102, UNIQUE :2118-2120). Backfill `0110` fn is idempotent + parity-asserting; only callers: migration itself + one integration test.
- **Journal:** highest idx **117** (`0117_new_maggott`, when 1784407016089, snapshot present) → this plan takes **migration 0118**. Highest ADR **148** → this plan takes **ADR-149**. ⚠ The unmerged `authored-caps-edit-lock-unification` plan also claims ADR-149/migr 0118 — renumber-by-provenance applies to whichever merges second; budget a renumber pass.
- **Docs state:** `docs/system-analytics/experiments.md` = legacy-only; **no evaluation-lab system-analytics doc exists** (acknowledged unpaid T0.2 debt); **no `web-evaluations.asyncapi.yaml`** (same debt); `docs/db/erd.md:44` stale "(Designed)" on ADR-124 + EXPERIMENTS/EXPERIMENT_RUNS blocks at :754/:776; `docs/db/evaluations-domain.md` exists (ADR-142..147, 0107–0114); `database-schema.md` documents both families (:56-57,1519-1582 legacy; :58-80,1584-1701 eval); `external/operations.openapi.yaml` experiment paths :726,774 + tag :99-105; `web.openapi.yaml` 8 experiment paths (:2696-3010), 15 evaluation paths; screens doc `docs/screens/projects/project-experiments.md`; glossary rows `docs/CLAUDE.md:109,135`; root `CLAUDE.md:480` mention.
- **Tests/e2e:** `web/e2e/experiment-comparison.spec.ts`; `forked-package-loop.spec.ts` creates an experiment via legacy API; `playwright.config.ts:31` AUTHED_SPEC regex contains `experiment-comparison`; **zero evaluations e2e**. i18n `evaluationsControlled` (36 keys: preflight/preview/policy) exists and is UNUSED by any component. `study-dtos.ts:24` carries `legacyExperimentId` → `migratedBadge`.
- **Authz forward-declarations:** `launchEvaluationRuns` (member) exists unused — the new launch routes MUST consume it; `readEvaluationEvidence`/`runEvaluations` stay reserved (out of scope).
- **Judge panels are agent-id-agnostic:** panel members carry a free-string `agentId` (config-schemas.ts:10); `defaultJudgeSpawn` (judges/launch.ts:457) launches whatever the binding names via `launchAgentRun`. The literal `core:experiment-judge` appears ONLY in `lib/experiments/constants.ts:3` (ext actor gate) + the legacy judge route — the agent itself is the natural panel judge and gets REFIT (D1a), not removed.
- **Pairwise is fail-closed by a single gate:** `dispatcher/start.ts:181-188` throws CONFIG for `aggregation.algorithm === "pairwise_tournament@1"` ("lands with the pairwise UI"); `computeTournament` (aggregation/tournament.ts:187) + `PAIRWISE_TOURNAMENT_ALGORITHM` are ready; `algorithms.ts:117-121` correctly refuses pairwise as scalar. Missing: match provisioning (pair each two participants per ADR-147 D13), per-match verdict persistence shape, aggregation routing to `computeTournament`, UI.
- **Standardization lib is COMPLETE, routes/UI missing:** `standardization.ts` exports `checkStandardizationEligible` / `standardizeRecipe` / `getCurrentStandardizedRecipe` / `rollbackStandardization` (2-phase eligibility→standardize→rollback, append-only audit ledger, admin-only, no Run status change; 8 integration tests exist).
- **Study Lab UI:** sections = header/liveness → participants (+add-observed) → profile+startEvaluation → executions+scoreboard → VerdictPanel. Controlled-launch UI slots between participants and startEvaluation. Props are server-loaded from `[studyId]/page.tsx` via `lab-queries.ts` (3 exports) — new queries land there.
- **Legacy variant parity axes** (create-experiment-modal + variant-editor): per-variant `key`,`label`,`runnerId`,`packagePin` (server-filtered pin-options, ADR-132), `executionPolicy` (JSON), capability overlay add/remove ×4 (rules/skills/mcps/subagents); experiment-level base branch/ref + rubric; replicates = launch-time param, not create-time.

## Gate baseline (captured 2026-07-22 at HEAD `2a2ff3908`, worktree `web/` pristine)

| Gate | Baseline |
|---|---|
| `web` `tsc --noEmit` | **GREEN** (0 errors) |
| `supervisor` `tsc --noEmit` | **GREEN** (0 errors) |
| `validate:docs:all` | **GREEN** (385 mermaid / 728 anchors / 150 files) |
| `validate:contracts` | **GREEN** (7 specs + 5 adapter mirrors) |
| integration (launch-batch, Testcontainers) | **GREEN** 19/19 — Docker + `pgvector:pg16` confirmed available |
| `test:unit` | **RED — 5 pre-existing failures in 2 files** (below) |

`pnpm --filter maister-web test:unit` → **Test Files 2 failed | 718 passed (720); Tests 5 failed | 6959 passed (6964)**.
The gate for every later phase is **zero NEW red vs this list**, not a green suite.

Proof of pre-existence: `git status --porcelain -- web/` is EMPTY (all Phase 0
edits are under `docs/`, `scripts/`, `.ai-factory/`), so vitest loaded the same
`web/` bytes as HEAD.

1. `lib/services/__tests__/runs-launch-branch.test.ts` › *launchRun — experiment membership transaction (ADR-124)* — 4 tests:
   `inherits experiment membership for manual relaunchOfRunId sources`,
   `keeps a relaunch plain when the source run is not an experiment member`,
   `uses the force launchability gate for budget restarts of active experiment members`,
   `does not force a busy non-member budget restart`.
2. `app/api/runs/[runId]/hitl/[hitlRequestId]/respond/__tests__/route.test.ts` › `HITL respond route — kind=budget_breach` › `terminalizes and relaunches a flow restart with recovered launch options` — 1 test.

**These are not incidental.** All four in (1) test the legacy experiment-membership
launch path that **T3.3 deletes outright**, and (2) exercises the same launch path via
budget-breach relaunch. Expect them to be REMOVED (not repaired) in Phase 3 —
if any still exists after T3.3, that is a sign the removal was incomplete.

⚠ **Gate hygiene (learned the hard way this session):** `pnpm test:unit | tail`
reported success while the run had failed — the pipeline returns `tail`'s status.
Always `set -o pipefail`, and read the summary line rather than trusting an exit code
that passed through a pipe.

## Contract-Surface Trace (skill-context rule — every changing surface → its spec file)

| Surface | Change | Spec file(s) |
|---|---|---|
| `POST /api/projects/{slug}/evaluations/studies/{studyId}/launch-preflight` | ADD | `docs/api/web.openapi.yaml` + `docs/system-analytics/evaluations.md` |
| `POST .../studies/{studyId}/launch-batches` (create+kick) | ADD | same |
| `GET .../studies/{studyId}/launch-batches/{batchId}` | ADD | same |
| `POST .../launch-batches/{batchId}/retry` | ADD | same |
| `GET .../evaluations/pin-options` (packagePin picker feed) | ADD (port of legacy pin-options) | same |
| 8 × `/api/projects/{slug}/experiments*` paths | REMOVE | `web.openapi.yaml` (:2696-3010) |
| 2 × `/api/v1/ext/projects/{slug}/experiments*` + `experiments` tag + scopes | REMOVE | `external/operations.openapi.yaml` (:99-105,726,774) |
| `POST /api/v1/ext/evaluations/result` — pairwise pick payload variant | EXTEND (load-bearing field semantics) | `external/operations.openapi.yaml` + mcp tool-contract test (evaluation_result_submit) |
| Standardization routes (eligibility/standardize/rollback/current) | ADD | `web.openapi.yaml` + `docs/system-analytics/evaluations.md` |
| MCP `experiment_get`/`experiment_advise` | REMOVE | `mcp/src/tools.ts` TOOL_SPECS/resolveRouting + `tool-contract.test.ts` TOOL_OP + rebuild `mcp/dist` |
| Study SSE stream events (existing) + batch progress events (if added) | DOCUMENT | NEW `docs/api/async/web-evaluations.asyncapi.yaml` |
| Error codes | no new codes (reuse CONFIG/PRECONDITION/CONFLICT) | `docs/error-taxonomy.md` — verify-only |
| Env vars | none added (kill switch pre-exists) | verify-only vs `docs/configuration.md` + `.env.example` |
| DB: DROP `experiments`,`experiment_runs`, DROP FUNCTION backfill | migration **0118** | `database-schema.md`, `db/erd.md`, `db/evaluations-domain.md` (backfill refs), journal+snapshot triple |
| Authz actions − 3 experiment, + consume `launchEvaluationRuns` | change | `docs/system-analytics/evaluations.md` (authz table) |
| Token scopes − `experiments:read|advise` | REMOVE | `token-scopes.ts` + `operations.openapi.yaml` + 3 token UI panels |
| Screens | `project-experiments.md` retire; NEW `project-evaluations.md` | `docs/screens/**` + README index |
| i18n | − `experiments` (158), + launch-dialog keys into `evaluationsLab`/`evaluationsControlled` | `messages/en.json` = `messages/ru.json` exact parity |
| maister-plugins | retire `core:experiment-judge` agent | plugins repo + web fixture `lib/agents/__tests__/fixtures/core-package/maister-agents/experiment-judge.md` |

## Identifier Trust Labels (new routes; skill-context rule)

All new routes: `slug` = url-param (project resolved server-side); `studyId`/`batchId` = url-param cross-checked against `project.id` via `getStudyForProject`-style ownership guard (server-state) → 404 on mismatch; session = auth-context; body carries ONLY non-locator payload (`items[{recipeId, replicateCount}]`, `idempotencyKey`, recipe definitions). `recipeId` in body is cross-resource → MUST be validated `recipe.studyId === studyId` server-side (mismatch → 409 CONFLICT). No body-controlled filesystem/project locators anywhere.

## Phases & Tasks

### Phase 0 — SDD spec freeze (docs-first; exit = complete + internally consistent specs)

- [x] **T0.1 ADR-149 "Experiments cut-over completion"** in `docs/decisions.md` (allocate at main HEAD; header stub first, then body). Records D1–D6, the controlled-launch wiring surface (routes, seam adapter semantics `autoPromote:false` + forced `evaluation_study` hold, launchKey idempotency), judge REFIT + rename (D1a), predicate migration (`isLaunchedLineageRun` drops the experiment leg), reason rename `experiment_member → launched_lineage`, DROP-with-safety-backfill shape, pairwise-execution wiring decisions (match-verdict persistence shape per ADR-147 D13 — align, don't re-invent), standardization route surface, and Superseded-notes appended to ADR-124/ADR-132 (their experiment-axis parts; packagePin concept itself lives on in recipes). AC: `validate:docs:all` + ADR-anchor script green; every later task cites ADR-149 sections, not chat memory.
- [x] **T0.2 NEW `docs/system-analytics/evaluations.md`** per docs R5 (Purpose / Domain entities / State machines / Process flows / Expectations / Edge cases / Linked artifacts): full Evaluation Lab domain incl. controlled launches — recipe definition + slots, batch FSM (`queued|launching|completed|partial|failed` + item FSM incl. CAS + retry budget), ALL 15 preflight refusal codes exactly as coded, kill-switch behavior (create refuses CONFIG; mid-batch halt leaves items queued; retry no-ops), forced promotion hold, GC hold for open studies, launched-lineage exclusion, authz table. The doc ALSO covers pairwise execution (match provisioning per ADR-147 D13, verdict persistence, `computeTournament` aggregation, quorum/byes), standardization (eligibility → standardize → rollback flows, admin-only, append-only ledger), AND the batch lifecycle behaviors already in the lib: decided/archived-study create-refusal + mid-batch drive halt, stuck-`launching` recovery (adopt-by-participant / re-queue), `launchKey` convergence contract. Every numeric window/constant stated in the doc MUST be checked against the code constant it mirrors (GC-horizon patch precedent: prose "7d" vs `DEFAULT_GC_AGE_DAYS = 14`). Mark `experiments.md` header "(Legacy — removal in progress, ADR-149; superseded by evaluations.md)" now; file deleted in Phase 5. Update `docs/CLAUDE.md` glossary rows (add evaluations.md; annotate experiments.md row). Fix `docs/db/erd.md:44` stale "(Designed)". AC: doc states every transition + refusal exactly as the code gates them (allow-list wording); `validate:docs:all` green. (Pre-existing debt NOT in scope: adding the 18 evaluation ERD blocks to `db/erd.md`, stale `db/README.md` index — note as known debt in the doc.)
- [x] **T0.3 API contract freeze**: `web.openapi.yaml` — add the 5 new evaluation paths PLUS the standardization routes (eligibility GET, standardize POST, rollback POST, current GET — exact paths frozen here) and any pairwise-specific response-shape extensions (execution DTO carrying tournament ranking/matches) (request/response schemas incl. refusals[], batch DTO, per-item statuses, example payloads; identifier labels per table above); NEW `docs/api/async/web-evaluations.asyncapi.yaml` documenting the existing study SSE channel + any batch-progress event added in T1.4 (if none added, document current events only — closing the unpaid debt); mark the 8 experiment web paths + 2 ext paths + tag/scopes for Phase-2/4 removal (comment markers, removed in those phases so `validate:contracts` stays green per phase). AC: `validate:contracts` green; every new route in the yaml has a matching planned route file and vice versa.
- [x] **T0.4 DB migration spec** (in ADR-149 + `database-schema.md` draft). **CORRECTED 2026-07-21 (implement-time finding): this plan needs TWO migrations, not one.** `evaluation_judge_attempts` (schema.ts:2743-2824) has no match-identity columns and its `UNIQUE(execution_id, role, ordinal, retry_ordinal)` has no dimension in which two rows could differ by which PAIR they judge — so T1.7 cannot persist match identity without DDL, and Phase 1.5 precedes Phase 5.
  - **Migration `0118`** carries TWO additive changes (one for Phase 1, one for Phase 1.5):
    - *(Phase 1, T1.2 — seam idempotency)* add `runs.evaluation_batch_item_id text` + partial `UNIQUE INDEX … WHERE evaluation_batch_item_id IS NOT NULL`, mirroring `runs.scheduled_launch_id`. The seam threads the batch item id into the run INSERT so the dedup resolves in the creating statement (adversarial finding — a post-hoc lookup double-launches). No FK.
    - *(Phase 1.5, T1.7 — pairwise)* add `match_a text` + `match_b text` (nullable; NULL for every non-pairwise attempt) to `evaluation_judge_attempts`, and widen the unique to `(execution_id, role, ordinal, retry_ordinal, match_a, match_b)` **with `nullsNotDistinct`** — Postgres treats NULLs as distinct by default, so a plain widened UNIQUE would silently destroy the existing non-pairwise dedup (mirror `evaluationCriterionResults` at schema.ts:2860-2865). No backfill (all existing rows → NULL/NULL).
    - Both land in one migration since Phase 1 precedes Phase 1.5 and both are pure additive DDL; the file is authored once and both features consume it.
  - **Migration `0119` (Phase 5, T5.1)** = `SELECT evaluation_backfill_from_experiments()` (safety re-run; no-op when empty) → `DROP TABLE experiment_runs` → `DROP TABLE experiments` → `DROP FUNCTION evaluation_backfill_from_experiments()`.
  - Journal `when` strictly monotonic after 1784407016089 for BOTH; snapshots generated via `db:generate` (no-drift); `task_activity`/`inbox_items` CHECK kinds untouched. AC: spec names the exact SQL statements for both + the journal/snapshot triples + the fresh-container test proving 0000→0119.
- [x] **T0.5 Spec quality gate**: (a) completeness ✓ — every Contract-Surface Trace row has a frozen spec section; error-codes and env-vars rows verified (all 18 codes pre-exist, `MAISTER_CONTROLLED_RECIPES_ENABLED` documented in `configuration.md:1200` + `.env.example`); (b) consistency ✓ — route paths, the 11-value execution FSM, the 15 preflight codes, and status codes agree across ADR-149 ↔ web.openapi ↔ evaluations.md; (c) no logical holes ✓ — kill-switch/preflight/redirect/batch edges each have stated behavior; (d) ACs ✓; (e) **adversarial-invariant pass DONE** — three parallel skeptics, log below.

**T0.5(e) attack log** (3 agents, 2026-07-22; each verdict traced to a call path; every BREAKS folded back into the spec/plan):

| Invariant | Attack | Verdict | Resolution |
|---|---|---|---|
| I6 seam idempotency | `launchKey` re-read dedup double-launches (no run column; binding written post-seam; lease-free double-drive) | **BREAKS** | ADR-149 + T1.2 rewritten: persist `runs.evaluation_batch_item_id` INSIDE the run INSERT under partial UNIQUE (migr `0118`); re-select loser. |
| Decided-Study / terminalized-item | seam-duration TOCTOU + unguarded success-write resurrects `failed`→`launched` and lets a `decided` Study acquire a participant | **BREAKS** | ADR-149 + T1.3: re-check admission inside the participant tx; CAS the success write on `status='launching'` like the failure write. |
| I6 cap behavior | does `launchRun` throw at cap? | **HOLDS** | Confirmed `launchRun` returns `Pending` (runs.ts:1934), never throws; spec simplified. |
| I4 observed-immutability | any path mutate observed→launched? | **HOLDS** | Only `removedAt` is ever updated; CHECK makes it unrepresentable. Kept as-is. |
| I5 recipe immutability | in-place `definition` rewrite? | **HOLDS (vacuous)** | No production update path; tombstone has no writer — documented in Edge cases. |
| I9 kill switch | stuck-recovery runs before the switch check | **BREAKS (letter)** | ADR-149 + T1.3: hoist `controlledRecipesEnabled()` to drive entry. No run ever escaped. |
| I10 scope removal | `tokenHasScope` never consults the enum; `*` matches all; `?? readBoard` fails open | **BREAKS (my ADR justification was false)** | ADR-149 + T2.1 rewritten: route removal is the mechanism; delete route+map-entry+enum together; regression asserts route 404, not string non-match. |
| I11 standardization admin | lib has NO actor check; cites a nonexistent route | **BREAKS (unenforced)** | ADR-149 + T1.9: service-level `UNAUTHORIZED` on non-human actor; fix stale comment. |
| I1 launched-lineage (relaunch) | ralph-loop + ADR-119 force-relaunch mint UNHELD successors | **BREAKS (pre-existing)** | Out of cut-over scope; documented as "Known lineage gaps" in evaluations.md so the guarantee isn't silently overclaimed. |
| I3 prefilter post-drop | `NOT EXISTS experiment_runs` crashes every tick after `0119` | **UNCERTAIN→confirmed** | T3.4 hardened: REPLACE not delete; cross-check test on a dropped-table schema. |
| I2 backfilled hold | `0110` writes participant but no hold | **UNCERTAIN** | Documented exception in Expectations + Edge cases; lineage predicate still protects. |
| I8 verdict/MCP/scheduler | any automation reach `recordVerdict`? | **HOLDS + flag** | Sole writers are `verdicts.ts`/`standardization.ts`; added `recordVerdict` service guard to T1.9 note as defense in depth. |
| Workbench lifecycle vs lineage | export/snapshot/handoff/drop ignore lineage | **UNCERTAIN (pre-existing)** | Documented in Known lineage gaps; out of cut-over scope. |

Exit: all boxes ticked; every BREAKS either fixed in the frozen spec or explicitly scoped-out with a written reason. Net plan deltas: `runs.evaluation_batch_item_id` added to migration `0118`; T1.2/T1.3/T2.1/T1.9/T3.4 hardened.

Commit checkpoint → **Commit 1** `docs: freeze experiments cut-over specs (ADR-149)`

### Phase 1 — Wire controlled launches (TDD; specs from Phase 0 are the SSOT)

- [x] **T1.1 Live `PreflightContractLoaders`** (`web/lib/evaluations/preflight-loaders.ts`): flow-manifest → `PreflightFlowRevision` projection, method/runner/overlay catalog loaders over real project data (honest-absence: missing catalog → refusal, never fabricated pass) + **`POST .../launch-preflight` route** (authz `launchEvaluationRuns`; body = recipe definition(s); response `{ok, refusals[], warnings[]}` verbatim from core). RED: loader integration tests (Testcontainers) per refusal class that live data can trigger (flow missing / engine floor / runner unknown / overlay unknown / pin invalid) + route test (401/403/404-ownership/422-shape/200). GREEN: implement. Logging: INFO preflight verdict {studyId, ok, refusalCodes}; DEBUG loader resolution.
- [~] **T1.2 Default `LaunchRunSeam` adapter + materialization threading** — CORE DONE (migration 0118 `runs.evaluation_batch_item_id`; `launchRun` persists it + adopts on re-drive; forced `evaluation_study` hold; cap→Pending confirmed; seam `defaultLaunchRunSeam` in `launch-seam.ts`). Verified: 3 new idempotency integ tests + 11/11 lineage-guards no-regression + 29 judge/batch/recipe integ green + tsc clean + zero-new-red on the 5 baseline fails. Fixed a widening regression: the 6-col judge-attempt UNIQUE broke `judges/launch.ts:204` onConflict target (added match_a/match_b). **REMAINING: per-recipe slot-runner override + capabilityOverlay + pinned flow-revision threading — `launchRun` needs a per-session runner-override input the recipe carries (co-evolve, marked in `launch-seam.ts`). Until then a variant runs on the task default flow/runner; executionPolicy IS applied; preflight still refuses incompatible recipes.** (`web/lib/evaluations/launch-seam.ts`): calls `launchRun({autoPromote:false, ...})`, writes the launched participant (tombstone-only semantics preserved), forces the `evaluation_study` promotion hold, threads `buildMaterializationSnapshot` output into the run's session materialization (`run_sessions` ephemeral overrides — the runner-graph consumes the SNAPSHOT, not a live catalog re-read, per the persist-the-launch-decision rule). `launchKey` idempotency: re-invocation with the same key adopts the existing run (no duplicate launch); `launchKey` = the batch item id (launch-batch.ts:541) — the adapter keys its participant/run lookup on it. RED first: integration tests — seam launches a real (stubbed-supervisor) run with hold set + participant row + snapshot persisted; launchKey re-entry adopts; cap-saturated launch → run lands `Pending` and the batch item still records `launched` (queued position), never a false `failed`. This is the highest-risk task — its spec section (ADR-149 §seam) is frozen in Phase 0.
- [ ] **T1.3 Batch routes**: `POST .../launch-batches` (validates recipes ∈ study (409 on mismatch), creates recipes inline when the payload carries definitions (D3), `createControlledLaunchBatch` → kick `runControlledLaunchBatch` with the T1.2 seam post-commit), `GET .../launch-batches/{batchId}` (status DTO incl. per-item state/error/runId), `POST .../retry`. Two-phase order (skill-context): durable intent commit BEFORE any launch side effect (lib already does); route returns 201 with batchId even if the drive loop then halts (kill switch) — items stay queued and visible. RED: route integration tests — idempotencyKey dedup (same digest → same batch, `deduped:true`), digest mismatch → 409, kill-switch-off create → CONFIG 4xx, empty items → CONFIG, RBAC (viewer 403), cross-study recipeId → 409, retry respects maxAttempts budget.
- [ ] **T1.4 Study Lab launch UI**: new section between Participants and Start-evaluation — launch dialog with N inline variant editors (parity axes: label, runner picker, packagePin picker fed by the T1.5 route, executionPolicy JSON field, capability overlay add/remove ×4), replicate count per variant, existing-recipe reuse list, preflight preview step (renders refusals/warnings via `evaluationsControlled` keys — finally consuming them), confirm → POST batch, batch status strip (per-item chips; refresh via existing study SSE or polling — whichever T0.3 froze), kill-switch-disabled state with reason, and the launch affordance HIDDEN/disabled on `decided|archived` studies (launch-preconditions-in-UI rule — the create UI must enforce what the lib refuses). i18n: new keys in `evaluationsLab`/`evaluationsControlled`, EN=RU exact parity. Accessibility per web/CLAUDE.md (portal modal to body, focus trap/restore, aria). RED: `renderToStaticMarkup` contract tests (dialog renders all axes; preview renders refusal copy; disabled state) — no jsdom.
- [ ] **T1.5 `GET .../evaluations/pin-options`**: port the legacy pin-options feed (server-filtered local-cut vs upstream pins by taskId) into the evaluations namespace BEFORE legacy removal; shared logic stays in `web/lib/packages/pin.ts`. RED: route test incl. authz + task-scoped filtering.
- [ ] **T1.6 Phase gate**: full suite green (`pnpm --filter maister-web test:unit` sharded + integration), typecheck ×2, `validate:contracts`. New app-route tests confirmed matched by runner globs (runnability rule).

Commit checkpoint → **Commit 2** `feat(evaluations): wire controlled launches — preflight, batches, Study Lab UI`

### Phase 1.5 — M48 completion: pairwise execution + standardization (D5; TDD)

- [ ] **T1.7 Pairwise execution path**: remove the fail-closed gate (dispatcher/start.ts:181-188) BEHIND a working path — order: (a) match provisioning: for a pairwise-method execution, provision judge attempts per PAIR of participants (round-robin per ADR-147 D13; byes for odd counts), persisting match identity `(a, b)` on the attempt rows per the T0.1-frozen shape; (b) match-verdict persistence: judge `result_submit` for a pairwise attempt records the pick (a|b|tie) — extend the ext result route/facade validation for pairwise payloads (fail-closed for non-pairwise methods stays); (c) aggregation routing: seal/worker path routes pairwise executions through `computeTournament` (never scalar `algorithms.ts`), persisting ranking + per-match outcomes on the aggregate; (d) only then delete the CONFIG gate. RED first: integration — pairwise method execution provisions N·(N−1)/2 matches (+bye), verdicts aggregate to the tournament ranking, quorum shortfall → `partial`/`review_required` per existing FSM, non-pairwise flows untouched (regression). Logging: INFO match provisioning {executionId, matches}; INFO tournament aggregate {executionId, rankingTop}.
- [ ] **T1.8 Pairwise UI**: Study Lab executions section renders tournament results for pairwise methods — ranking list + compact match matrix (pick per cell), quorum/bye badges; scoreboard branch keyed on the method's aggregation algorithm from the execution DTO (frozen in T0.3). i18n additions EN=RU. RED: renderToStaticMarkup contracts (ranking render, matrix render, mixed-status matches).
- [ ] **T1.9 Standardization routes + UI**: routes per T0.3 freeze over the existing lib — eligibility GET, standardize POST, rollback POST, current GET (admin via `requireProjectAction(projectId, "manageProjectEvaluationOverrides")` — the existing project-admin action; do NOT mint a new one). **SERVICE-LEVEL GUARD REQUIRED (adversarial finding B4): the lib takes `actorUserId` as an opaque string and performs NO role/actor-type check, while its comment falsely claims "the route enforces admin". Add an actor-type refusal (`UNAUTHORIZED` on a non-human actor) INSIDE `standardizeRecipe`/`rollbackStandardization`, mirroring `concludeExperiment`'s service guard — route-only enforcement is silently defeated by the next importer. Correct the stale lib comment.** Version guards mirror the lib's advisory-lock + revision backstop. UI: Study page standardization block (eligibility state → standardize confirm with shared portaled confirmation → current-standard badge → rollback) + visibility of the append-only ledger. RED: route integration tests (RBAC 403, eligibility refusals, standardize→current→rollback round-trip, stale-version 409); UI render contracts. Logging: INFO standardize/rollback {studyId, recipeId, actor}.
- [ ] **T1.10 Phase gate**: full suites + typecheck + `validate:contracts`; pairwise + standardization sections of `evaluations.md` verified against shipped behavior (SDD lockstep).

Commit checkpoint → **Commit 3** `feat(evaluations): pairwise execution path + recipe standardization (M48)`

### Phase 2 — External surface removal (D1)

- [ ] **T2.1 Remove ext routes + scopes fanout**: delete `app/api/v1/ext/projects/[slug]/experiments/**`; remove `experiments:read|advise` from `token-scopes.ts`, `AGENT_TOKEN_SCOPES`, `PROJECT_ACTION_BY_SCOPE`; strip labels from the 3 token UI panels; update `operations.openapi.yaml` (paths + tag + scope enums). Grep-gate: zero `experiments:` scope literals outside historical ADR text. **ORDERING IS A CORRECTNESS PROPERTY (adversarial findings B1–B3): the ROUTE deletion is what actually closes the door — `tokenHasScope` compares bare strings and never consults the enum, and `verify.ts` hands DB scopes to the actor unnormalized, so removing the enum value alone is a no-op for matching. Delete the route, its `PROJECT_ACTION_BY_SCOPE` entry, AND the enum value in the SAME change: dropping the map entry while the route survives makes a write route authorize at `readBoard` (viewer), because `projectActionForScope` falls back rather than refusing. Do NOT claim "degrades closed" as the safety mechanism — a wildcard `*` token still matches the removed label; only route removal is load-bearing.** The regression test asserts the deleted ROUTE 404s for a token that formerly carried the scope, not that the string fails to match.
- [ ] **T2.2 Remove MCP tools**: `experiment_get`/`experiment_advise` from TOOL_SPECS + resolveRouting; TOOL_OP entries from `tool-contract.test.ts`; `pnpm --filter @maister/mcp build` (`mcp/dist` is GITIGNORED — the build is for local suites/runtime, not a commit; deployment builds dist itself; check dist mtime > src mtime before trusting a local facade run). tools/tool-contract/scope-contract suites green. AC: contract test still derefs every remaining TOOL_OP op successfully (drift guard intact).
- [ ] **T2.3 REFIT judge agent (D1a + D7)**: maister-plugins repo — rewrite `packages/core/maister-agents/experiment-judge.md` instructions onto the attempt-bound `evaluation_*` tool family (context_get → evidence_list/read → objective_results → result_submit; incl. pairwise pick payloads per T1.7), drop every `experiment_get`/`experiment_advise` mention; **id and file stem stay `core:experiment-judge`/`experiment-judge.md`** — existing panel bindings unaffected. Separate commit in that repo + **new `core/vX.Y.Z` tag in this cycle** (allocate next after `git tag -l 'core/*'`). Web: update fixture `lib/agents/__tests__/fixtures/core-package/maister-agents/experiment-judge.md` in lockstep (byte-identical contract) + any tests pinning its content. The `EXPERIMENT_JUDGE_AGENT_ID` actor gate dies with the ext routes (T2.1 — its only consumers). **The legacy judge ROUTE is NOT deleted here** — `JudgePanel` (verdict-panel.tsx:352) still calls it from the live legacy UI until Phase 4; it moves to T4.2. Known mid-branch degradation (accepted — the branch merges as one unit): between Commits 4 and 6 the legacy "ask judge" button launches a refit agent whose `experiment_*` tools no longer exist, so legacy advisories can't land; the whole legacy surface is removed two phases later.
- [ ] **T2.4 Phase gate**: suites green incl. mcp workspace; `validate:contracts` green with removed paths gone.

Commit checkpoint → **Commit 4** `feat(evaluations)!: remove legacy experiment ext API and MCP tools; refit judge` (+ 1 commit in maister-plugins)

### Phase 3 — Internal decoupling (largest mechanical surface; TDD via invariant tests FIRST)

- [ ] **T3.1 Membership predicate migration**: `isLaunchedLineageRun` drops the `isExperimentMemberRun` leg → evaluation-participants-only. **Invariant test written RED before the change**: a seeded backfilled legacy study (via the 0110 fn on a seeded experiment) keeps its historical run EXCLUDED from auto-promotion/auto-delivery/sync after the leg is removed (proves backfilled participants carry launched semantics). Rename `NOT_APPLICABLE_REASONS "experiment_member" → "launched_lineage"`. **Exhaustive fanout (implement-time finding — 6 literal sites, no DTO/zod/observatory site stores it):** `auto-promotion/evaluate.ts:55` (array member), `evaluate.ts:174` (returned reason), `messages/en.json:4367`, `messages/ru.json:4367`, `auto-promotion/__tests__/evaluate.test.ts:390` (title) and `:397` (asserted value). **The i18n key is interpolated dynamically at `components/runs/auto-promotion-panel.tsx:299` (`t(\`notApplicable.${evaluation.reason}\`)`), so renaming the array member without BOTH catalogs silently breaks the label — the closure test at `evaluate.test.ts:375-383` (iterates `NOT_APPLICABLE_REASONS`, asserts both catalogs have the key) is the tripwire; keep it green.** No persisted rows store the literal (it is computed per request), so no read-time mapping is needed.
- [ ] **T3.2 Status-sync + diff-snapshot call removal**: delete `syncExperimentStatusForRun`/`captureExperimentDiffSnapshotForRun` call sites in ALL 9 modules (~30 sites: state-transitions ×5, keepalive-sweeper ×4, runner-graph ×6, promote ×3, hitl ×5, workbench ×2, agents/launch ×1, cascade ×1, services/runs ×1) + the 7 `vi.mock` test files. Per-module: verify no adjacent logic depends on their return values. Suite green after EACH module (mechanical but sequenced).
- [ ] **T3.3 Launch-path removal in services/runs.ts**: `deriveExperimentMembershipFromSource`, `EXPERIMENT_MEMBER_LAUNCHABLE_STATUSES`, `assertExperimentMembershipLaunchable`, `experimentRuns` insert, `markExperimentRunning` CAS — remove; launched-participant creation is now exclusively the T1.2 seam. runner-graph: remove `buildExperimentMaterializationSelection`/`loadExperimentOverlayForRun`/`persistExperimentMaterializationDelta` — the recipe-snapshot path from T1.2 is the only overlay source for launched participants. RED-first regression: a controlled-launched run still materializes its overlay (snapshot-driven) after legacy path removal.
- [ ] **T3.4 Sweep/GC/promote predicates**: auto-promote handler SQL prefilter (`scheduler/handlers/auto-promote.ts:139`) → `NOT EXISTS (SELECT 1 FROM evaluation_participants ep WHERE ep.run_id = runs.id AND ep.source_type = 'launched')`. **This is REPLACE, never delete (adversarial finding U1): the prefilter references `experiment_runs`, which `0119` drops — leaving it in place raises `relation "experiment_runs" does not exist` and crashes EVERY auto-promote tick instance-wide. A cross-check test must run the real sweep tick against a schema with the legacy table already dropped.** **Two non-negotiables (implement-time finding): `source_type='launched'` MUST be in the predicate (else observed participants leak into the exclusion, violating membership.ts:10-13) and `removed_at` MUST NOT be filtered (a tombstoned launched participant keeps its hold).** GC `experimentNotBlocked` (`gc/workspace-gc.ts:226-237`) removed — **ordering constraint (implement-time finding): the evaluation mirror at `:245-260` covers only `experiment_runs` rows the 0110 backfill converted, so this removal is safe ONLY because (a) T3.3 removes the legacy `experiment_runs` write path at `services/runs.ts:1735-1744` first, and (b) migration `0119` re-runs the backfill before the app accepts traffic. Do not reorder.** Regression: open-study workspace survives GC. sync-target `isExperimentMember` (`runs/sync-target.ts:132`) renamed — **it is server-internal with NO DTO/UI consumer; the rename is confined to `sync-target.ts` + `__tests__/sync-target.integration.test.ts`. The identically-named field on `auto-promotion/evaluate.ts:112` / `readers.ts:121` is a DIFFERENT type — rename the two independently.**
- [ ] **T3.5 Overlay schema ownership**: move `experimentCapabilityOverlaySchema` → `web/lib/evaluations/capability-overlay.ts` (rename `evaluationCapabilityOverlaySchema`), update recipe-schema.ts import; legacy variant-config.ts keeps a re-export until Phase 5 deletion. Byte-identical schema (existing materialize tests keep proving overlay merge).
- [ ] **T3.6 Phase gate**: full suites + typecheck; grep-gate `from "@/lib/experiments` → allowed only inside `lib/experiments/**` itself + variant-config re-export site. Known-red annotation: `experiment-comparison.spec.ts` (playwright, outside unit/integration gates) may assert legacy status auto-transitions removed here — it is knowingly stale from this phase until its deletion in T4.3; NOT a regression.

Commit checkpoint → **Commit 5** `refactor(evaluations): decouple run lifecycle from legacy experiments`

### Phase 4 — UI cut-over + e2e (D4)

- [ ] **T4.1 Redirects**: `projects/[slug]/experiments/page.tsx` → `redirect(/projects/[slug]/evaluations)` (permanent); `[experimentId]/page.tsx` → look up `evaluation_studies.legacy_experiment_id` → redirect to the Study page; unknown id → evaluations list. Deep-link contract tested (3 cases, integration-level).
- [ ] **T4.2 Remove legacy UI + web API**: delete `components/experiments/**` (+4 tests), remaining `api/projects/[slug]/experiments/**` routes **incl. the judge route deferred from T2.3** (+route tests), ProjectTabs `experiments` key (union, order array, label, href, tab test), i18n `experiments` group from BOTH catalogs (158 keys; parity check stays green), `web.openapi.yaml` experiment paths. Redirect pages from T4.1 are the ONLY survivors under the experiments path.
- [ ] **T4.3 E2E**: NEW `web/e2e/evaluation-lab.spec.ts` (stub-supervisor seeded): create study → add observed run → inline-recipe controlled batch launch (2 variants × 1 replicate) → per-item status visible → start evaluation → record verdict. Update `playwright.config.ts` AUTHED_SPEC (`experiment-comparison` → `evaluation-lab`); delete `experiment-comparison.spec.ts`; rewrite `forked-package-loop.spec.ts` provenance assertions over a controlled batch with a packagePin recipe instead of the legacy experiment API. Scope runs by explicit file path; e2e shared-infra ports killed first (project trap).
- [ ] **T4.4 Screens docs**: NEW `docs/screens/projects/project-evaluations.md` (list + Study Lab incl. launch dialog); retire `project-experiments.md`; update screens README index + `docs/CLAUDE.md:135` glossary row.

Commit checkpoint → **Commit 6** `feat(evaluations)!: retire experiments UI — redirects, tab removal, e2e migration`

### Phase 5 — Legacy removal + DB drop (D2)

- [ ] **T5.1 Migration 0118** exactly per T0.4 spec (safety backfill call → 2 DROPs → DROP FUNCTION); journal entry (when > 1784407016089, monotonic) + snapshot via `db:generate` (no-drift); schema.ts tables + relations removed. Tests: journal-integrity + drift suites; fresh-container migration suite proves 0000→0118 on empty AND on seeded-legacy data (backfill-then-drop leaves studies queryable, `legacy_snapshot` intact).
- [ ] **T5.2 Delete `web/lib/experiments/**`** (all 24 files incl. tests) + authz actions `readExperiments|manageExperiments|concludeExperiments` + `docs/system-analytics/experiments.md` + variant-config re-export shim from T3.5.
- [ ] **T5.3 Residual grep-gate** (explicit allow-list; anything else = failure): allowed remaining `experiment` mentions = `legacy_experiment_id`/`legacy_snapshot` columns + their DTO/badge (`migratedBadge`), `task_activity`/`inbox_items` `experiment_concluded` kind (historical CHECK), the judge agent id/fixture `core:experiment-judge`/`experiment-judge.md` (D1a — id kept, instructions refit), historical migration files 0090–0110 (immutable), ADR bodies + plan files + ROADMAP history, backfill-related comments in evaluations code. AC: `grep -ri experiment web/lib web/app web/components mcp/src supervisor/src --include='*.ts*'` output ⊆ allow-list.
- [ ] **T5.4 DB docs as-built**: `database-schema.md` (remove legacy sections :56-57,1519-1582, cascade refs), `db/erd.md` (remove 2 entity blocks + prose), `db/evaluations-domain.md` (backfill-function references → "dropped in 0118, ADR-149").

Commit checkpoint → **Commit 7** `feat(evaluations)!: drop legacy experiment tables and lib (migration 0118)`

### Phase 6 — Verification sweep + final docs

- [ ] **T6.1 Full gate set**: typecheck ×2 (web+supervisor), unit (sharded), integration (real-PG Testcontainers), mcp workspace suites, `validate:docs:all`, `validate:contracts`, journal-integrity, targeted playwright (`evaluation-lab.spec.ts`, `forked-package-loop.spec.ts` by explicit path). Zero-NEW-red vs main baseline; any pre-existing red named explicitly in the completion note. Gate hygiene: `set -o pipefail` / tool-level exit codes — `pnpm <cmd> | tail` lies about failures (2026-07-18 patch precedent: tail's exit 0 hid 4 failures).
- [ ] **T6.2 Docs/ROADMAP final**: ROADMAP — M46 `[x]`; M47 status → seam adapter + creation UI + E2E DONE (pairwise/standardization remainder note stays on M48); glossary/root-CLAUDE.md:480 mention sweep; `docs/CLAUDE.md` rows final. `/aif-docs` checkpoint (Docs: yes).
- [ ] **T6.3 Renumber-collision check** (pre-integration): re-derive `max(ADR)`/`max(journal idx)` at CURRENT main HEAD; if the edit-lock-unification (or any sibling) merged first and took 149/0118 — run the budgeted renumber pass (ADR anchors, migration triple, prose greps `0118`/`ADR-149`, journal `when` re-seat). Renumber hygiene (2026-07-18 patch precedent): any migration that DROP+re-CREATEs a CHECK/enum on a SHARED table must be re-derived from schema.ts against main's newest migration touching the same constraint — 0118 currently drops whole tables only (no re-created constraints), re-verify that stays true after any renumber/rebase.

Commit checkpoint → **Commit 8** `docs: as-built sync for experiments cut-over` (if any drift found in T6.1–T6.3)

## Commit Plan

1. `docs: freeze experiments cut-over specs (ADR-149)` — Phase 0
2. `feat(evaluations): wire controlled launches — preflight, batches, Study Lab UI` — Phase 1
3. `feat(evaluations): pairwise execution path + recipe standardization (M48)` — Phase 1.5
4. `feat(evaluations)!: remove legacy experiment ext API and MCP tools; refit judge` — Phase 2 (+1 commit AND new `core/vX.Y.Z` tag in maister-plugins, D7)
5. `refactor(evaluations): decouple run lifecycle from legacy experiments` — Phase 3
6. `feat(evaluations)!: retire experiments UI — redirects, tab removal, e2e migration` — Phase 4
7. `feat(evaluations)!: drop legacy experiment tables and lib (migration 0118)` — Phase 5
8. `docs: as-built sync for experiments cut-over` — Phase 6 (as needed)

## Test Matrix (TDD; RED first per task; minimal overlap — each behavior asserted at exactly one level)

| Behavior | Level | Phase |
|---|---|---|
| Preflight refusal codes over live loaders (each live-triggerable class once) + honest absence | integration | 1 |
| Batch: idempotency dedup / digest 409 / kill-switch CONFIG / empty CONFIG / cross-study recipe 409 / RBAC | route integration | 1 |
| Batch item CAS two-racer (single item claimed once) | lib integration (exists — extend only if seam changes claims) | 1 |
| Seam: autoPromote:false + forced hold + participant row + snapshot persisted; launchKey re-entry adopts; cap-saturated → Pending not failed | integration | 1 |
| packagePin creatable-now-unlaunchable → launch-time refusal (create-side validate + launch re-validate parity) | integration | 1 |
| Kill switch mid-batch: drive halts, items stay queued, retry no-ops | lib integration (exists — verify covers route path) | 1 |
| Study decided/archived: create refuses (CONFLICT) AND a queued batch's drive halts mid-batch; launch UI hidden on such studies | lib integration (exists :139,:303 — verify) + route/render assertion only where lib gap | 1 |
| Stuck-`launching` item recovery: adopt via existing participant (same launchKey) / re-queue when none | lib integration (exists :365-429 — verify; add route-level only if uncovered) | 1 |
| Launch dialog / preview / disabled render contracts + EN=RU parity | renderToStaticMarkup unit | 1 |
| Pairwise: N participants → N·(N−1)/2 matches (+bye on odd), verdicts → tournament ranking; quorum shortfall → partial/review_required; non-pairwise regression untouched | integration | 1.5 |
| Pairwise result_submit validates pick payload (a\|b\|tie) against match identity; non-pairwise methods still refuse pairwise payloads | route integration | 1.5 |
| Tournament ranking + match matrix render (incl. mixed statuses, byes) | renderToStaticMarkup unit | 1.5 |
| Standardization: RBAC 403 / eligibility refusals / standardize→current→rollback round-trip / stale-version 409 | route integration | 1.5 |
| Removed scopes degrade soft on existing tokens (unknown scope ≠ match) | unit | 2 |
| MCP contract drift guard green post-removal | mcp contract test | 2 |
| Historical backfilled run stays launched-lineage-excluded after predicate leg removal | integration (RED before T3.1) | 3 |
| Controlled-launched run materializes overlay via snapshot after legacy path removal | integration (RED before T3.3) | 3 |
| Launched participant never auto-promotes via REAL scheduler tick | wiring-seam integration | 3 |
| Open-study workspace survives GC post-predicate-removal | integration | 3 |
| Redirects: list / legacy-id→study / unknown-id→list | integration | 4 |
| Study create → observed → controlled batch → verdict end-to-end | playwright stub-supervisor | 4 |
| Migration 0118 on empty + seeded-legacy (studies queryable after drop); journal integrity; snapshot no-drift | migration suites | 5 |
| Residual grep allow-list | scripted gate | 5 |

## Risks

1. **T1.2 seam/materialization threading** — highest complexity; frozen spec first (ADR-149 §seam), snapshot-driven (persist-launch-decision rule), RED integration before code.
2. **ADR-149/migration-0118 collision** with unmerged siblings — T6.3 renumber pass budgeted.
3. **~30-site mechanical removal (T3.2)** — sequenced per-module with suite green between modules; no batch mega-edit.
4. **e2e shared infra** (ports 3100/7788) — kill first, baseline-prove first.
5. **`/tmp` worktrees break React** — this worktree is under `/Users` ✓.

## Unresolved Questions

None. (Round 2: inline recipes OK; rename `launched_lineage` OK (D6); judge REFIT (D1a); M48 remainder included (D5). Round 3 2026-07-21: judge id STAYS `core:experiment-judge` — no rename; core package tag ships in this cycle (D7).)
