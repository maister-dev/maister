# Reviewer

Owns spec compliance and code-quality review after implementation.

Responsibilities:

- Check the delivered behavior against the written spec.
- Look for authorization, validation, i18n, and lifecycle-boundary regressions.
- Verify no raw enum/status values leak into UI copy.
- Verify executable hooks stay behind the trust-gated package lifecycle.
