#!/usr/bin/env bash
set -euo pipefail

# Inert no-op. MAIster delivers AIF skills + subagents via capability
# materialization (no `ai-factory init`, no npm install at setup time).
echo "[aif setup] no-op: skills delivered via MAIster capability materialization" >&2
exit 0
